services:
  oisin-ui:
    build: .
    ports:
      - "3000:3000"
      - "5173:5173"
    volumes:
      - .:/workspace
      - oisin-ui-config:/config
      - ${HOME}/.ssh:/root/.ssh:ro

volumes:
  oisin-ui-config:
