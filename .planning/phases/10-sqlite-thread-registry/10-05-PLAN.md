---
phase: 10-sqlite-thread-registry
plan: 05
type: execute
wave: 4
depends_on: ["10-04"]
files_modified:
  - packages/server/src/server/thread/thread-registry.test.ts
  - packages/server/src/server/thread/session-reaper.test.ts
  - packages/server/src/server/thread/startup-reconcile.test.ts
autonomous: true

must_haves:
  truths:
    - "thread-registry.test.ts tests the SQLite-backed implementation"
    - "session-reaper.test.ts is deleted"
    - "Tests cover: createThread (idle directly), deleteThread, switchThread, getActiveThread, findThreadByAgentId, findThreadByTerminalId, startup reconciliation orphan cleanup"
    - "All tests pass"
  artifacts:
    - path: "packages/server/src/server/thread/thread-registry.test.ts"
      provides: "SQLite-backed ThreadRegistry tests"
    - path: "packages/server/src/server/thread/startup-reconcile.test.ts"
      provides: "Startup reconciliation tests (crash recovery + orphan cleanup)"
  key_links:
    - from: "packages/server/src/server/thread/thread-registry.test.ts"
      to: "packages/server/src/server/thread/thread-registry.ts"
      via: "import ThreadRegistry"
      pattern: "import.*ThreadRegistry"
---

<objective>
Rewrite thread-registry.test.ts for the SQLite backend. Delete session-reaper.test.ts. Add startup-reconcile.test.ts.

Purpose: Verify the new implementation is correct. Tests use an in-memory SQLite DB (`:memory:`) for speed.
Output: Passing test suite covering the critical paths.
</objective>

<execution_context>
@~/.config/Claude/get-shit-done/workflows/execute-plan.md
@~/.config/Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@packages/server/src/server/thread/thread-registry.ts
@packages/server/src/server/thread/thread-registry.test.ts
@packages/server/src/server/thread/startup-reconcile.ts
@packages/server/src/server/thread/db.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite thread-registry.test.ts and delete session-reaper.test.ts</name>
  <files>
    packages/server/src/server/thread/thread-registry.test.ts
    packages/server/src/server/thread/session-reaper.test.ts
    packages/server/src/server/thread/startup-reconcile.test.ts
  </files>
  <action>
**Delete session-reaper.test.ts:**
```bash
rm packages/server/src/server/thread/session-reaper.test.ts
```

**Rewrite thread-registry.test.ts** to test the SQLite-backed implementation.

Use `:memory:` DB for tests — call `initDb(":memory:")` in `beforeEach` and close in `afterEach`.

Test cases to cover:

1. **createThread** — creates row with status='idle' directly, worktreePath stored, returns ThreadRecord
2. **getActiveThread** — null initially; after switchThread, returns correct thread
4. **switchThread** — sets active_thread_id on project; getActiveThread returns it
5. **deleteThread** — row gone; getThread returns null; in-memory agentId map cleared
6. **updateThread with agentId** — agentId stored in memory; findThreadByAgentId returns thread
7. **updateThread with sessionKey** — sessionKey in memory; NOT in DB (verify via direct db.get)
8. **findThreadByTerminalId** — terminal_id in DB; returns correct thread
9. **findThreadByAgentId** — in-memory map; returns correct thread
10. **listThreads** — returns all threads for project
11. **setProjects** — upserts; re-calling with same data doesn't duplicate

**Create startup-reconcile.test.ts** covering:

1. **orphan worktree cleanup** — worktree on disk not in DB → deleteWorktree called
2. **no orphans** — worktree in DB → not deleted
3. **listPaseoWorktrees failure** — error listing worktrees for a project → skipped, no crash

For startup-reconcile tests, mock `listPaseoWorktrees` and `deletePaseoWorktreeChecked` (use vi.mock or jest.mock per project test framework).

Check existing test files to determine test framework (vitest vs jest):
```bash
grep -r "vitest\|jest" packages/server/package.json | head -5
```

Use `:memory:` DB for all tests. Reset DB between tests with `beforeEach`.

**Important:** The `initDb` function uses a module-level `_db` singleton. For tests, you'll need to either:
- Export a `resetDb()` function from db.ts for test use, OR
- Re-import the module fresh each test (harder with ESM)

Simplest approach: add `export function _resetDbForTesting(): void { _db = null; }` to db.ts (clearly marked as test-only). Call it in `afterEach`.
  </action>
  <verify>
    `cd packages/server && npx vitest run src/server/thread/thread-registry.test.ts src/server/thread/startup-reconcile.test.ts`
    All tests pass.
    `ls packages/server/src/server/thread/session-reaper.test.ts` — should fail (deleted).
  </verify>
  <done>
    thread-registry.test.ts and startup-reconcile.test.ts pass. session-reaper.test.ts deleted. No test references to JSON file, ThreadSessionReaper, or provisioning status.
  </done>
</task>

</tasks>

<verification>
- All new tests pass
- `cd packages/server && npx tsc --noEmit` still clean
- `grep -r "session-reaper\|ThreadSessionReaper" packages/server/src/` returns nothing
</verification>

<success_criteria>
Tests cover SQLite-backed ThreadRegistry and startup reconciliation. All pass. Reaper tests gone.
</success_criteria>

<output>
After completion, create `.planning/phases/10-sqlite-thread-registry/10-05-SUMMARY.md`
</output>
