---
phase: 10-sqlite-thread-registry
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - packages/server/src/server/thread/thread-registry.ts
autonomous: true

must_haves:
  truths:
    - "ThreadRegistry public interface is identical to the JSON-backed version"
    - "All reads/writes go to SQLite — no JSON file"
    - "Thread creation writes status='provisioning' first, then updates to 'idle' after worktree creation"
    - "sessionKey and agentId are NOT stored in DB — runtime-only"
    - "findThreadByAgentId and findThreadByTerminalId still work (terminal_id in DB, agentId in-memory map)"
  artifacts:
    - path: "packages/server/src/server/thread/thread-registry.ts"
      provides: "SQLite-backed ThreadRegistry with identical public interface"
      exports: ["ThreadRegistry", "ThreadRecord", "ProjectRecord", "ThreadStatus", "ThreadLaunchConfig"]
  key_links:
    - from: "packages/server/src/server/thread/thread-registry.ts"
      to: "packages/server/src/server/thread/db.ts"
      via: "getDb() calls"
      pattern: "getDb\\(\\)"
    - from: "packages/server/src/server/session.ts"
      to: "packages/server/src/server/thread/thread-registry.ts"
      via: "findThreadByAgentId, findThreadByTerminalId"
      pattern: "findThreadBy"
---

<objective>
Rewrite `thread-registry.ts` to be SQLite-backed. Same exported types and public method signatures. No callers change.

Purpose: Eliminate JSON file race conditions and stale-state bugs.
Output: Fully functional SQLite-backed ThreadRegistry.
</objective>

<execution_context>
@~/.config/Claude/get-shit-done/workflows/execute-plan.md
@~/.config/Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@packages/server/src/server/thread/thread-registry.ts
@packages/server/src/server/thread/db.ts
@packages/server/src/server/session.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite ThreadRegistry with SQLite backend</name>
  <files>
    packages/server/src/server/thread/thread-registry.ts
  </files>
  <action>
Completely rewrite `thread-registry.ts`. Keep ALL exported types and ALL public method signatures identical. Callers must not change.

**Exported types to preserve (same shapes):**
- `ThreadStatus` — z.enum(["provisioning","idle","running","error","closed"]) — ADD "provisioning" to the enum
- `ThreadLaunchConfig`, `ThreadLinks`, `ProjectRecord`, `ThreadRecord`, `ThreadRegistryState`
- `ThreadLinks` — keep `sessionKey` and `agentId` fields as `string | null | undefined` (they're still on the type for callers), but they are NOT written to DB
- `ThreadRecord.links.worktreePath` — still present on type, now sourced from `threads.worktree_path` column

**Runtime-only in-memory state (NOT in DB):**
- `agentId` — keep a `Map<string, string>` (threadKey → agentId) in memory
- `sessionKey` — keep a `Map<string, string>` (threadKey → sessionKey) in memory
- These maps are populated by `updateThread()` calls and cleared on `deleteThread()`

**Key implementation notes:**

`load()`: Run `initDb(dbPath)` if not yet initialized. Return current snapshot. No JSON file.

`getSnapshot()`: Build `ThreadRegistryState` from DB query + in-memory maps. Projects from `projects` table, threads from `threads` table joined with in-memory agentId/sessionKey maps.

`createThread(input)`: 
1. INSERT into threads with `status='provisioning'`, `worktree_path` = the worktreePath from input
2. Return the thread record with status='provisioning'
3. Caller (thread-lifecycle.ts) creates worktree, then calls `updateThreadStatus({status:'idle'})`

Wait — the current `createThread` signature in thread-registry.ts does NOT take worktreePath. Look at the existing signature carefully:
```ts
async createThread(input: {
  projectId: string;
  threadId?: string;
  title: string;
  launchConfig: ThreadLaunchConfig;
  links?: Partial<ThreadLinks>;
  ...
})
```
The `links.worktreePath` is passed via `links`. Keep this signature. But now `worktreePath` is required in DB — so if `links?.worktreePath` is missing, throw an error ("worktreePath is required").

`deleteThread(projectId, threadId)`: DELETE from threads WHERE project_id=? AND thread_id=?. Also clear in-memory agentId/sessionKey maps.

`updateThread(input)`: UPDATE threads SET ... WHERE project_id=? AND thread_id=?. If input.links contains agentId/sessionKey, update in-memory maps only (not DB).

`updateThreadStatus(input)`: UPDATE threads SET status=?, updated_at=?, last_status_at=? WHERE project_id=? AND thread_id=?.

`findThreadByAgentId(agentId)`: Scan in-memory agentId map, then fetch thread from DB.

`findThreadByTerminalId(terminalId)`: SELECT from threads WHERE terminal_id=?.

`setProjects(projects)`: Upsert all projects. Use INSERT OR REPLACE.

`switchThread(projectId, threadId)`: UPDATE projects SET active_thread_id=? WHERE project_id=?. Also clear active_thread_id on other projects if needed (match existing behavior — only one active thread globally).

`getActiveThread()`: SELECT projects WHERE active_thread_id IS NOT NULL, then fetch that thread.

`flush()`: No-op (SQLite writes are immediate). Keep method for interface compat.

`load()`: No-op after first call (DB already open). Keep for interface compat.

**DB row ↔ ThreadRecord mapping:**
- `launch_config` column: `JSON.stringify(launchConfig)` on write, `JSON.parse(launch_config)` on read
- `links` on ThreadRecord: `{ worktreePath: row.worktree_path, terminalId: row.terminal_id ?? null, agentId: inMemoryAgentMap.get(key) ?? null, sessionKey: inMemorySessionMap.get(key) ?? null }`

**Remove entirely:**
- All JSON file read/write logic (`readFile`, `writeFile`, `rename`)
- `THREAD_REGISTRY_FILENAME`, `THREAD_REGISTRY_VERSION` constants
- Legacy compat schema/migration code
- `LegacyCompatibilitySchema`, `LegacyPlaceholderStateSchema`
- `ThreadRegistryStateSchema` (internal, not exported)
  </action>
  <verify>
    `cd packages/server && npx tsc --noEmit` — no errors.
    Grep: `grep -n "readFile\|writeFile\|thread-registry.json" packages/server/src/server/thread/thread-registry.ts` — should return nothing.
    Grep: `grep -n "agentId\|sessionKey" packages/server/src/server/thread/thread-registry.ts` — should show in-memory map handling only, not SQL columns.
  </verify>
  <done>
    ThreadRegistry compiles. All public methods present. No JSON file I/O. agentId/sessionKey in-memory only. worktreePath required on createThread.
  </done>
</task>

</tasks>

<verification>
- `cd packages/server && npx tsc --noEmit` passes
- No callers of ThreadRegistry need changes (session.ts, thread-lifecycle.ts, bootstrap.ts compile)
</verification>

<success_criteria>
SQLite-backed ThreadRegistry with identical public interface. JSON file gone. provisioning status added. agentId/sessionKey runtime-only. worktreePath required and stored in DB.
</success_criteria>

<output>
After completion, create `.planning/phases/10-sqlite-thread-registry/10-02-SUMMARY.md`
</output>
