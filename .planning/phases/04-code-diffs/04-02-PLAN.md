---
phase: 04-code-diffs
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - packages/web/src/thread/thread-store.ts
  - packages/web/src/diff/diff-types.ts
  - packages/web/src/diff/diff-store.ts
  - packages/web/src/main.tsx
autonomous: true
must_haves:
  truths:
    - When users switch active threads, the diff view updates to that thread's changes and stops showing updates from the previous thread.
    - Live diff updates remain correct after thread switches, page activity, and websocket reconnect paths.
    - Users can manually refresh the current thread diff snapshot without reloading the app.
  artifacts:
    - path: packages/web/src/thread/thread-store.ts
      provides: ThreadSummary client type includes worktreePath from server payload.
    - path: packages/web/src/diff/diff-types.ts
      provides: typed diff snapshot, panel state, and per-thread diff cache contracts.
    - path: packages/web/src/diff/diff-store.ts
      provides: useSyncExternalStore diff state with subscribe/unsubscribe lifecycle keyed by active thread.
    - path: packages/web/src/main.tsx
      provides: diff store bootstrap alongside thread store startup.
  key_links:
    - from: packages/web/src/thread/thread-store.ts
      to: packages/web/src/diff/diff-store.ts
      via: active thread identity and worktreePath drive subscription target selection.
      pattern: activeThreadKey|worktreePath|setActiveThread
    - from: packages/web/src/diff/diff-store.ts
      to: packages/web/src/lib/ws.ts
      via: subscribe_checkout_diff_request and unsubscribe_checkout_diff_request messages.
      pattern: subscribe_checkout_diff_request|unsubscribe_checkout_diff_request
    - from: packages/server/src/shared/messages.ts
      to: packages/web/src/diff/diff-types.ts
      via: checkout_diff_update payload shape consumed as typed parsed files.
      pattern: checkout_diff_update|files|ParsedDiffFile
---

<objective>
Build the thread-scoped diff data plane as an external store that owns websocket subscription lifecycle.

Purpose: keep runtime-driven diff state aligned with existing architecture rules (no socket lifecycle in components) and prevent cross-thread diff leakage.
Output: diff-store module with thread-keyed subscription, cache, refresh action, and startup wiring.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-code-diffs/04-CONTEXT.md
@.planning/phases/04-code-diffs/04-RESEARCH.md
@.planning/phases/04-code-diffs/04-code-diffs-01-SUMMARY.md
@packages/web/src/thread/thread-store.ts
@packages/web/src/lib/ws.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add thread-scoped diff external store with ws subscription lifecycle</name>
  <files>packages/web/src/diff/diff-types.ts, packages/web/src/diff/diff-store.ts</files>
  <action>Create a dedicated diff store using `useSyncExternalStore` that keeps panel open state, width percent seed, loading/error status, and diff payload cache keyed by `projectId:threadId`. Implement actions to set active thread context, subscribe using `subscribe_checkout_diff_request`, unsubscribe old subscriptions on thread change/clear, and process `subscribe_checkout_diff_response` + `checkout_diff_update` messages by matching active `subscriptionId`.</action>
  <verify>bun run --filter @oisin/web typecheck</verify>
  <done>Diff state is fully externalized with deterministic thread-keyed subscription/unsubscription and typed payload handling.</done>
</task>

<task type="auto">
  <name>Task 2: Wire thread worktree context and diff-store startup</name>
  <files>packages/web/src/thread/thread-store.ts, packages/web/src/main.tsx</files>
  <action>Extend thread store `ThreadSummary` typing and response handling to retain nullable `worktreePath` from server thread payloads. Start diff store in app bootstrap (`main.tsx`) next to thread store. Expose minimal selectors/helpers needed by App layer to pass active thread identity/worktree into diff store without moving websocket lifecycle into components.</action>
  <verify>bun run --filter @oisin/web typecheck</verify>
  <done>Active thread payload includes worktreePath in client state, and diff store is initialized at app startup for immediate runtime availability.</done>
</task>

</tasks>

<verification>
Run `bun run --filter @oisin/web typecheck` and confirm no React component owns checkout diff websocket lifecycle.
</verification>

<success_criteria>
Switching active thread updates diff subscription target deterministically, previous thread subscriptions are cleaned up, and diff snapshot updates can be requested from store actions.
</success_criteria>

<output>
After completion, create `.planning/phases/04-code-diffs/04-code-diffs-02-SUMMARY.md`
</output>
