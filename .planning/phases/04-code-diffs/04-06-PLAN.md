---
phase: 04-code-diffs
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/server/src/server/utils/diff-highlighter.ts
  - packages/server/src/utils/checkout-git.ts
  - packages/server/src/shared/messages.ts
  - packages/web/src/diff/diff-types.ts
  - packages/web/src/diff/diff2html-adapter.ts
  - packages/web/src/components/diff-file-section.tsx
  - packages/web/e2e/diff-panel.spec.ts
autonomous: true
gap_closure: true
must_haves:
  truths:
    - Diff panel shows per-file uncommitted changes with syntax-highlighted code.
    - Rename, binary, and too-large files render explicit summary rows without broken hunk UI.
    - Rename rows render deterministic `old/path -> new/path` labels from structured payload data.
  artifacts:
    - path: packages/server/src/shared/messages.ts
      provides: ParsedDiffFile schema with explicit rename metadata fields used in checkout diff payloads.
    - path: packages/server/src/utils/checkout-git.ts
      provides: structured diff entries that propagate rename metadata (`oldPath`) for renamed files.
    - path: packages/web/src/diff/diff2html-adapter.ts
      provides: diff2html mapping with language detection and token-aware highlight fidelity path.
    - path: packages/web/src/components/diff-file-section.tsx
      provides: deterministic rename summary labeling and unchanged summary-only handling for binary/too-large states.
    - path: packages/web/e2e/diff-panel.spec.ts
      provides: regression checks for rename summary rendering and highlighted code visibility.
  key_links:
    - from: packages/server/src/utils/checkout-git.ts
      to: packages/server/src/shared/messages.ts
      via: structured parsed file object includes optional oldPath consumed by checkout diff schemas.
      pattern: oldPath|ParsedDiffFileSchema
    - from: packages/server/src/shared/messages.ts
      to: packages/web/src/diff/diff-types.ts
      via: shared payload contract extension mirrored in web parsed diff type.
      pattern: oldPath\?:\s*string
    - from: packages/web/src/diff/diff2html-adapter.ts
      to: packages/web/src/components/diff-file-section.tsx
      via: adapter display path + rendered HTML include deterministic rename and syntax-highlighted output.
      pattern: getDiffFileDisplayPath|language|diff2html-wrapper
---

<objective>
Close remaining DIFF-01 rendering gaps by making rename metadata explicit in the contract and restoring reliable syntax-highlight fidelity in diff rendering.

Purpose: ensure browser diff output is deterministic for rename rows and visibly syntax-highlighted for code hunks.
Output: explicit rename fields in parsed diff payloads, adapter highlighting improvements, and regression coverage for rename/highlight behavior.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-code-diffs/04-CONTEXT.md
@.planning/phases/04-code-diffs/04-code-diffs-VERIFICATION.md
@.planning/phases/04-code-diffs/04-code-diffs-04-SUMMARY.md
@packages/server/src/shared/messages.ts
@packages/server/src/utils/checkout-git.ts
@packages/web/src/diff/diff2html-adapter.ts
@packages/web/src/components/diff-file-section.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add explicit rename metadata to parsed diff contracts</name>
  <files>packages/server/src/server/utils/diff-highlighter.ts, packages/server/src/utils/checkout-git.ts, packages/server/src/shared/messages.ts, packages/web/src/diff/diff-types.ts</files>
  <action>Extend parsed diff file types/schemas with optional `oldPath` rename metadata (present only for renames). Ensure structured diff assembly in `getCheckoutDiff()` propagates `oldPath` from git rename detection into parsed files for normal, placeholder, and fallback file paths where applicable. Keep binary/too_large semantics unchanged. Mirror the contract update in web diff types so rename data is typed end-to-end.</action>
  <verify>bun run --filter @getpaseo/server typecheck && bun run --filter @oisin/web typecheck</verify>
  <done>Checkout diff payloads expose deterministic rename metadata and clients consume it without type casts or path-string inference.</done>
</task>

<task type="auto">
  <name>Task 2: Restore syntax-highlight fidelity and deterministic rename rendering</name>
  <files>packages/web/src/diff/diff2html-adapter.ts, packages/web/src/components/diff-file-section.tsx</files>
  <action>Update adapter mapping to stop forcing `language: 'txt'`; derive language from file path/extension (prefer renamed target path). Implement a token-aware rendering path so syntax styles from parsed diff tokens are preserved in generated output, or wire a verified equivalent highlight route that yields stable highlighted markup classes in the rendered diff HTML. Update rename display helpers to prefer structured `oldPath` metadata and only fall back to path parsing when metadata is absent.</action>
  <verify>bun run --filter @oisin/web typecheck</verify>
  <done>Expanded file hunks render with visible syntax highlighting, and rename rows always show `old/path -> new/path` using structured metadata when available.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression coverage for rename metadata and highlight output</name>
  <files>packages/web/e2e/diff-panel.spec.ts</files>
  <action>Extend diff panel browser regression to assert deterministic rename label rendering and presence of highlight markup/classes in expanded code hunks. Keep existing read-only/collapsed behavior assertions intact and avoid skip patterns that silently bypass these checks when active thread data exists.</action>
  <verify>bun run --filter @getpaseo/server test:e2e -- e2e/diff-panel.spec.ts</verify>
  <done>Automated regression fails when rename metadata is missing/ambiguous or highlighted markup is absent from rendered diff content.</done>
</task>

</tasks>

<verification>
Run server and web workspace typechecks, then targeted diff panel browser regression. Confirm rename rows use explicit metadata and expanded hunks contain highlight-specific markup/classes rather than plain text-only rendering.
</verification>

<success_criteria>
DIFF-01 gap closure complete: rename/binary/too_large states are explicit and deterministic, and rendered code hunks in the diff panel are visibly syntax-highlighted.
</success_criteria>

<output>
After completion, create `.planning/phases/04-code-diffs/04-code-diffs-06-SUMMARY.md`
</output>
