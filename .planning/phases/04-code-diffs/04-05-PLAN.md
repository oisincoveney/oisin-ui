---
phase: 04-code-diffs
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/server/src/server/session.ts
  - packages/server/src/server/daemon-e2e/checkout-diff-subscription.e2e.test.ts
autonomous: true
gap_closure: true
must_haves:
  truths:
    - File rows in diff UI appear in the same order as git diff output.
    - Revisiting a thread after switching still shows that thread's files in stable git order.
    - Files remain collapsed by default while preserving upstream file ordering.
  artifacts:
    - path: packages/server/src/server/session.ts
      provides: checkout diff snapshot generation that preserves getCheckoutDiff().structured order without post-sort.
    - path: packages/server/src/server/daemon-e2e/checkout-diff-subscription.e2e.test.ts
      provides: regression coverage for non-alphabetic git order through initial subscribe and update pushes.
  key_links:
    - from: packages/server/src/utils/checkout-git.ts
      to: packages/server/src/server/session.ts
      via: computeCheckoutDiffSnapshot forwards structured file array without reordering.
      pattern: getCheckoutDiff\(|files\s*=\s*\[\.\.\.\(diffResult\.structured
    - from: packages/server/src/server/session.ts
      to: packages/server/src/server/daemon-e2e/checkout-diff-subscription.e2e.test.ts
      via: checkout_diff_update payload order assertions for mixed non-alphabetic paths.
      pattern: checkout_diff_update|toEqual\(\[
---

<objective>
Close git-order regressions by removing server-side snapshot resorting and locking order behavior with deterministic daemon e2e coverage.

Purpose: satisfy DIFF-01 file-order truths so thread-scoped diffs remain aligned with actual git output.
Output: checkout diff snapshot path preserving upstream order plus regression tests that fail on any alphabetical normalization.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-code-diffs/04-CONTEXT.md
@.planning/phases/04-code-diffs/04-code-diffs-VERIFICATION.md
@.planning/phases/04-code-diffs/04-code-diffs-01-SUMMARY.md
@packages/server/src/server/session.ts
@packages/server/src/server/daemon-e2e/checkout-diff-subscription.e2e.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove checkout snapshot alphabetical normalization</name>
  <files>packages/server/src/server/session.ts</files>
  <action>Update `computeCheckoutDiffSnapshot()` to emit files in the exact order returned by `getCheckoutDiff(..., includeStructured: true)`. Remove path-based sorting logic entirely in the snapshot path and keep this function as a pure forwarder of structured order for both initial subscribe responses and subsequent watch refresh updates. Do not add new client-side sorting workarounds.</action>
  <verify>bun run --filter @getpaseo/server typecheck</verify>
  <done>`checkout_diff_update` and `subscribe_checkout_diff_response` payloads preserve git/listing sequence instead of alphabetical order.</done>
</task>

<task type="auto">
  <name>Task 2: Add non-alphabetic order regression for subscribe and update flows</name>
  <files>packages/server/src/server/daemon-e2e/checkout-diff-subscription.e2e.test.ts</files>
  <action>Strengthen daemon e2e coverage so file names intentionally conflict with alphabetical order and assertions prove git-native sequence survives both initial subscribe payload and at least one subsequent update payload after additional changes. Include a thread-switch/re-subscribe assertion path if needed to prove cached snapshot order remains stable per subscription lifecycle.</action>
  <verify>bun run --filter @getpaseo/server test -- src/server/daemon-e2e/checkout-diff-subscription.e2e.test.ts</verify>
  <done>Regression fails if any server layer re-sorts files alphabetically and passes only when git-order parity is preserved end-to-end.</done>
</task>

</tasks>

<verification>
Run server typecheck and targeted daemon e2e subscription test. Confirm assertions exercise non-alphabetic path sets and cover both first snapshot and follow-up update emissions.
</verification>

<success_criteria>
Server checkout diff payload ordering matches git output across subscribe/update/revisit paths, closing phase verification gaps tied to alphabetical sorting.
</success_criteria>

<output>
After completion, create `.planning/phases/04-code-diffs/04-code-diffs-05-SUMMARY.md`
</output>
