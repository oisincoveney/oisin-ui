---
phase: 04-code-diffs
plan: 03
type: execute
wave: 3
depends_on:
  - "04-02"
files_modified:
  - packages/web/package.json
  - packages/web/src/components/ui/resizable.tsx
  - packages/web/src/components/ui/scroll-area.tsx
  - packages/web/src/components/diff-panel.tsx
  - packages/web/src/components/diff-mobile-sheet.tsx
  - packages/web/src/App.tsx
autonomous: true
must_haves:
  truths:
    - Desktop shows a right-side diff panel that is closed by default and toggled from header controls.
    - Diff panel width is resizable within 30-60% bounds and persisted globally across sessions.
    - Switching threads always closes the diff panel.
    - Mobile uses a full-screen diff sheet with top-left back/close affordance.
  artifacts:
    - path: packages/web/src/components/diff-panel.tsx
      provides: desktop right-side diff container with header actions and file list shell.
    - path: packages/web/src/components/diff-mobile-sheet.tsx
      provides: mobile full-screen sheet shell and close interaction pattern.
    - path: packages/web/src/components/ui/resizable.tsx
      provides: shadcn resizable primitives used for terminal/diff split.
    - path: packages/web/src/App.tsx
      provides: terminal + diff layout composition and thread-switch close behavior.
  key_links:
    - from: packages/web/src/App.tsx
      to: packages/web/src/diff/diff-store.ts
      via: panel open state, width state, and active thread sync actions.
      pattern: useDiffSnapshot|toggleDiffPanel|setDiffPanelWidth|setDiffThreadContext
    - from: packages/web/src/components/diff-mobile-sheet.tsx
      to: packages/web/src/components/ui/sheet.tsx
      via: mobile fullscreen modal behavior.
      pattern: Sheet|SheetContent|side
    - from: packages/web/src/components/diff-panel.tsx
      to: packages/web/src/components/ui/resizable.tsx
      via: bounded panel widths and drag handle.
      pattern: ResizablePanelGroup|ResizablePanel|minSize|maxSize
---

<objective>
Implement the diff panel UX shell (desktop and mobile) with locked layout behaviors before wiring detailed diff rendering.

Purpose: deliver the correct interaction model (toggle, resize, persistence, close-on-switch) so diff content can drop in without layout churn.
Output: shadcn-based panel/sheet components integrated into App layout with persisted width and thread-switch close behavior.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-code-diffs/04-CONTEXT.md
@.planning/phases/04-code-diffs/04-RESEARCH.md
@.planning/phases/04-code-diffs/04-code-diffs-02-SUMMARY.md
@packages/web/src/App.tsx
@packages/web/src/components/ui/sheet.tsx
@packages/web/src/hooks/use-mobile.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add required ShadCN primitives and diff shell components</name>
  <files>packages/web/package.json, packages/web/src/components/ui/resizable.tsx, packages/web/src/components/ui/scroll-area.tsx, packages/web/src/components/diff-panel.tsx, packages/web/src/components/diff-mobile-sheet.tsx</files>
  <action>Add phase-required UI dependencies (`react-resizable-panels`, plus diff renderer deps staged for next plan) in `packages/web/package.json`. Add shadcn `resizable` and `scroll-area` primitives (registry-style patterns). Create `diff-panel` (desktop shell) and `diff-mobile-sheet` (mobile shell) components using only shadcn primitives; include header toggle/close affordances, refresh action slot, and file-list container area. Do not hand-roll custom splitter/sheet logic.</action>
  <verify>bun run --filter @oisin/web typecheck</verify>
  <done>Desktop and mobile diff shell components exist and compile with shadcn primitives only.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate resizable layout + panel lifecycle into App</name>
  <files>packages/web/src/App.tsx</files>
  <action>Replace terminal-only layout with terminal+diff composition: on desktop, use `ResizablePanelGroup` with terminal pane and right diff pane constrained to min 30 / max 60; persist width globally via diff store and restore on reload. Keep panel default closed and keep terminal focus after open. On thread switch, always close panel by invoking diff-store close action from active-thread transition logic. On mobile, open the full-screen diff sheet variant with top-left back/close action.</action>
  <verify>bun run --filter @oisin/web typecheck</verify>
  <done>App enforces locked panel behavior (default closed, bounded persisted resize, close on switch, mobile full-screen sheet) while terminal streaming remains intact.</done>
</task>

</tasks>

<verification>
Run `bun run --filter @oisin/web typecheck` and smoke-check layout behavior in browser: desktop split+resize persistence and mobile sheet open/close.
</verification>

<success_criteria>
Users can open/close a right-side diff panel on desktop and a full-screen sheet on mobile, with globally persisted width and guaranteed close on thread switch.
</success_criteria>

<output>
After completion, create `.planning/phases/04-code-diffs/04-code-diffs-03-SUMMARY.md`
</output>
