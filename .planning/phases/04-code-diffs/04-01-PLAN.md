---
phase: 04-code-diffs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/server/src/shared/messages.ts
  - packages/server/src/server/session.ts
  - packages/server/src/utils/checkout-git.ts
  - packages/server/src/server/daemon-e2e/checkout-diff-subscription.e2e.test.ts
autonomous: true
must_haves:
  truths:
    - Thread payloads expose each thread worktree path so diff subscriptions can target the correct cwd per thread.
    - Diff files keep git command order instead of alphabetical reordering.
    - Checkout diff subscription regressions fail if ordering or thread cwd mapping drifts.
  artifacts:
    - path: packages/server/src/shared/messages.ts
      provides: ThreadSummary schema includes nullable worktreePath and stays wire-compatible.
    - path: packages/server/src/server/session.ts
      provides: toThreadSummary projects thread.links.worktreePath into outbound thread payloads.
    - path: packages/server/src/utils/checkout-git.ts
      provides: getCheckoutDiff preserves listCheckoutFileChanges ordering.
    - path: packages/server/src/server/daemon-e2e/checkout-diff-subscription.e2e.test.ts
      provides: deterministic order assertions aligned to git diff order semantics.
  key_links:
    - from: packages/server/src/server/thread/thread-registry.ts
      to: packages/server/src/server/session.ts
      via: thread.links.worktreePath propagated by toThreadSummary.
      pattern: links\.worktreePath|toThreadSummary
    - from: packages/server/src/server/session.ts
      to: packages/server/src/shared/messages.ts
      via: thread_list_response payload typed against ThreadSummarySchema.
      pattern: thread_list_response|ThreadSummarySchema|worktreePath
    - from: packages/server/src/utils/checkout-git.ts
      to: packages/server/src/server/session.ts
      via: checkout diff subscription payload file order delivered unchanged.
      pattern: getCheckoutDiff|checkout_diff_update
---

<objective>
Align server contracts and diff ordering with Phase 4 locked decisions before web UI work starts.

Purpose: guarantee strict thread->cwd isolation and git-order file lists so downstream diff UI never renders incorrect or shuffled data.
Output: updated thread summary contract, ordering fix in checkout diff pipeline, and regression coverage proving both behaviors.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-code-diffs/04-CONTEXT.md
@.planning/phases/04-code-diffs/04-RESEARCH.md
@.planning/phases/03-project-and-thread-management/03-project-and-thread-management-02-SUMMARY.md
@packages/server/src/shared/messages.ts
@packages/server/src/server/session.ts
@packages/server/src/utils/checkout-git.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expose thread worktree path in thread summary contract</name>
  <files>packages/server/src/shared/messages.ts, packages/server/src/server/session.ts</files>
  <action>Add nullable `worktreePath` to `ThreadSummarySchema` and keep existing fields/backward compatibility intact. Update `toThreadSummary` in session layer to map `thread.links.worktreePath` into thread payloads for list/create/switch responses. Do not derive cwd from repo root when worktree path exists; diff UI must receive real thread worktree path for isolation.</action>
  <verify>bun run --filter @getpaseo/server typecheck</verify>
  <done>Thread payloads now include the per-thread worktree path whenever available, and server typing remains strict and build-safe.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve git diff file order and update subscription regression</name>
  <files>packages/server/src/utils/checkout-git.ts, packages/server/src/server/daemon-e2e/checkout-diff-subscription.e2e.test.ts</files>
  <action>Remove alphabetical resorting in `getCheckoutDiff` so file order follows git diff output order from `listCheckoutFileChanges`. Update daemon e2e assertions to validate git-order behavior (not alphabetic) while still verifying update delivery and subscription correlation by `subscriptionId`.</action>
  <verify>bun run --filter @getpaseo/server test -- checkout-diff-subscription.e2e.test.ts</verify>
  <done>Diff update payload file ordering matches git output order and regression tests catch future ordering regressions.</done>
</task>

</tasks>

<verification>
Run server-only checks for this foundation change: `bun run --filter @getpaseo/server typecheck` and targeted daemon e2e checkout diff subscription test.
</verification>

<success_criteria>
Thread summaries expose worktreePath and checkout diff updates preserve git order, with automated coverage preventing regressions before web diff implementation begins.
</success_criteria>

<output>
After completion, create `.planning/phases/04-code-diffs/04-code-diffs-01-SUMMARY.md`
</output>
