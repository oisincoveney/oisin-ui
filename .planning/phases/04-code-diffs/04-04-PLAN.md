---
phase: 04-code-diffs
plan: 04
type: execute
wave: 4
depends_on:
  - "04-03"
files_modified:
  - packages/web/package.json
  - packages/web/src/diff/diff2html-adapter.ts
  - packages/web/src/components/diff-file-section.tsx
  - packages/web/src/components/diff-panel.tsx
  - packages/web/src/components/diff-mobile-sheet.tsx
  - packages/web/src/index.css
  - packages/web/e2e/diff-panel.spec.ts
autonomous: true
must_haves:
  truths:
    - Diff panel shows per-file uncommitted changes with syntax-highlighted code.
    - Files appear in git diff order and are collapsed by default with path plus +/- counts.
    - Rename, binary, and too-large file states render explicit summary rows without broken hunk UI.
    - Panel supports manual refresh and updates reflect new terminal edits for the active thread.
  artifacts:
    - path: packages/web/src/diff/diff2html-adapter.ts
      provides: mapping from server ParsedDiffFile payloads to diff2html render input with preserved metadata.
    - path: packages/web/src/components/diff-file-section.tsx
      provides: lazy per-file collapsible renderer with default-collapsed behavior and hunk limiting.
    - path: packages/web/src/components/diff-panel.tsx
      provides: changed-file list rendering with metadata rows, refresh trigger, and update status.
    - path: packages/web/e2e/diff-panel.spec.ts
      provides: browser regression coverage for panel rendering and refresh/update behavior.
  key_links:
    - from: packages/web/src/diff/diff-store.ts
      to: packages/web/src/components/diff-panel.tsx
      via: active-thread diff snapshot and refresh action propagation.
      pattern: files|refresh|loading|error
    - from: packages/web/src/components/diff-file-section.tsx
      to: packages/web/src/diff/diff2html-adapter.ts
      via: convert single parsed file to diff2html HTML only when expanded.
      pattern: Collapsible|html\(|toDiff2Html
    - from: packages/web/src/components/diff-panel.tsx
      to: packages/web/e2e/diff-panel.spec.ts
      via: selectors and behavior assertions for collapsed rows, metadata, and refresh.
      pattern: data-testid|diff-panel|refresh
---

<objective>
Deliver final DIFF-01 behavior by rendering thread-scoped uncommitted diffs with syntax highlighting and refresh feedback.

Purpose: complete user-visible diff review flow in-browser while keeping terminal-first workflow and locked organization rules.
Output: diff2html-backed lazy rendering components, file metadata rows, large/binary handling, and browser regression coverage.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-code-diffs/04-CONTEXT.md
@.planning/phases/04-code-diffs/04-RESEARCH.md
@.planning/phases/04-code-diffs/04-code-diffs-03-SUMMARY.md
@packages/server/src/shared/messages.ts
@packages/web/src/diff/diff-store.ts
@packages/web/e2e/thread-sidebar.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement diff2html adapter and lazy per-file renderer</name>
  <files>packages/web/package.json, packages/web/src/diff/diff2html-adapter.ts, packages/web/src/components/diff-file-section.tsx, packages/web/src/index.css</files>
  <action>Add `diff2html` and `highlight.js` dependencies, then build an adapter that converts server `ParsedDiffFile` payloads into diff2html input while preserving git order and file metadata. Implement `diff-file-section` with shadcn `Collapsible`, default collapsed rows, lazy HTML generation on expand, 3-line context default, rename label formatting (`old -> new`), binary summary-only rendering, and large-file behavior showing first N hunks plus explicit expand action.</action>
  <verify>bun run --filter @oisin/web typecheck</verify>
  <done>Per-file diff rendering is syntax-highlighted, lazily computed, and honors locked rename/binary/large-file behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Wire full panel behavior and add browser regression coverage</name>
  <files>packages/web/src/components/diff-panel.tsx, packages/web/src/components/diff-mobile-sheet.tsx, packages/web/e2e/diff-panel.spec.ts</files>
  <action>Complete diff panel composition using diff store snapshot: show changed file rows (path + additions/deletions), collapsed-by-default sections, loading/empty/error states, and manual refresh control. Ensure mobile sheet uses the same diff content behavior. Add Playwright regression spec that verifies: panel opens from active thread, file metadata rows render in expected order, sections are collapsed by default, and refresh/update path reflects new edits from terminal-driven changes.</action>
  <verify>bun run --filter @oisin/web typecheck && bun run --filter @oisin/web test:e2e -- e2e/diff-panel.spec.ts</verify>
  <done>DIFF-01 end-to-end behavior is covered: syntax-highlighted per-thread diffs render correctly and update after refresh/new edits.</done>
</task>

</tasks>

<verification>
Run web workspace typecheck and targeted diff-panel Playwright spec. If Bun e2e startup flakes in this environment, capture failure and rerun once with deterministic startup logs.
</verification>

<success_criteria>
On active thread selection, users can open diff panel/sheet and review syntax-highlighted uncommitted changes by file, then refresh to see new terminal edits without leaving browser.
</success_criteria>

<output>
After completion, create `.planning/phases/04-code-diffs/04-code-diffs-04-SUMMARY.md`
</output>
