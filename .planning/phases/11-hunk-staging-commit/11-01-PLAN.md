---
phase: 11-hunk-staging-commit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/server/src/shared/messages.ts
  - packages/server/src/utils/checkout-git.ts
  - packages/server/src/server/session.ts
autonomous: true

must_haves:
  truths:
    - "Server accepts checkout_stage_request and returns checkout_stage_response"
    - "Server accepts checkout_unstage_request and returns checkout_unstage_response"
    - "Stage/unstage triggers diff subscription refresh"
  artifacts:
    - path: "packages/server/src/shared/messages.ts"
      provides: "CheckoutStageRequest/Response, CheckoutUnstageRequest/Response schemas"
      contains: "checkout_stage_request"
    - path: "packages/server/src/utils/checkout-git.ts"
      provides: "stageFile(), unstageFile() functions"
      exports: ["stageFile", "unstageFile"]
    - path: "packages/server/src/server/session.ts"
      provides: "handleCheckoutStageRequest, handleCheckoutUnstageRequest handlers"
      contains: "handleCheckoutStageRequest"
  key_links:
    - from: "packages/server/src/server/session.ts"
      to: "packages/server/src/utils/checkout-git.ts"
      via: "stageFile/unstageFile import"
      pattern: "import.*stageFile.*checkout-git"
---

<objective>
Add server-side message types and handlers for file-level staging and unstaging.

Purpose: Backend foundation for stage/unstage operations — enables frontend to trigger `git add` and `git reset HEAD` via WebSocket messages.
Output: New message schemas, git wrappers, and session handlers that mirror existing commit endpoint pattern.
</objective>

<execution_context>
@~/.config/Claude/get-shit-done/workflows/execute-plan.md
@~/.config/Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-hunk-staging-commit/11-CONTEXT.md
@.planning/phases/11-hunk-staging-commit/11-RESEARCH.md
@packages/server/src/shared/messages.ts
@packages/server/src/utils/checkout-git.ts
@packages/server/src/server/session.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add message schemas for stage/unstage</name>
  <files>packages/server/src/shared/messages.ts</files>
  <action>
Add four new schemas after CheckoutCommitRequestSchema (around line 764):

1. CheckoutStageRequestSchema:
   - type: z.literal('checkout_stage_request')
   - cwd: z.string()
   - path: z.string()
   - requestId: z.string()

2. CheckoutStageResponseSchema:
   - type: z.literal('checkout_stage_response')
   - payload: { cwd, path, success: boolean, error: CheckoutErrorSchema.nullable(), requestId }

3. CheckoutUnstageRequestSchema:
   - type: z.literal('checkout_unstage_request')
   - cwd: z.string()
   - path: z.string()
   - requestId: z.string()

4. CheckoutUnstageResponseSchema:
   - type: z.literal('checkout_unstage_response')
   - payload: { cwd, path, success: boolean, error: CheckoutErrorSchema.nullable(), requestId }

Add the request schemas to SessionInboundMessageSchema union.
Add the response schemas to SessionOutboundMessageSchema union.
Export all four schemas.
  </action>
  <verify>Run `bun run --filter @getpaseo/server typecheck` — no type errors</verify>
  <done>Message schemas exist and are included in inbound/outbound unions</done>
</task>

<task type="auto">
  <name>Task 2: Add git wrapper functions for stage/unstage</name>
  <files>packages/server/src/utils/checkout-git.ts</files>
  <action>
Add two new exported async functions after commitChanges (around line 1555):

1. stageFile(cwd: string, filePath: string): Promise<void>
   - Call requireGitRepo(cwd)
   - Use execFileAsync("git", ["add", "--", filePath], { cwd })
   - Note: `git add --` handles new, modified, and deleted files correctly

2. unstageFile(cwd: string, filePath: string): Promise<void>
   - Call requireGitRepo(cwd)
   - Use execFileAsync("git", ["reset", "HEAD", "--", filePath], { cwd })
   - Note: Use `git reset HEAD` not `git restore --staged` — reset works for newly-added files that aren't in HEAD yet

Both functions should follow the same pattern as commitChanges: await requireGitRepo, then execFileAsync.
  </action>
  <verify>Run `bun run --filter @getpaseo/server typecheck` — exports resolve</verify>
  <done>stageFile and unstageFile functions exported from checkout-git.ts</done>
</task>

<task type="auto">
  <name>Task 3: Add session handlers for stage/unstage requests</name>
  <files>packages/server/src/server/session.ts</files>
  <action>
1. Import stageFile, unstageFile from checkout-git.ts (add to existing import)

2. Add case handlers in the main message switch (around line 1574, near checkout_commit_request):
   - case 'checkout_stage_request': return this.handleCheckoutStageRequest(msg)
   - case 'checkout_unstage_request': return this.handleCheckoutUnstageRequest(msg)

3. Add handleCheckoutStageRequest method (after handleCheckoutCommitRequest):
   - Extract { cwd, path, requestId } from msg
   - try: await stageFile(cwd, path)
   - Call this.scheduleCheckoutDiffRefreshForCwd(cwd)
   - Emit checkout_stage_response with success: true
   - catch: Emit checkout_stage_response with success: false, error: this.toCheckoutError(error)

4. Add handleCheckoutUnstageRequest method (after handleCheckoutStageRequest):
   - Same pattern as stage, but call unstageFile instead
   - Emit checkout_unstage_response

Follow the exact pattern of handleCheckoutCommitRequest for consistency.
  </action>
  <verify>Run `bun run --filter @getpaseo/server typecheck` — no type errors</verify>
  <done>Session handles checkout_stage_request and checkout_unstage_request messages</done>
</task>

</tasks>

<verification>
1. `bun run --filter @getpaseo/server typecheck` passes
2. `bun run --filter @getpaseo/server build` succeeds
3. grep confirms: `checkout_stage_request` in messages.ts, `stageFile` in checkout-git.ts, `handleCheckoutStageRequest` in session.ts
</verification>

<success_criteria>
- Server compiles without errors
- New message types are in the schema unions
- stageFile/unstageFile functions are exported
- Session handlers exist and follow commit handler pattern
</success_criteria>

<output>
After completion, create `.planning/phases/11-hunk-staging-commit/11-01-SUMMARY.md`
</output>
