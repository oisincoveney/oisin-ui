---
phase: 03-project-and-thread-management
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/server/src/shared/messages.ts
  - packages/server/src/server/session.ts
  - packages/server/src/server/thread/thread-lifecycle.ts
  - packages/server/src/server/daemon-e2e/thread-management.e2e.test.ts
  - packages/server/e2e/thread-management-web.spec.ts
autonomous: true
gap_closure: true
must_haves:
  truths:
    - User-selected base branch in New Thread dialog is honored when creating the thread worktree.
    - User-selected command mode and args are honored when starting the thread agent.
    - Create-thread inline error UX remains intact while branch/command fields are now end-to-end wired.
  artifacts:
    - path: packages/server/src/shared/messages.ts
      provides: thread_create schema accepts and validates baseBranch and commandOverride payloads.
    - path: packages/server/src/server/session.ts
      provides: thread_create handler forwards baseBranch and commandOverride into lifecycle create path.
    - path: packages/server/src/server/thread/thread-lifecycle.ts
      provides: lifecycle create uses requested baseBranch and launchConfig.commandOverride for provisioning.
    - path: packages/server/src/server/daemon-e2e/thread-management.e2e.test.ts
      provides: daemon regression coverage for baseBranch + commandOverride behavior.
  key_links:
    - from: packages/web/src/thread/thread-store.ts
      to: packages/server/src/shared/messages.ts
      via: thread_create_request payload parsing for baseBranch and launchConfig.commandOverride.
      pattern: thread_create_request|baseBranch|commandOverride
    - from: packages/server/src/server/session.ts
      to: packages/server/src/server/thread/thread-lifecycle.ts
      via: createThread call includes user-selected baseBranch and commandOverride.
      pattern: createThread\(|baseBranch|commandOverride
    - from: packages/server/src/server/thread/thread-lifecycle.ts
      to: packages/server/src/server/agent/agent-manager.ts
      via: agent launch config consumes command override mode/args.
      pattern: commandOverride|mode|argv
---

<objective>
Close the Phase 3 verification gap by wiring create-thread base branch and command override from web payload to server lifecycle execution.

Purpose: make thread creation inputs truthful so users can control starting branch and launch command as the dialog promises.
Output: updated thread-create contract/lifecycle plus regression tests proving branch/command behavior is applied.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-project-and-thread-management/03-project-and-thread-management-VERIFICATION.md
@.planning/phases/03-project-and-thread-management/03-project-and-thread-management-03-SUMMARY.md
@packages/server/src/shared/messages.ts
@packages/server/src/server/session.ts
@packages/server/src/server/thread/thread-lifecycle.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire baseBranch + commandOverride through thread_create contract and lifecycle</name>
  <files>packages/server/src/shared/messages.ts, packages/server/src/server/session.ts, packages/server/src/server/thread/thread-lifecycle.ts</files>
  <action>Add `baseBranch` to thread_create request schema and keep launchConfig commandOverride shape strict/typed in shared messages. Update thread_create session handler to pass parsed `baseBranch` and `launchConfig.commandOverride` into lifecycle create arguments. In lifecycle create flow, prefer caller-provided base branch (fallback to current/default branch only when absent) and apply command override when starting the agent. Do not silently drop these fields; if unsupported/invalid, return structured RPC validation/handler errors so inline dialog errors remain meaningful.</action>
  <verify>bun run --filter @getpaseo/server typecheck</verify>
  <done>Thread create payload fields for base branch and command override are accepted and consumed end-to-end, with explicit error behavior on invalid inputs.</done>
</task>

<task type="auto">
  <name>Task 2: Add regression coverage for branch and command override behavior</name>
  <files>packages/server/src/server/daemon-e2e/thread-management.e2e.test.ts, packages/server/e2e/thread-management-web.spec.ts</files>
  <action>Extend daemon e2e to assert thread_create with explicit baseBranch creates worktree from that branch and commandOverride reaches launched agent command configuration. Extend browser thread-management spec to exercise New Thread dialog branch + command fields and verify failures still render inline, not as detached toasts. Keep assertions deterministic and scoped to existing Phase 3 harness patterns.</action>
  <verify>bun run --filter @getpaseo/server test -- thread-management.e2e && bun run --filter @getpaseo/server test:e2e -- e2e/thread-management-web.spec.ts</verify>
  <done>Automated tests fail if baseBranch/commandOverride wiring regresses and pass only when create-thread UI contract is true end-to-end.</done>
</task>

</tasks>

<verification>
Run Bun workspace checks for this gap closure only: `bun run --filter @getpaseo/server typecheck`, daemon e2e thread-management target, and browser thread-management spec.
</verification>

<success_criteria>
Phase 3 gap is closed: New Thread dialog branch + command inputs are applied by server create lifecycle, and regression tests guard the contract from future drift.
</success_criteria>

<output>
After completion, create `.planning/phases/03-project-and-thread-management/03-project-and-thread-management-05-SUMMARY.md`
</output>
