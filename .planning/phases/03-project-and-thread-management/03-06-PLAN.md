---
phase: 03-project-and-thread-management
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - paseo.json
  - packages/server/src/server/daemon-e2e/thread-management.e2e.test.ts
  - packages/server/e2e/thread-management-web.spec.ts
autonomous: true
gap_closure: true
must_haves:
  truths:
    - User can create a thread from the web UI for this repo without the create action hanging on "Creating...".
    - Thread creation no longer fails because bootstrap expects npm lockfiles in a Bun-lockfile repo.
    - Runtime regression coverage fails if lockfile-incompatible setup reappears in thread creation flow.
  artifacts:
    - path: paseo.json
      provides: Worktree setup command uses Bun-compatible install bootstrap instead of npm ci.
    - path: packages/server/src/server/daemon-e2e/thread-management.e2e.test.ts
      provides: Daemon thread-create lifecycle regression proving setup succeeds in a bun.lock repository.
    - path: packages/server/e2e/thread-management-web.spec.ts
      provides: Browser create-thread regression proving UI create completes and new thread appears for Bun-lockfile setup runtime.
  key_links:
    - from: paseo.json
      to: packages/server/src/utils/worktree.ts
      via: worktree.setup commands executed during thread create bootstrap.
      pattern: worktree.setup|runWorktreeSetupCommands
    - from: packages/server/src/server/session.ts
      to: packages/server/src/server/thread/thread-lifecycle.ts
      via: thread_create request path invokes lifecycle create that triggers worktree setup.
      pattern: thread_create_request|createThread
    - from: packages/web/src/components/thread-create-dialog.tsx
      to: packages/server/e2e/thread-management-web.spec.ts
      via: UI create flow assertion that dialog resolves and thread row renders.
      pattern: Create Thread|thread-alpha|data-active
---

<objective>
Close the Phase 3 operational runtime gap where thread creation hangs because worktree setup uses npm lockfile semantics in a Bun-lockfile repository.

Purpose: keep create-thread usable in the Docker-served runtime for this repo's actual lockfile strategy.
Output: lockfile-compatible worktree setup command plus daemon/browser regressions covering this runtime path.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-project-and-thread-management/03-project-and-thread-management-VERIFICATION.md
@.planning/phases/03-project-and-thread-management/03-project-and-thread-management-05-SUMMARY.md
@paseo.json
@packages/server/src/utils/worktree.ts
@packages/server/src/server/thread/thread-lifecycle.ts
@packages/server/src/server/daemon-e2e/thread-management.e2e.test.ts
@packages/server/e2e/thread-management-web.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make worktree bootstrap command lockfile-compatible for this repo runtime</name>
  <files>paseo.json</files>
  <action>Replace the first worktree setup command from `npm ci` to a Bun-compatible install command that honors `bun.lock` (for example `bun install --frozen-lockfile`). Keep remaining setup steps unchanged. Do not introduce npm-lockfile assumptions in worktree setup for this repo.</action>
  <verify>bun run --filter @getpaseo/server typecheck</verify>
  <done>Thread bootstrap no longer requires package-lock.json and can execute setup in Bun-lockfile worktrees.</done>
</task>

<task type="auto">
  <name>Task 2: Add runtime-path regressions for Bun-lockfile thread creation</name>
  <files>packages/server/src/server/daemon-e2e/thread-management.e2e.test.ts, packages/server/e2e/thread-management-web.spec.ts</files>
  <action>Add focused coverage that creates threads against repos configured with `bun.lock` + `paseo.json` worktree setup and asserts create succeeds end-to-end (accepted server response and visible thread row in web flow). Include an assertion that no create-thread error path is returned for setup bootstrap. Keep fixture setup minimal and deterministic; avoid broad refactors or unrelated UI assertions.</action>
  <verify>bun run --filter @getpaseo/server test -- thread-management.e2e && bun run --filter @getpaseo/server test:e2e -- e2e/thread-management-web.spec.ts</verify>
  <done>Automated coverage reproduces this lockfile strategy and fails if create-thread regresses into setup-command hang/failure.</done>
</task>

</tasks>

<verification>
Validate the targeted runtime gap only: server typecheck plus thread-management daemon and web e2e specs covering Bun-lockfile setup during thread create.
</verification>

<success_criteria>
From web UI, creating a thread in a configured project on this runtime completes successfully, setup uses Bun-lockfile-compatible bootstrap, and regressions guard the path.
</success_criteria>

<output>
After completion, create `.planning/phases/03-project-and-thread-management/03-project-and-thread-management-06-SUMMARY.md`
</output>
