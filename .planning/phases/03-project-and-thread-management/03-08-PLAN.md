---
phase: 03-project-and-thread-management
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - paseo.json
  - packages/server/src/utils/worktree.test.ts
  - packages/server/src/server/daemon-e2e/thread-management.e2e.test.ts
  - packages/server/e2e/thread-management-web.spec.ts
autonomous: true
gap_closure: true
must_haves:
  truths:
    - Create Thread succeeds from the web UI for a configured project without setup failing on npm workspace resolution.
    - Worktree setup commands in this repo no longer use npm `--workspace` syntax that is incompatible with current runtime/worktree layout.
    - Regression tests fail if `No workspaces found` reappears in the create-thread setup path.
  artifacts:
    - path: paseo.json
      provides: Canonical worktree setup command list aligned to bun + current repo workspace layout.
    - path: packages/server/src/utils/worktree.test.ts
      provides: Contract regression ensuring default setup commands do not include npm workspace-incompatible relay build command.
    - path: packages/server/src/server/daemon-e2e/thread-management.e2e.test.ts
      provides: Daemon create-thread regression asserting setup path succeeds and does not surface `No workspaces found`.
    - path: packages/server/e2e/thread-management-web.spec.ts
      provides: Browser regression asserting Create Thread dialog completes with no setup workspace failure for configured project flow.
  key_links:
    - from: paseo.json
      to: packages/server/src/utils/worktree.ts
      via: worktree setup commands executed during thread create bootstrap.
      pattern: worktree.setup|runWorktreeSetupCommands
    - from: packages/server/src/server/session.ts
      to: packages/server/src/server/thread/thread-lifecycle.ts
      via: thread_create request invokes lifecycle create path that runs setup commands.
      pattern: thread_create_request|createThread
    - from: packages/web/src/components/thread-create-dialog.tsx
      to: packages/server/e2e/thread-management-web.spec.ts
      via: dialog submit path must close successfully without inline setup workspace errors.
      pattern: Create Thread|No workspaces found
---

<objective>
Close the new Phase 3 live UI gap where Create Thread fails during worktree setup with `npm run build --workspace=@getpaseo/relay ... No workspaces found`.

Purpose: keep thread creation reliable in Docker web runtime by aligning setup commands to this repo's bun/workspace reality.
Output: updated `paseo.json` setup commands plus daemon/browser regressions that catch this exact failure mode.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-project-and-thread-management/03-project-and-thread-management-VERIFICATION.md
@.planning/phases/03-project-and-thread-management/03-project-and-thread-management-07-SUMMARY.md
@paseo.json
@packages/server/src/utils/worktree.ts
@packages/server/src/utils/worktree.test.ts
@packages/server/src/server/daemon-e2e/thread-management.e2e.test.ts
@packages/server/e2e/thread-management-web.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove npm-workspace-incompatible setup step from canonical worktree bootstrap</name>
  <files>paseo.json</files>
  <action>Update `worktree.setup` to a command sequence fully compatible with this repository's package manager/workspace model. Remove the obsolete `npm run build --workspace=@getpaseo/relay` step (no local relay workspace exists in this runtime). Keep bun-based install/setup semantics and existing env-copy behavior intact. Do not leave any `npm ... --workspace=...` setup command in this file.</action>
  <verify>bun run --filter @getpaseo/server typecheck</verify>
  <done>`paseo.json` contains no npm workspace setup command; create-thread setup no longer references missing `@getpaseo/relay` workspace.</done>
</task>

<task type="auto">
  <name>Task 2: Add regression coverage for the `No workspaces found` create-thread failure mode</name>
  <files>packages/server/src/utils/worktree.test.ts, packages/server/src/server/daemon-e2e/thread-management.e2e.test.ts, packages/server/e2e/thread-management-web.spec.ts</files>
  <action>Add targeted regressions that lock this gap: (1) a config/contract test asserting default setup commands exclude npm workspace syntax and specifically the removed relay build command, and (2) daemon + browser create-thread flow coverage asserting setup succeeds and response/UI does not surface `No workspaces found`. Keep fixtures deterministic and focused on setup-command compatibility, not unrelated UI behavior.</action>
  <verify>bun run --filter @getpaseo/server test -- worktree.test.ts thread-management.e2e.test.ts && bun run --filter @getpaseo/server test:e2e -- e2e/thread-management-web.spec.ts</verify>
  <done>Test suite fails if npm-workspace setup command is reintroduced or if create-thread returns the workspace failure in daemon/web flows.</done>
</task>

</tasks>

<verification>
Confirm create-thread setup no longer uses incompatible npm workspace syntax and that targeted unit + daemon + browser regressions pass with no `No workspaces found` failure path.
</verification>

<success_criteria>
For configured projects, Create Thread succeeds end-to-end in the web UI; canonical worktree setup commands are bun/workspace-compatible for this repo; and regressions catch reintroduction of the prior npm workspace failure.
</success_criteria>

<output>
After completion, create `.planning/phases/03-project-and-thread-management/03-project-and-thread-management-08-SUMMARY.md`
</output>
