---
phase: 03-project-and-thread-management
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/server/src/utils/worktree.ts
  - paseo.json
  - packages/server/src/utils/worktree.test.ts
  - packages/server/src/server/daemon-e2e/thread-management.e2e.test.ts
  - packages/server/e2e/thread-management-web.spec.ts
autonomous: true
gap_closure: true
must_haves:
  truths:
    - Creating a thread from the web UI succeeds in this runtime even when Bun frozen-lockfile install would otherwise fail during worktree setup.
    - Worktree bootstrap remains deterministic and safe: setup does not require manual lockfile edits and does not silently mutate tracked repo state.
    - Regression coverage fails if the `lockfile had changes, but lockfile is frozen` failure path causes create-thread to error again.
  artifacts:
    - path: packages/server/src/utils/worktree.ts
      provides: Guarded worktree-setup handling for Bun frozen-lockfile mismatch with deterministic retry/cleanup behavior.
    - path: paseo.json
      provides: Runtime setup command sequence aligned with guarded Bun install behavior for this repo/worktree bootstrap path.
    - path: packages/server/src/utils/worktree.test.ts
      provides: Unit coverage for setup-command failure classification and retry/no-retry safety rules.
    - path: packages/server/src/server/daemon-e2e/thread-management.e2e.test.ts
      provides: Daemon create-thread regression reproducing frozen-lockfile mismatch and asserting successful thread creation after fix.
    - path: packages/server/e2e/thread-management-web.spec.ts
      provides: Browser regression proving Create Thread completes (no error path) for the exact frozen-lockfile mismatch scenario.
  key_links:
    - from: packages/server/src/utils/worktree.ts
      to: packages/server/src/server/thread/thread-lifecycle.ts
      via: create-thread lifecycle surfaces setup failures or successful guarded recovery during worktree bootstrap.
      pattern: runWorktreeSetupCommands|Worktree setup command failed|createThread
    - from: paseo.json
      to: packages/server/src/utils/worktree.ts
      via: configured setup commands execute through worktree setup runner for each new thread worktree.
      pattern: worktree.setup|bun install --frozen-lockfile
    - from: packages/server/src/server/daemon-e2e/thread-management.e2e.test.ts
      to: packages/server/e2e/thread-management-web.spec.ts
      via: both tests exercise same failure signature and assert create-thread success/no-error outcomes.
      pattern: lockfile had changes, but lockfile is frozen|thread_create_response|Create Thread
---

<objective>
Close the new Phase 3 runtime gap where thread creation fails in live UI because worktree setup runs `bun install --frozen-lockfile` and hits lockfile-change rejection.

Purpose: keep thread creation reliable in Docker/runtime usage while preserving deterministic and safe repo/worktree bootstrap behavior.
Output: guarded setup-path fix plus regression tests that reproduce and lock this exact failure mode.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-project-and-thread-management/03-project-and-thread-management-VERIFICATION.md
@.planning/phases/03-project-and-thread-management/03-project-and-thread-management-06-SUMMARY.md
@paseo.json
@packages/server/src/utils/worktree.ts
@packages/server/src/utils/worktree.test.ts
@packages/server/src/server/thread/thread-lifecycle.ts
@packages/server/src/server/daemon-e2e/thread-management.e2e.test.ts
@packages/server/e2e/thread-management-web.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden worktree setup execution for Bun frozen-lockfile mismatch without unsafe side effects</name>
  <files>packages/server/src/utils/worktree.ts, paseo.json</files>
  <action>Implement guarded handling for `bun install --frozen-lockfile` setup failures that match `lockfile had changes, but lockfile is frozen`. Keep strict frozen behavior as the first attempt; only for this explicit mismatch signature, run a deterministic fallback path that allows bootstrap to complete while preserving safe repo/worktree state (no silent tracked-file drift). Keep all other setup-command failures hard-fail. Update `paseo.json` setup only as needed to align with this guarded behavior; do not broaden to permissive/unbounded retries.</action>
  <verify>bun run --filter @getpaseo/server test -- worktree.test.ts</verify>
  <done>Create-thread bootstrap no longer hard-fails on the known frozen-lockfile mismatch path, and unrelated setup failures still fail fast.</done>
</task>

<task type="auto">
  <name>Task 2: Add regression coverage for exact lockfile-frozen failure mode in daemon and browser thread-create flows</name>
  <files>packages/server/src/utils/worktree.test.ts, packages/server/src/server/daemon-e2e/thread-management.e2e.test.ts, packages/server/e2e/thread-management-web.spec.ts</files>
  <action>Add targeted tests that reproduce the exact stderr failure signature (`lockfile had changes, but lockfile is frozen`) during worktree setup and assert post-fix behavior: thread create returns accepted response in daemon e2e and Create Thread dialog resolves with thread row visible and no error path in browser e2e. Reuse deterministic fixture setup and keep assertions focused on this failure mode (no broad unrelated UI assertions).</action>
  <verify>bun run --filter @getpaseo/server test -- thread-management.e2e.test.ts && bun run --filter @getpaseo/server test:e2e -- e2e/thread-management-web.spec.ts</verify>
  <done>Regression suite deterministically catches reintroduction of frozen-lockfile create-thread failures in both server and web runtime paths.</done>
</task>

</tasks>

<verification>
Verify the runtime gap is closed by running targeted server + web regressions and confirming create-thread succeeds for the reproduced frozen-lockfile mismatch scenario without manual intervention.
</verification>

<success_criteria>
In this runtime, Create Thread succeeds end-to-end despite the prior frozen-lockfile mismatch failure mode, worktree bootstrap remains deterministic/safe, and automated regressions fail if this exact error path returns.
</success_criteria>

<output>
After completion, create `.planning/phases/03-project-and-thread-management/03-project-and-thread-management-07-SUMMARY.md`
</output>
