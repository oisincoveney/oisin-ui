---
phase: 03-project-and-thread-management
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - packages/server/src/server/session.ts
  - packages/server/src/server/thread/thread-lifecycle.ts
  - packages/server/src/server/thread/thread-registry.ts
  - packages/server/src/terminal/terminal-manager.ts
  - packages/server/src/utils/worktree.ts
autonomous: true
must_haves:
  truths:
    - Creating a thread provisions a fresh git worktree, starts tmux + agent, and auto-switches to the new thread.
    - Switching threads reattaches to the target thread terminal while the previous thread keeps running.
    - Deleting a thread always cleans worktree, tmux session, and agent process; dirty worktrees require explicit override confirmation.
  artifacts:
    - path: packages/server/src/server/thread/thread-lifecycle.ts
      provides: transactional create/switch/delete orchestration with compensating rollback.
    - path: packages/server/src/server/session.ts
      provides: websocket handlers wiring thread lifecycle RPCs to runtime operations.
    - path: packages/server/src/terminal/terminal-manager.ts
      provides: deterministic per-thread tmux session key generation and thread-aware terminal lookup.
    - path: packages/server/src/utils/worktree.ts
      provides: reusable dirty-worktree detection + safe deletion helpers for thread teardown guardrails.
  key_links:
    - from: packages/server/src/server/thread/thread-lifecycle.ts
      to: packages/server/src/utils/worktree.ts
      via: create/delete worktree operations with rollback and ownership checks.
      pattern: createWorktree|deletePaseoWorktree|status --porcelain
    - from: packages/server/src/server/thread/thread-lifecycle.ts
      to: packages/server/src/terminal/terminal-manager.ts
      via: deterministic tmux naming + attach target selection per thread.
      pattern: sessionKey|ensure.*thread|attach_terminal_stream_request
    - from: packages/server/src/server/session.ts
      to: packages/server/src/server/thread/thread-registry.ts
      via: request handlers updating active thread pointer and lifecycle status.
      pattern: thread_create|thread_switch|thread_delete
---

<objective>
Implement server-side thread lifecycle orchestration for create/switch/delete with strict resource cleanup and deterministic tmux naming.

Purpose: deliver PROJ-02/03/04/05 backend behavior while preserving Phase 2 reconnect and stream-id hardening.
Output: transactional lifecycle handlers in session layer that bind worktree, tmux, and agent resources to persisted thread identity.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-project-and-thread-management/03-CONTEXT.md
@.planning/phases/03-project-and-thread-management/03-RESEARCH.md
@.planning/phases/03-project-and-thread-management/03-project-and-thread-management-01-SUMMARY.md
@.planning/phases/02-terminal-i/02-terminal-i-06-SUMMARY.md
@packages/server/src/server/session.ts
@packages/server/src/terminal/terminal-manager.ts
@packages/server/src/utils/worktree.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build transactional thread lifecycle service</name>
  <files>packages/server/src/server/thread/thread-lifecycle.ts, packages/server/src/server/thread/thread-registry.ts, packages/server/src/utils/worktree.ts, packages/server/src/terminal/terminal-manager.ts</files>
  <action>Implement lifecycle orchestration in strict order: create worktree -> create/ensure thread terminal session with deterministic `oisin-{project}-{thread}` key (sanitized + collision-safe suffix/hash) -> create/start agent with selected provider -> persist thread record -> return attach target. On any failure, run compensating rollback to avoid orphaned resources. For delete, always permit request but preflight dirty check and require explicit `forceDirtyDelete` confirmation when uncommitted changes exist; then terminate agent, kill tmux session(s), and delete worktree via ownership-safe utilities.</action>
  <verify>npm run test --workspace=@getpaseo/server -- thread-lifecycle</verify>
  <done>Thread create/delete paths are atomic, orphan-safe, and expose deterministic session identity; dirty worktree guard requires explicit override before destructive delete.</done>
</task>

<task type="auto">
  <name>Task 2: Wire session handlers for project/thread create/switch/delete and stream reattach</name>
  <files>packages/server/src/server/session.ts</files>
  <action>Add message handlers for new project/thread RPCs and connect them to lifecycle service + registry operations. Ensure create auto-starts selected provider and returns active thread/terminal for immediate attach. Ensure switch updates active thread pointer and returns terminal identity without killing previous thread resources. Emit thread status/unread updates suitable for sidebar state. Preserve existing attach-cycle/stale-stream safety checks from Phase 2; do not weaken input gating or stale stream rejection logic.</action>
  <verify>npm run typecheck --workspace=@getpaseo/server</verify>
  <done>Session layer can create, switch, list, and delete threads with lifecycle cleanup and no regressions to reconnect/stream-id protections.</done>
</task>

</tasks>

<verification>
Server can orchestrate full thread lifecycle with deterministic resource ownership and without regressing Phase 2 transport guarantees.
</verification>

<success_criteria>
Backend supports multiple isolated project threads end-to-end: create with selected agent/base branch, switch while background threads run, and delete with complete cleanup + dirty confirmation.
</success_criteria>

<output>
After completion, create `.planning/phases/03-project-and-thread-management/03-project-and-thread-management-02-SUMMARY.md`
</output>
