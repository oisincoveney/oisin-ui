---
phase: 03-project-and-thread-management
plan: 04
type: execute
wave: 3
depends_on:
  - "03-02"
  - "03-03"
files_modified:
  - packages/server/src/server/bootstrap.ts
  - packages/server/src/server/thread/session-reaper.ts
  - packages/server/src/server/daemon-e2e/thread-management.e2e.test.ts
  - packages/server/e2e/thread-management-web.spec.ts
  - packages/web/e2e/thread-sidebar.spec.ts
autonomous: true
must_haves:
  truths:
    - Background thread exits/errors surface as sidebar status changes plus toast notifications.
    - Session reaper cleans orphaned tmux/worktree resources not tracked by thread registry.
    - End-to-end tests prove create/switch/delete lifecycle, keyboard navigation wrap, and cleanup guarantees.
  artifacts:
    - path: packages/server/src/server/thread/session-reaper.ts
      provides: periodic reconciliation job for registry vs tmux/worktree resources.
    - path: packages/server/src/server/bootstrap.ts
      provides: daemon startup wiring for thread registry lifecycle and reaper scheduling.
    - path: packages/server/src/server/daemon-e2e/thread-management.e2e.test.ts
      provides: server lifecycle regression coverage for create/switch/delete/dirty-confirm semantics.
    - path: packages/server/e2e/thread-management-web.spec.ts
      provides: browser e2e flow for sidebar thread management and terminal switching.
    - path: packages/web/e2e/thread-sidebar.spec.ts
      provides: UI-level keyboard wrap/unread/highlight regression checks.
  key_links:
    - from: packages/server/src/server/thread/session-reaper.ts
      to: packages/server/src/server/thread/thread-registry.ts
      via: reconciliation of persisted thread state with live tmux/worktree resources.
      pattern: reconcile|orphan|cleanup
    - from: packages/server/e2e/thread-management-web.spec.ts
      to: packages/web/src/thread/thread-store.ts
      via: browser interaction assertions for create/switch/delete and background status/toast behavior.
      pattern: New Thread|unread|Cmd\+Arrow|Delete thread
    - from: packages/server/src/server/daemon-e2e/thread-management.e2e.test.ts
      to: packages/server/src/server/session.ts
      via: request/response lifecycle assertions for transactional orchestration and cleanup.
      pattern: thread_create|thread_switch|thread_delete
---

<objective>
Harden Phase 3 with orphan cleanup and regression tests covering full multi-thread behavior from daemon APIs through web UI.

Purpose: lock in lifecycle correctness and prevent regressions before moving to Phase 4 diff features.
Output: session reaper wired in daemon plus targeted server/browser tests validating requirements and locked UX behavior.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-project-and-thread-management/03-CONTEXT.md
@.planning/phases/03-project-and-thread-management/03-RESEARCH.md
@.planning/phases/03-project-and-thread-management/03-project-and-thread-management-02-SUMMARY.md
@.planning/phases/03-project-and-thread-management/03-project-and-thread-management-03-SUMMARY.md
@packages/server/src/server/bootstrap.ts
@packages/server/src/server/daemon-e2e/terminal.e2e.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add thread session reaper and bootstrap wiring</name>
  <files>packages/server/src/server/thread/session-reaper.ts, packages/server/src/server/bootstrap.ts</files>
  <action>Implement a periodic reaper (5-minute interval) that compares thread registry records against live tmux sessions/worktree paths and cleans orphaned resources in safe order. Record structured logs for detected drifts and performed cleanup actions. Wire reaper startup/shutdown into daemon bootstrap lifecycle so timers are cleaned on process stop. Keep reaper conservative: only remove resources provably under Paseo ownership or registry control.</action>
  <verify>npm run test --workspace=@getpaseo/server -- session-reaper</verify>
  <done>Daemon continuously reconciles orphaned thread resources without touching non-Paseo paths or active tracked sessions.</done>
</task>

<task type="auto">
  <name>Task 2: Add server e2e lifecycle regressions for thread management</name>
  <files>packages/server/src/server/daemon-e2e/thread-management.e2e.test.ts</files>
  <action>Create daemon e2e coverage for: project listing from configured registry, thread create auto-start+auto-switch, switch preserving background terminal activity, delete with dirty-worktree confirmation requirement, and full cleanup of worktree/tmux/agent resources. Include assertions that Phase 2 reconnect/attach behavior remains functional after multiple thread switches.</action>
  <verify>npm run test --workspace=@getpaseo/server -- thread-management.e2e</verify>
  <done>Server e2e tests fail on lifecycle leaks/regressions and pass only when create/switch/delete semantics satisfy PROJ-01..PROJ-05 backend expectations.</done>
</task>

<task type="auto">
  <name>Task 3: Add browser e2e regressions and run workspace-level verification gates</name>
  <files>packages/server/e2e/thread-management-web.spec.ts, packages/web/e2e/thread-sidebar.spec.ts</files>
  <action>Add browser tests against the docker-served web UI to validate sidebar render, New Thread dialog inline errors, active highlight, Cmd+Up/Down wrap navigation, unread indicator behavior, and background exit/error toast + status transition. After test additions, run workspace-level verification only (not monorepo-wide) to avoid OOM: server typecheck/tests and web typecheck/e2e smoke subset.</action>
  <verify>npm run typecheck --workspace=@getpaseo/server && npm run typecheck --workspace=@oisin/web && npm run test:e2e --workspace=@getpaseo/server -- e2e/thread-management-web.spec.ts</verify>
  <done>Automated browser + workspace verification gates cover the full Phase 3 user flows and pass without requiring monorepo-wide typecheck.</done>
</task>

</tasks>

<verification>
Thread lifecycle and UI behavior are protected by daemon and browser regressions, and periodic orphan cleanup is active in daemon runtime.
</verification>

<success_criteria>
Phase 3 requirements are testable and durable: sidebar project/thread management works end-to-end, background threads are observable and resilient, and resource cleanup guarantees are enforced by automation.
</success_criteria>

<output>
After completion, create `.planning/phases/03-project-and-thread-management/03-project-and-thread-management-04-SUMMARY.md`
</output>
