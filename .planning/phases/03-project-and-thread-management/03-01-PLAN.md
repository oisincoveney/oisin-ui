---
phase: 03-project-and-thread-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/server/src/server/persisted-config.ts
  - packages/server/src/server/config.ts
  - packages/server/src/server/thread/thread-registry.ts
  - packages/server/src/shared/messages.ts
  - packages/server/src/client/daemon-client.ts
autonomous: true
must_haves:
  truths:
    - Projects shown in the sidebar come from one authoritative configured-repos registry, not ad-hoc cwd inference.
    - Thread identity is concrete (`projectId`, `threadId`) across wire contracts instead of the Phase 2 placeholder literals.
    - Existing clients can still bootstrap terminal attach while Phase 3 contracts roll out.
  artifacts:
    - path: packages/server/src/server/persisted-config.ts
      provides: durable schema for configured project repositories with compatibility defaults.
    - path: packages/server/src/server/thread/thread-registry.ts
      provides: persisted project/thread metadata store keyed by project and thread IDs.
    - path: packages/server/src/shared/messages.ts
      provides: project/thread RPC schemas and additive compatibility fields for migration.
    - path: packages/server/src/client/daemon-client.ts
      provides: typed client methods for project/thread list and lifecycle RPCs.
  key_links:
    - from: packages/server/src/server/persisted-config.ts
      to: packages/server/src/server/thread/thread-registry.ts
      via: configured project repo list bootstrap into runtime registry.
      pattern: projects|repositories|repoRoot
    - from: packages/server/src/shared/messages.ts
      to: packages/server/src/client/daemon-client.ts
      via: zod-validated request/response contracts for thread APIs.
      pattern: project_.*_request|thread_.*_request
    - from: packages/server/src/shared/messages.ts
      to: packages/server/src/server/session.ts
      via: additive compatibility on ensure default terminal payload while introducing concrete thread fields.
      pattern: ensure_default_terminal_response|projectId|threadId
---

<objective>
Define the Phase 3 contract foundation: canonical configured project source, persisted thread registry, and additive protocol migration away from placeholder thread identity.

Purpose: PROJ-01..PROJ-05 depend on stable project/thread identity and persistence before lifecycle/UI work can be safely built.
Output: persisted projects/threads schema + registry + client/server wire contracts with backward-compatible terminal bootstrap migration.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-project-and-thread-management/03-CONTEXT.md
@.planning/phases/03-project-and-thread-management/03-RESEARCH.md
@.planning/phases/02-terminal-i/02-terminal-i-06-SUMMARY.md
@packages/server/src/server/persisted-config.ts
@packages/server/src/shared/messages.ts
@packages/server/src/client/daemon-client.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add authoritative configured project registry to persisted daemon config</name>
  <files>packages/server/src/server/persisted-config.ts, packages/server/src/server/config.ts</files>
  <action>Extend persisted config schema with a `projects.repositories` collection as the single source for PROJ-01 (each item includes stable `projectId`, display name, and absolute repo root path; optional default base branch). Add compatibility migration for old config files so missing projects config parses as empty list and does not break startup. Expose normalized configured projects on daemon config so session/thread layers consume one canonical source. Do not infer project list from active agents or transient terminal cwd.</action>
  <verify>npm run typecheck --workspace=@getpaseo/server</verify>
  <done>Daemon starts with old and new config files, and runtime has a deterministic configured projects list suitable for sidebar rendering.</done>
</task>

<task type="auto">
  <name>Task 2: Implement persisted thread registry and migration-safe identity model</name>
  <files>packages/server/src/server/thread/thread-registry.ts</files>
  <action>Create a thread registry module persisted under `$PASEO_HOME` that stores project metadata, thread records, active thread pointer, launch config (provider + mode/command override), terminal/agent/worktree links, unread counters, and status timestamps. Include load-time migration strategy: if legacy placeholder-only state is present, synthesize a compatibility seed record while preserving current terminal attach behavior until real threads are created. Ensure atomic writes (temp file + rename) to prevent partial JSON corruption.</action>
  <verify>npm run test --workspace=@getpaseo/server -- thread-registry</verify>
  <done>Registry can load/save deterministically, survives restart, and provides concrete `projectId/threadId` identity without breaking pre-Phase-3 runtime data.</done>
</task>

<task type="auto">
  <name>Task 3: Add project/thread RPC contracts and daemon-client methods with additive compatibility</name>
  <files>packages/server/src/shared/messages.ts, packages/server/src/client/daemon-client.ts</files>
  <action>Add typed request/response schemas and client methods for `project_list`, `project_add`, `thread_list`, `thread_create`, `thread_delete`, and `thread_switch`, plus thread status/unread updates for sidebar rendering. Replace hardcoded placeholder identity literals in new contracts with concrete IDs while keeping `ensure_default_terminal_response` additive compatibility fields during migration (do not remove existing fields in this plan). Keep all new message shapes zod-validated and requestId-correlated.</action>
  <verify>npm run typecheck --workspace=@getpaseo/server</verify>
  <done>Client and server share concrete project/thread wire types, and Phase 2 terminal bootstrap remains compatible during transition.</done>
</task>

</tasks>

<verification>
Configured project source, thread persistence model, and wire contracts exist and compile with compatibility preserved for existing terminal bootstrap flows.
</verification>

<success_criteria>
Phase 3 foundation is in place: canonical configured projects registry, persisted thread identity model, and typed project/thread RPC contracts ready for lifecycle and UI implementation.
</success_criteria>

<output>
After completion, create `.planning/phases/03-project-and-thread-management/03-project-and-thread-management-01-SUMMARY.md`
</output>
