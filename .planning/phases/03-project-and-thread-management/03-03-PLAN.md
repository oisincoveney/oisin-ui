---
phase: 03-project-and-thread-management
plan: 03
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - packages/web/package.json
  - packages/web/src/thread/thread-store.ts
  - packages/web/src/components/app-sidebar.tsx
  - packages/web/src/components/thread-create-dialog.tsx
  - packages/web/src/components/thread-delete-dialog.tsx
  - packages/web/src/App.tsx
  - packages/web/src/main.tsx
autonomous: true
must_haves:
  truths:
    - Sidebar shows configured projects with nested threads and a top-level New Thread action.
    - User can create a thread with name, agent/command, and base branch; errors render inline in the dialog.
    - Clicking a thread switches active terminal stream while previous thread continues running in background.
    - Cmd+Up/Down navigates threads with wraparound and active row highlighting.
  artifacts:
    - path: packages/web/src/thread/thread-store.ts
      provides: external thread/session state store with ws lifecycle-safe subscription boundary.
    - path: packages/web/src/components/app-sidebar.tsx
      provides: shadcn sidebar composition for projects/threads + active/unread presentation.
    - path: packages/web/src/components/thread-create-dialog.tsx
      provides: create-thread dialog with inline validation/error rendering and branch/provider selectors.
    - path: packages/web/src/components/thread-delete-dialog.tsx
      provides: delete confirmation flow with dirty-worktree extra confirmation state.
    - path: packages/web/src/App.tsx
      provides: terminal panel reattach on active-thread switch and keyboard thread navigation hooks.
  key_links:
    - from: packages/web/src/thread/thread-store.ts
      to: packages/web/src/lib/ws.ts
      via: external store subscriptions/mutations that keep ws lifecycle outside React component tree.
      pattern: useSyncExternalStore|subscribe|sendWsMessage
    - from: packages/web/src/components/thread-create-dialog.tsx
      to: packages/server/src/client/daemon-client.ts
      via: create request payload with provider + base branch + command/mode options.
      pattern: thread_create_request|branch_suggestions_request|list_available_providers_request
    - from: packages/web/src/App.tsx
      to: packages/web/src/thread/thread-store.ts
      via: active-thread change triggers attach_terminal_stream_request to selected terminal.
      pattern: activeThread|attach_terminal_stream_request
---

<objective>
Deliver Phase 3 web UX: shadcn sidebar + thread dialogs + external thread store + keyboard/thread switching behavior.

Purpose: satisfy PROJ-01/04/05 and the locked UX decisions without regressing existing connection lifecycle correctness.
Output: thread-aware web interface that creates/switches/deletes threads through new RPCs and keeps background sessions alive.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-project-and-thread-management/03-CONTEXT.md
@.planning/phases/03-project-and-thread-management/03-RESEARCH.md
@.planning/phases/03-project-and-thread-management/03-project-and-thread-management-01-SUMMARY.md
@.planning/phases/02-terminal-i/02-terminal-i-06-SUMMARY.md
@packages/web/src/App.tsx
@packages/web/src/lib/ws.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add shadcn sidebar/dialog primitives and external thread store</name>
  <files>packages/web/package.json, packages/web/src/thread/thread-store.ts, packages/web/src/components/app-sidebar.tsx, packages/web/src/main.tsx</files>
  <action>Install and wire required shadcn-compatible primitives (sidebar/dialog/alert-dialog/collapsible/tooltip/sonner as needed) in web workspace. Build a `thread-store` external state module (not React component state) that tracks projects, threads, active thread, unread markers, background status transitions, and request in-flight/error state. Expose store via `useSyncExternalStore` hooks so ws lifecycle remains outside components. Implement shadcn sidebar blocks with projects expanded by default, active row highlight, unread indicator, status dot/text, and top-level New Thread button.</action>
  <verify>npm run typecheck --workspace=@oisin/web</verify>
  <done>Sidebar renders from external store project/thread data using shadcn blocks, and thread/session state is managed outside React component lifecycle.</done>
</task>

<task type="auto">
  <name>Task 2: Implement create/delete thread dialogs with locked UX constraints</name>
  <files>packages/web/src/components/thread-create-dialog.tsx, packages/web/src/components/thread-delete-dialog.tsx, packages/web/src/thread/thread-store.ts</files>
  <action>Create New Thread dialog collecting thread name, provider/command selection, and base branch suggestions (local + remote). Show creation errors inline inside dialog (no toast). On success, auto-switch to created thread. Add delete dialog always available; when server reports dirty worktree, require explicit second confirmation before final delete request. Keep form validation deterministic and include disabled/loading states for in-flight lifecycle actions.</action>
  <verify>npm run typecheck --workspace=@oisin/web</verify>
  <done>Thread create/delete UX matches locked requirements, including inline creation errors and dirty-worktree extra confirmation.</done>
</task>

<task type="auto">
  <name>Task 3: Rebind terminal attach on active-thread switch and add keyboard wrap navigation</name>
  <files>packages/web/src/App.tsx, packages/web/src/thread/thread-store.ts</files>
  <action>Integrate thread-store with existing terminal stream orchestration so switching active thread sends attach for selected terminal and keeps prior thread session running untouched. Add Cmd+Up/Down thread navigation with wraparound ordering and preventDefault behavior. Ensure switch path auto-scrolls terminal to bottom and clears unread for activated thread. Preserve existing reconnect attach-cycle protections and stream-id input gating from Phase 2.</action>
  <verify>npm run typecheck --workspace=@oisin/web</verify>
  <done>Click or keyboard switching updates active terminal stream correctly, previous threads continue in background, and unread/active visual states stay consistent.</done>
</task>

</tasks>

<verification>
Web UI renders project/thread sidebar via shadcn primitives, supports locked create/delete/switch flows, and keeps terminal stream lifecycle correctness under thread switching.
</verification>

<success_criteria>
Users can manage and navigate project threads from sidebar with required dialogs, keyboard shortcuts, and active/unread/status indicators while preserving background thread execution.
</success_criteria>

<output>
After completion, create `.planning/phases/03-project-and-thread-management/03-project-and-thread-management-03-SUMMARY.md`
</output>
