---
phase: 03-project-and-thread-management
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/src/lib/ws.ts
  - packages/web/src/thread/thread-store.ts
  - packages/web/src/components/thread-create-dialog.tsx
  - packages/server/e2e/thread-management-web.spec.ts
autonomous: true
gap_closure: true
must_haves:
  truths:
    - Create Thread never stays in `Creating…` indefinitely.
    - If WS is not open at submit time, Create Thread exits pending immediately and shows an actionable inline error.
    - If request send succeeds but no matching response arrives within a bounded timeout, Create Thread exits pending and shows an actionable inline timeout error.
    - Regression tests fail if pending request entries can leak again during disconnect/no-response paths.
  artifacts:
    - path: packages/web/src/lib/ws.ts
      provides: Send API that reports whether a session message was actually sent on an OPEN socket.
    - path: packages/web/src/thread/thread-store.ts
      provides: Request lifecycle guards (send-failure handling + bounded timeout cleanup) for thread create pending state.
    - path: packages/server/e2e/thread-management-web.spec.ts
      provides: Browser regressions for disconnected-send and no-response timeout create-thread failure modes.
  key_links:
    - from: packages/web/src/thread/thread-store.ts
      to: packages/web/src/lib/ws.ts
      via: send request path must branch on send success/failure instead of assuming send happened.
      pattern: sendRequest|sendWsMessage
    - from: packages/web/src/thread/thread-store.ts
      to: packages/web/src/components/thread-create-dialog.tsx
      via: `create.pending` + `create.error` must always transition out of pending for unsent/timeout paths.
      pattern: create\.pending|create\.error|Creating…
    - from: packages/server/e2e/thread-management-web.spec.ts
      to: packages/web/src/thread/thread-store.ts
      via: e2e assertions enforce bounded failure behavior for disconnect/no-response flows.
      pattern: Create Thread|Creating…|timeout|disconnected
---

<objective>
Close the live Phase 3 UI leak where Create Thread can remain stuck in `Creating…` during reconnect windows.

Purpose: keep thread creation reliable under intermittent WS transport by guaranteeing pending cleanup and user-visible failure states.
Output: guarded WS send semantics + bounded request timeout handling + browser regressions for disconnect/no-response paths.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-project-and-thread-management/03-project-and-thread-management-08-SUMMARY.md
@packages/web/src/lib/ws.ts
@packages/web/src/thread/thread-store.ts
@packages/web/src/components/thread-create-dialog.tsx
@packages/server/e2e/thread-management-web.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make create-thread request lifecycle fail-fast and bounded</name>
  <files>packages/web/src/lib/ws.ts, packages/web/src/thread/thread-store.ts, packages/web/src/components/thread-create-dialog.tsx</files>
  <action>Change WS send contract so thread-store can detect unsent requests when socket is not OPEN (e.g., return boolean/result from `sendWsMessage`). In thread-store, replace fire-and-forget pending registration with a tracked request lifecycle that (a) removes pending + sets `create.pending=false` and actionable `create.error` immediately when send fails, and (b) starts a bounded timeout for `thread-create` responses that always clears pending entry and sets a timeout error if no response arrives. Ensure timeout/cleanup is canceled on normal response and on store stop/reset paths to prevent timer/request leaks. Keep existing successful create flow unchanged (dialog closes only on successful response).</action>
  <verify>bun run --filter @oisin/web typecheck</verify>
  <done>Create Thread cannot remain pending forever: unsent and no-response paths both resolve to `pending=false` with inline actionable error text.</done>
</task>

<task type="auto">
  <name>Task 2: Add browser regressions for disconnect and no-response pending leak</name>
  <files>packages/server/e2e/thread-management-web.spec.ts</files>
  <action>Add deterministic Playwright regressions that lock both failure modes: (1) submit while transport is disconnected/not open and assert dialog exits `Creating…` promptly with inline actionable error; (2) simulate no-response send path (e.g., drop/neutralize WS send from page context for create request) and assert bounded timeout error appears and pending button state recovers. Keep assertions explicit that `Creating…` does not persist indefinitely and no silent hang remains.</action>
  <verify>bun run --filter @getpaseo/server test:e2e -- e2e/thread-management-web.spec.ts</verify>
  <done>Regression suite fails if create-thread can silently leak pending state during reconnect/disconnect/no-response scenarios.</done>
</task>

</tasks>

<verification>
Verify create-thread submit always reaches terminal state (success or actionable error) with no indefinite pending UI, and browser regressions cover both unsent and timeout leak paths.
</verification>

<success_criteria>
In Docker web UI, Create Thread never hangs in `Creating…`; disconnected submit surfaces actionable inline error immediately; no-response submit surfaces actionable timeout within bounded time; targeted browser regression catches reintroduction.
</success_criteria>

<output>
After completion, create `.planning/phases/03-project-and-thread-management/03-project-and-thread-management-09-SUMMARY.md`
</output>
