---
phase: 05-docker-runtime-verification-closure
plan: 05
type: execute
wave: 3
depends_on: [05-04]
files_modified:
  - .planning/phases/05-docker-runtime-verification-closure/scripts/runtime-gate.sh
  - docker-compose.yml
  - scripts/start.sh
  - .planning/phases/05-docker-runtime-verification-closure/evidence/compose-up-attached.txt
  - .planning/phases/05-docker-runtime-verification-closure/evidence/compose-ps-start.json
  - .planning/phases/05-docker-runtime-verification-closure/evidence/compose-logs-start.txt
  - .planning/phases/05-docker-runtime-verification-closure/evidence/process-tree.txt
  - .planning/phases/05-docker-runtime-verification-closure/evidence/tmux-runtime.txt
  - .planning/phases/05-docker-runtime-verification-closure/evidence/ws-handshake.md
  - .planning/phases/05-docker-runtime-verification-closure/evidence/compose-ps-stop.json
  - .planning/phases/05-docker-runtime-verification-closure/evidence/post-stop-process-check.txt
  - .planning/phases/01-foundation-and-docker/01-foundation-and-docker-VERIFICATION.md
  - .planning/phases/05-docker-runtime-verification-closure/05-docker-runtime-verification-closure-VERIFICATION.md
  - .planning/v1-v1-MILESTONE-AUDIT.md
autonomous: true
gap_closure: true
must_haves:
  truths:
    - Runtime gate reaches daemon-ready state without duplicate-daemon lock churn in container logs.
    - Runtime gate completes successfully on repeated runs in the same Docker runtime (deterministic pass, not one-off).
    - Evidence artifacts are regenerated from successful gate execution and contain tmux-live, browser-origin WS 101, and clean stop/no-orphan proof.
    - Verification docs reference only refreshed passing artifacts and contain no stale failure narrative.
  artifacts:
    - path: .planning/phases/05-docker-runtime-verification-closure/scripts/runtime-gate.sh
      provides: deterministic gate orchestration that neutralizes persisted daemon-lock conflicts before readiness checks.
    - path: scripts/start.sh
      provides: startup behavior compatible with runtime-gate lock conflict prevention (single daemon owner, clean exit).
    - path: .planning/phases/05-docker-runtime-verification-closure/evidence/compose-up-attached.txt
      provides: startup log proof of daemon readiness without `Another Paseo daemon is already running` restart loop.
    - path: .planning/phases/05-docker-runtime-verification-closure/evidence/ws-handshake.md
      provides: browser-origin websocket handshake proof with `HTTP 101 seen: yes` from Docker app URL.
    - path: .planning/phases/05-docker-runtime-verification-closure/evidence/post-stop-process-check.txt
      provides: explicit `no-orphan-processes-detected` marker after teardown.
    - path: .planning/phases/05-docker-runtime-verification-closure/05-docker-runtime-verification-closure-VERIFICATION.md
      provides: phase closure report aligned to refreshed successful runtime artifacts.
  key_links:
    - from: .planning/phases/05-docker-runtime-verification-closure/scripts/runtime-gate.sh
      to: scripts/start.sh
      via: gate enforces clean daemon-lock state and waits for real daemon-ready signal before evidence capture.
      pattern: Daemon did not become ready in time|Another Paseo daemon is already running
    - from: .planning/phases/05-docker-runtime-verification-closure/scripts/runtime-gate.sh
      to: .planning/phases/05-docker-runtime-verification-closure/evidence/compose-up-attached.txt
      via: repeated gate runs overwrite startup logs and fail fast on duplicate-daemon lock churn.
      pattern: Server listening on http://0.0.0.0:6767
    - from: .planning/phases/05-docker-runtime-verification-closure/scripts/runtime-gate.sh
      to: .planning/phases/05-docker-runtime-verification-closure/evidence/ws-handshake.md
      via: successful gate run records browser-origin websocket upgrade markers.
      pattern: source: browser|page_url: http://localhost:44285|HTTP 101 seen: yes
    - from: .planning/phases/05-docker-runtime-verification-closure/evidence/*
      to: .planning/phases/05-docker-runtime-verification-closure/05-docker-runtime-verification-closure-VERIFICATION.md
      via: verification report cites only fresh post-fix artifacts.
      pattern: compose-up-attached|process-tree|tmux-runtime|ws-handshake|post-stop-process-check
---

<objective>
Close the operational runtime-gate failure caused by duplicate-daemon lock conflict so DOCK-01 re-validation is deterministic again.

Purpose: restore trustworthy Phase-05 runtime verification by eliminating daemon lock churn and regenerating evidence/docs from a clean successful run.
Output: hardened runtime-gate flow, refreshed passing evidence set, and verification documents consistent with the new run.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-docker-runtime-verification-closure/05-RESEARCH.md
@.planning/phases/05-docker-runtime-verification-closure/05-docker-runtime-verification-closure-VERIFICATION.md
@.planning/phases/05-docker-runtime-verification-closure/05-docker-runtime-verification-closure-03-SUMMARY.md
@.planning/phases/05-docker-runtime-verification-closure/05-docker-runtime-verification-closure-04-SUMMARY.md
@.planning/phases/05-docker-runtime-verification-closure/scripts/runtime-gate.sh
@scripts/start.sh
@docker-compose.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove duplicate-daemon lock conflict from runtime gate startup path</name>
  <files>.planning/phases/05-docker-runtime-verification-closure/scripts/runtime-gate.sh, scripts/start.sh, docker-compose.yml</files>
  <action>Identify where persisted runtime state causes `Another Paseo daemon is already running` during compose startup and implement a deterministic fix in automation (for example isolate `PASEO_HOME` for gate runs or clear stale lock state before daemon boot). Keep single-daemon ownership semantics; do not bypass lock checks in server code and do not add fragile sleep-only retries. Ensure gate still fails if a real concurrent daemon conflict exists.</action>
  <verify>Run `bash .planning/phases/05-docker-runtime-verification-closure/scripts/runtime-gate.sh` and confirm startup reaches `Server listening on http://0.0.0.0:6767` without any `Another Paseo daemon is already running` lines in `compose-up-attached.txt`.</verify>
  <done>Runtime gate no longer times out on daemon readiness due to duplicate-daemon lock churn in current Docker runtime.</done>
</task>

<task type="auto">
  <name>Task 2: Prove deterministic gate success and regenerate canonical runtime artifacts</name>
  <files>.planning/phases/05-docker-runtime-verification-closure/evidence/compose-up-attached.txt, .planning/phases/05-docker-runtime-verification-closure/evidence/compose-ps-start.json, .planning/phases/05-docker-runtime-verification-closure/evidence/compose-logs-start.txt, .planning/phases/05-docker-runtime-verification-closure/evidence/process-tree.txt, .planning/phases/05-docker-runtime-verification-closure/evidence/tmux-runtime.txt, .planning/phases/05-docker-runtime-verification-closure/evidence/ws-handshake.md, .planning/phases/05-docker-runtime-verification-closure/evidence/compose-ps-stop.json, .planning/phases/05-docker-runtime-verification-closure/evidence/post-stop-process-check.txt</files>
  <action>Execute runtime gate twice back-to-back after the startup fix and treat the second run as canonical artifact refresh to prove determinism. Overwrite stale evidence files on success only. Preserve strict checks for tmux runtime marker, browser-origin WS 101 marker, and no-orphan teardown marker.</action>
  <verify>Both consecutive gate runs exit 0; canonical artifacts show `Server listening on http://0.0.0.0:6767`, `tmux-session-running`, `HTTP 101 seen: yes`, and `no-orphan-processes-detected` with no duplicate-daemon error in startup logs.</verify>
  <done>Operational runtime gate is reproducible and evidence set is freshly generated from a successful deterministic execution path.</done>
</task>

<task type="auto">
  <name>Task 3: Refresh verification chain to match new successful runtime evidence</name>
  <files>.planning/phases/01-foundation-and-docker/01-foundation-and-docker-VERIFICATION.md, .planning/phases/05-docker-runtime-verification-closure/05-docker-runtime-verification-closure-VERIFICATION.md, .planning/v1-v1-MILESTONE-AUDIT.md</files>
  <action>Update verification and milestone docs to cite regenerated artifacts from Task 2 and remove any stale references to readiness timeout or daemon-lock restart loop. Keep DOCK-01 truth chain explicit: one-container runtime, browser WS 101, clean stop/no-orphans. Ensure status fields and narrative are internally consistent across all three docs.</action>
  <verify>Each doc references refreshed evidence artifact paths from the latest successful run; DOCK-01 remains satisfied/passed with no contradictory failure language.</verify>
  <done>Documentation and evidence are synchronized, making Phase-05 re-verification reliable and auditable.</done>
</task>

</tasks>

<verification>
From clean state, run runtime-gate twice sequentially and confirm both runs pass while startup logs remain free of duplicate-daemon lock errors; then cross-check phase and milestone docs cite the newly regenerated artifacts only.
</verification>

<success_criteria>
`runtime-gate.sh` completes deterministically in current Docker runtime, no duplicate-daemon lock conflict appears during gate runs, and all verification artifacts/docs are refreshed and consistent with successful execution.
</success_criteria>

<output>
After completion, create `.planning/phases/05-docker-runtime-verification-closure/05-docker-runtime-verification-closure-05-SUMMARY.md`
</output>
