---
phase: 05-docker-runtime-verification-closure
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/start.sh
  - docker-compose.yml
  - .planning/phases/05-docker-runtime-verification-closure/scripts/runtime-gate.sh
  - .planning/phases/05-docker-runtime-verification-closure/evidence/compose-up-attached.txt
  - .planning/phases/05-docker-runtime-verification-closure/evidence/compose-ps-start.json
  - .planning/phases/05-docker-runtime-verification-closure/evidence/compose-logs-start.txt
  - .planning/phases/05-docker-runtime-verification-closure/evidence/process-tree.txt
  - .planning/phases/05-docker-runtime-verification-closure/evidence/ws-handshake.md
  - .planning/phases/05-docker-runtime-verification-closure/evidence/compose-ps-stop.json
  - .planning/phases/05-docker-runtime-verification-closure/evidence/post-stop-process-check.txt
autonomous: true
gap_closure: true
must_haves:
  truths:
    - Browser app from Docker URL establishes daemon WebSocket with HTTP 101 during verification window.
    - Runtime verification no longer hits daemon lock/reachability churn before handshake check.
    - Controlled shutdown leaves zero running project containers and no orphaned runtime processes.
  artifacts:
    - path: scripts/start.sh
      provides: deterministic daemon startup flags/env and clean shutdown behavior for Docker runtime verification.
    - path: .planning/phases/05-docker-runtime-verification-closure/scripts/runtime-gate.sh
      provides: single reproducible command that captures startup, handshake, and stop evidence.
    - path: .planning/phases/05-docker-runtime-verification-closure/evidence/ws-handshake.md
      provides: explicit proof that WS request reached 101 Switching Protocols.
    - path: .planning/phases/05-docker-runtime-verification-closure/evidence/compose-ps-stop.json
      provides: post-stop compose snapshot proving no running `oisin-ui` container.
    - path: .planning/phases/05-docker-runtime-verification-closure/evidence/post-stop-process-check.txt
      provides: explicit `no-orphan-processes-detected` result.
  key_links:
    - from: scripts/start.sh
      to: packages/server/src/server/index.ts
      via: daemon starts with runtime-safe args/env and reaches stable listen state before WS checks.
      pattern: --no-relay|--no-mcp|PASEO_.*ENABLED
    - from: .planning/phases/05-docker-runtime-verification-closure/scripts/runtime-gate.sh
      to: .planning/phases/05-docker-runtime-verification-closure/evidence/ws-handshake.md
      via: script performs live WS upgrade probe and writes 101 outcome.
      pattern: 101 Switching Protocols|HTTP 101 seen: yes
    - from: .planning/phases/05-docker-runtime-verification-closure/scripts/runtime-gate.sh
      to: .planning/phases/05-docker-runtime-verification-closure/evidence/post-stop-process-check.txt
      via: script tears down compose project and records orphan check result.
      pattern: docker compose down --remove-orphans|no-orphan-processes-detected
---

<objective>
Close runtime evidence gaps by stabilizing Docker startup and regenerating proof for WS 101 + clean stop.

Purpose: remove nondeterministic daemon startup/refusal window and produce reproducible gap-closing evidence.
Output: startup/runtime script + fresh evidence set with passing WS handshake and post-stop cleanup proofs.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-docker-runtime-verification-closure/05-RESEARCH.md
@.planning/phases/05-docker-runtime-verification-closure/05-docker-runtime-verification-closure-VERIFICATION.md
@.planning/phases/05-docker-runtime-verification-closure/05-docker-runtime-verification-closure-01-SUMMARY.md
@scripts/start.sh
@docker-compose.yml
@packages/web/src/lib/ws.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stabilize Docker daemon startup for handshake window</name>
  <files>scripts/start.sh, docker-compose.yml</files>
  <action>Update container startup to eliminate transient daemon-unreachable windows that caused `ERR_CONNECTION_REFUSED`: (1) run daemon with explicit runtime-safe args (`--no-relay --no-mcp`) in this Docker verification path, (2) default speech-related env flags to disabled in `start.sh` for this runtime so local model bootstrap cannot delay daemon bind, and (3) keep shutdown trap semantics intact (no background child leakage). Keep existing ports (`44285` web, `6767` daemon) unchanged.</action>
  <verify>Run `docker compose up --build` and confirm logs no longer show repeated `Another Paseo daemon is already running` loops before first successful listen; confirm daemon reaches `Server listening on http://0.0.0.0:6767` quickly and WS client no longer reports `ERR_CONNECTION_REFUSED` during initial check window.</verify>
  <done>Startup path is deterministic enough that runtime handshake verification can run without refusal/race artifacts.</done>
</task>

<task type="auto">
  <name>Task 2: Add deterministic runtime gate evidence runner</name>
  <files>.planning/phases/05-docker-runtime-verification-closure/scripts/runtime-gate.sh</files>
  <action>Create an executable script that performs the full DOCK-01 gate flow end-to-end: bring stack up, wait for daemon listen readiness, capture startup artifacts (`compose-up-attached.txt`, `compose-ps-start.json`, `compose-logs-start.txt`, `process-tree.txt`), probe WS upgrade against `ws://localhost:6767/ws?clientSessionKey=web-client` and write structured result to `ws-handshake.md`, stop/tear down compose, capture `compose-ps-stop.json`, and write host orphan check output to `post-stop-process-check.txt`. Fail script on any gate miss (no 101, container still running, orphans found).</action>
  <verify>`bash .planning/phases/05-docker-runtime-verification-closure/scripts/runtime-gate.sh` exits 0 and writes all required evidence files; `ws-handshake.md` contains `HTTP 101 seen: yes`; `post-stop-process-check.txt` contains `no-orphan-processes-detected`.</verify>
  <done>One command now regenerates complete passing runtime evidence for the remaining verification gaps.</done>
</task>

<task type="auto">
  <name>Task 3: Re-run runtime gate and refresh canonical evidence artifacts</name>
  <files>.planning/phases/05-docker-runtime-verification-closure/evidence/compose-up-attached.txt, .planning/phases/05-docker-runtime-verification-closure/evidence/compose-ps-start.json, .planning/phases/05-docker-runtime-verification-closure/evidence/compose-logs-start.txt, .planning/phases/05-docker-runtime-verification-closure/evidence/process-tree.txt, .planning/phases/05-docker-runtime-verification-closure/evidence/ws-handshake.md, .planning/phases/05-docker-runtime-verification-closure/evidence/compose-ps-stop.json, .planning/phases/05-docker-runtime-verification-closure/evidence/post-stop-process-check.txt</files>
  <action>Execute the new runtime gate script and replace stale failing artifacts. Ensure evidence timestamps and contents correspond to the latest run only. Preserve the same artifact paths used by verification docs so downstream pass propagation has no path churn.</action>
  <verify>Artifacts show pass-state values: WS evidence includes 101 upgrade, post-stop compose snapshot shows no running `oisin-ui` service, and orphan check explicitly reports `no-orphan-processes-detected`.</verify>
  <done>All missing runtime evidence items from the current gap report are closed with fresh pass-state artifacts.</done>
</task>

</tasks>

<verification>
Run the runtime gate script once from clean state and confirm all three runtime truths pass simultaneously: startup in one container, WS 101 handshake, clean post-stop/no-orphan result.
</verification>

<success_criteria>
Phase-05 evidence set no longer contains `HTTP 101 seen: no`, `ERR_CONNECTION_REFUSED`, `State":"running` in post-stop snapshot, or `orphans-found`.
</success_criteria>

<output>
After completion, create `.planning/phases/05-docker-runtime-verification-closure/05-docker-runtime-verification-closure-03-SUMMARY.md`
</output>
