---
phase: 05-docker-runtime-verification-closure
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/start.sh
  - docker-compose.yml
  - .planning/phases/05-docker-runtime-verification-closure/scripts/runtime-gate.sh
generated_outputs:
  - .planning/phases/05-docker-runtime-verification-closure/evidence/compose-up-attached.txt
  - .planning/phases/05-docker-runtime-verification-closure/evidence/compose-ps-start.json
  - .planning/phases/05-docker-runtime-verification-closure/evidence/compose-logs-start.txt
  - .planning/phases/05-docker-runtime-verification-closure/evidence/process-tree.txt
  - .planning/phases/05-docker-runtime-verification-closure/evidence/tmux-runtime.txt
  - .planning/phases/05-docker-runtime-verification-closure/evidence/ws-handshake.md
  - .planning/phases/05-docker-runtime-verification-closure/evidence/compose-ps-stop.json
  - .planning/phases/05-docker-runtime-verification-closure/evidence/post-stop-process-check.txt
autonomous: true
gap_closure: true
must_haves:
  truths:
    - Docker runtime evidence proves tmux is actively running in-container during the verification window.
    - Browser app from Docker URL establishes daemon WebSocket with HTTP 101 during verification window.
    - Runtime verification no longer hits daemon lock/reachability churn before handshake check.
    - Controlled shutdown leaves zero running project containers and no orphaned runtime processes.
  artifacts:
    - path: scripts/start.sh
      provides: deterministic daemon startup flags/env and clean shutdown behavior for Docker runtime verification.
    - path: .planning/phases/05-docker-runtime-verification-closure/scripts/runtime-gate.sh
      provides: single reproducible command that captures startup, handshake, and stop evidence.
    - path: .planning/phases/05-docker-runtime-verification-closure/evidence/ws-handshake.md
      provides: browser-origin proof that app at `http://localhost:44285` upgraded `/ws` with HTTP 101.
    - path: .planning/phases/05-docker-runtime-verification-closure/evidence/tmux-runtime.txt
      provides: explicit runtime snapshot showing tmux server/session alive in the container.
    - path: .planning/phases/05-docker-runtime-verification-closure/evidence/compose-ps-stop.json
      provides: post-stop compose snapshot proving no running `oisin-ui` container.
    - path: .planning/phases/05-docker-runtime-verification-closure/evidence/post-stop-process-check.txt
      provides: explicit `no-orphan-processes-detected` result.
  key_links:
    - from: scripts/start.sh
      to: packages/server/src/server/index.ts
      via: daemon starts with runtime-safe args/env and reaches stable listen state before WS checks.
      pattern: --no-relay|--no-mcp|PASEO_.*ENABLED
    - from: .planning/phases/05-docker-runtime-verification-closure/scripts/runtime-gate.sh
      to: .planning/phases/05-docker-runtime-verification-closure/evidence/tmux-runtime.txt
      via: script forces deterministic tmux session creation and records tmux process/session proof before shutdown.
      pattern: tmux-session-running|tmux process present
    - from: .planning/phases/05-docker-runtime-verification-closure/scripts/runtime-gate.sh
      to: .planning/phases/05-docker-runtime-verification-closure/evidence/ws-handshake.md
      via: script captures WS upgrade from browser navigation to Docker-served app URL and writes 101 outcome.
      pattern: source: browser|page_url: http://localhost:44285|HTTP 101 seen: yes
    - from: .planning/phases/05-docker-runtime-verification-closure/scripts/runtime-gate.sh
      to: .planning/phases/05-docker-runtime-verification-closure/evidence/post-stop-process-check.txt
      via: script tears down compose project and records orphan check result.
      pattern: docker compose down --remove-orphans|no-orphan-processes-detected
---

<objective>
Close runtime evidence gaps by regenerating tmux-present runtime proof and browser-origin WS 101 proof.

Purpose: align DOCK-01 closure evidence to actual runtime behavior (tmux running + browser app handshake) with deterministic automation.
Output: updated runtime-gate automation + regenerated evidence outputs proving tmux runtime, browser WS 101, and clean stop.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-docker-runtime-verification-closure/05-RESEARCH.md
@.planning/phases/05-docker-runtime-verification-closure/05-docker-runtime-verification-closure-VERIFICATION.md
@.planning/phases/05-docker-runtime-verification-closure/05-docker-runtime-verification-closure-01-SUMMARY.md
@scripts/start.sh
@docker-compose.yml
@packages/web/src/lib/ws.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stabilize runtime preconditions and capture tmux-live proof</name>
  <files>scripts/start.sh, docker-compose.yml</files>
  <action>Keep startup deterministic for verification (`--no-relay --no-mcp`, speech features disabled defaults, signal-safe shutdown) and ensure tmux is live during evidence capture. If container startup does not naturally spawn tmux, update runtime gate flow to create a deterministic detached tmux session inside the running container before process capture. Keep ports unchanged (`44285` web, `6767` daemon).</action>
  <verify>Run runtime gate once and confirm `compose-up-attached.txt` has stable daemon listen logs without lock churn, `process-tree.txt` includes a `tmux` process entry, and `tmux-runtime.txt` contains a passing marker plus `tmux ls` output.</verify>
  <done>Evidence set now proves tmux is actively running in-container during runtime verification, not inferred.</done>
</task>

<task type="auto">
  <name>Task 2: Replace direct Node probe with browser-origin WS handshake capture</name>
  <files>.planning/phases/05-docker-runtime-verification-closure/scripts/runtime-gate.sh</files>
  <action>Update `runtime-gate.sh` so WS evidence is captured from browser context served at `http://localhost:44285` (Playwright or equivalent scripted browser), not an inline Node `ws` client. Capture handshake metadata in `ws-handshake.md` including `source: browser`, `page_url`, `request_url`, and `HTTP 101 seen: yes`. Keep script authoritative for full gate: startup capture, tmux runtime capture (`tmux-runtime.txt`), browser WS capture, teardown, and orphan checks; fail on any miss.</action>
  <verify>`bash .planning/phases/05-docker-runtime-verification-closure/scripts/runtime-gate.sh` exits 0; `ws-handshake.md` includes browser-context fields and `HTTP 101 seen: yes`; script output has no fallback to direct Node ws probing.</verify>
  <done>WS gate now proves Docker-served browser app handshake directly, closing the verification-source gap.</done>
</task>

<task type="auto">
  <name>Task 3: Re-run runtime gate and refresh canonical evidence artifacts</name>
  <files>.planning/phases/05-docker-runtime-verification-closure/evidence/compose-up-attached.txt, .planning/phases/05-docker-runtime-verification-closure/evidence/compose-ps-start.json, .planning/phases/05-docker-runtime-verification-closure/evidence/compose-logs-start.txt, .planning/phases/05-docker-runtime-verification-closure/evidence/process-tree.txt, .planning/phases/05-docker-runtime-verification-closure/evidence/tmux-runtime.txt, .planning/phases/05-docker-runtime-verification-closure/evidence/ws-handshake.md, .planning/phases/05-docker-runtime-verification-closure/evidence/compose-ps-stop.json, .planning/phases/05-docker-runtime-verification-closure/evidence/post-stop-process-check.txt</files>
  <action>Execute the updated runtime gate script and overwrite stale artifacts. Ensure fresh artifacts prove all gap truths at once: tmux process/session present, browser-origin WS 101 from Docker URL, and clean post-stop no-orphan state. Preserve canonical paths consumed by verification docs.</action>
  <verify>Artifacts include: `process-tree.txt` with `tmux`, `tmux-runtime.txt` with session proof, `ws-handshake.md` with browser context + `HTTP 101 seen: yes`, `compose-ps-stop.json` as empty/no running services, and `post-stop-process-check.txt` with `no-orphan-processes-detected`.</verify>
  <done>All remaining runtime evidence gaps are closed with fresh reproducible artifacts.</done>
</task>

</tasks>

<verification>
Run the runtime gate script once from clean state and confirm all four runtime truths pass simultaneously: startup in one container, tmux active in-container, browser-origin WS 101 handshake, clean post-stop/no-orphan result.
</verification>

<success_criteria>
Phase-05 evidence set no longer contains missing-tmux or Node-probe handshake gaps; it records tmux runtime proof, browser-origin `HTTP 101 seen: yes`, no post-stop running services, and no `orphans-found`.
</success_criteria>

<output>
After completion, create `.planning/phases/05-docker-runtime-verification-closure/05-docker-runtime-verification-closure-03-SUMMARY.md`
</output>
