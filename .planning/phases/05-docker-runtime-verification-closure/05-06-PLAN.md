---
phase: 05-docker-runtime-verification-closure
plan: 06
type: execute
wave: 4
depends_on: [05-05]
files_modified:
  - packages/server/src/server/pid-lock.ts
  - packages/server/src/server/pid-lock.test.ts
  - scripts/start.sh
  - docker-compose.yml
  - .planning/phases/05-docker-runtime-verification-closure/scripts/restart-stability-gate.sh
  - .planning/phases/05-docker-runtime-verification-closure/evidence/restart-compose-logs.txt
  - .planning/phases/05-docker-runtime-verification-closure/evidence/restart-ws-stability.md
  - .planning/phases/05-docker-runtime-verification-closure/evidence/compose-up-attached.txt
  - .planning/phases/05-docker-runtime-verification-closure/evidence/ws-handshake.md
  - .planning/phases/05-docker-runtime-verification-closure/evidence/post-stop-process-check.txt
  - .planning/phases/05-docker-runtime-verification-closure/05-docker-runtime-verification-closure-VERIFICATION.md
autonomous: true
gap_closure: true
must_haves:
  truths:
    - Standard `docker compose up` and `docker compose restart oisin-ui` do not enter duplicate-daemon lock churn.
    - Browser websocket reconnects once and remains connected after service restart (no reconnect loop).
    - Runtime gate still passes with HTTP 101 handshake and clean stop/no-orphan markers.
  artifacts:
    - path: packages/server/src/server/pid-lock.ts
      provides: stale-lock detection that tolerates PID reuse while preserving real concurrent-daemon protection.
    - path: scripts/start.sh
      provides: default startup preflight that clears only stale daemon lock state before launching server.
    - path: .planning/phases/05-docker-runtime-verification-closure/scripts/restart-stability-gate.sh
      provides: deterministic restart validation (compose restart + log scan + browser ws stability probe).
    - path: .planning/phases/05-docker-runtime-verification-closure/evidence/restart-ws-stability.md
      provides: timestamped websocket stability evidence before/after restart.
    - path: .planning/phases/05-docker-runtime-verification-closure/05-docker-runtime-verification-closure-VERIFICATION.md
      provides: updated closure report including normal-start/restart lock-churn regression proof.
  key_links:
    - from: scripts/start.sh
      to: packages/server/src/server/pid-lock.ts
      via: startup preflight and daemon lock acquisition both enforce stale-only cleanup and active-lock protection.
      pattern: PASEO_PRESTART_CLEAN_LOCK|Another Paseo daemon is already running
    - from: .planning/phases/05-docker-runtime-verification-closure/scripts/restart-stability-gate.sh
      to: .planning/phases/05-docker-runtime-verification-closure/evidence/restart-compose-logs.txt
      via: restart gate captures compose logs and fails if duplicate-daemon marker appears.
      pattern: docker compose restart oisin-ui|Another Paseo daemon is already running
    - from: .planning/phases/05-docker-runtime-verification-closure/scripts/restart-stability-gate.sh
      to: .planning/phases/05-docker-runtime-verification-closure/evidence/restart-ws-stability.md
      via: browser probe records ws open state before and after restart with bounded settle timeout.
      pattern: pre_restart_connected: yes|post_restart_connected: yes|reconnect_loop_detected: no
    - from: .planning/phases/05-docker-runtime-verification-closure/scripts/runtime-gate.sh
      to: .planning/phases/05-docker-runtime-verification-closure/evidence/ws-handshake.md
      via: existing runtime gate remains passing after lock-fix rollout.
      pattern: HTTP 101 seen: yes
---

<objective>
Close the normal Docker startup/restart lock-churn gap by making stale daemon lock handling safe-by-default and proving websocket stability after restart.

Purpose: remove operational flake in everyday `docker compose restart oisin-ui` usage without weakening single-daemon lock guarantees.
Output: hardened lock behavior, restart-stability gate artifacts, and refreshed phase verification evidence that still passes DOCK-01 runtime gate.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-docker-runtime-verification-closure/05-RESEARCH.md
@.planning/phases/05-docker-runtime-verification-closure/05-05-PLAN.md
@.planning/phases/05-docker-runtime-verification-closure/05-docker-runtime-verification-closure-05-SUMMARY.md
@packages/server/src/server/pid-lock.ts
@scripts/start.sh
@docker-compose.yml
@.planning/phases/05-docker-runtime-verification-closure/scripts/runtime-gate.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden PID-lock stale detection for restart-safe daemon ownership</name>
  <files>packages/server/src/server/pid-lock.ts, packages/server/src/server/pid-lock.test.ts</files>
  <action>Update PID-lock acquisition so stale locks are cleared when lock owner is no longer the actual daemon (including PID reuse edge cases), while preserving hard failure when a real active Paseo daemon owns the lock. Implement deterministic unit coverage for: stale/dead PID cleanup, PID-reused non-daemon process cleanup, and active-daemon conflict error. Do not add blanket lock deletion or sleep-based retries.</action>
  <verify>Run `bun test packages/server/src/server/pid-lock.test.ts` and `bun run --filter @oisin/server typecheck`.</verify>
  <done>Lock acquisition tolerates stale lock files from restarts but still blocks true concurrent-daemon startup.</done>
</task>

<task type="auto">
  <name>Task 2: Make default compose startup/restart path stable without opt-in env toggles</name>
  <files>scripts/start.sh, docker-compose.yml</files>
  <action>Change startup behavior so normal compose runs self-heal stale daemon lock state by default, not only when `PASEO_PRESTART_CLEAN_LOCK=1` is manually set. Keep explicit guardrails so active lock owners are not silently overridden. Ensure startup logs stay explicit about stale-lock cleanup vs active-lock refusal.</action>
  <verify>Run `docker compose down --remove-orphans && docker compose up --build -d && docker compose restart oisin-ui && docker compose logs --no-color oisin-ui` and confirm no `Another Paseo daemon is already running` loop appears.</verify>
  <done>Regular Docker start/restart flow no longer flaps due to persisted stale pid lock and does not require manual env overrides.</done>
</task>

<task type="auto">
  <name>Task 3: Add restart-stability gate and re-validate phase runtime closure</name>
  <files>.planning/phases/05-docker-runtime-verification-closure/scripts/restart-stability-gate.sh, .planning/phases/05-docker-runtime-verification-closure/evidence/restart-compose-logs.txt, .planning/phases/05-docker-runtime-verification-closure/evidence/restart-ws-stability.md, .planning/phases/05-docker-runtime-verification-closure/evidence/compose-up-attached.txt, .planning/phases/05-docker-runtime-verification-closure/evidence/ws-handshake.md, .planning/phases/05-docker-runtime-verification-closure/evidence/post-stop-process-check.txt, .planning/phases/05-docker-runtime-verification-closure/05-docker-runtime-verification-closure-VERIFICATION.md</files>
  <action>Create a deterministic restart-stability gate script that: (1) boots compose, (2) captures websocket connected state from browser context, (3) restarts `oisin-ui`, (4) verifies websocket returns and stays stable (no reconnect loop), and (5) fails if logs show duplicate-daemon lock churn. Then run existing `runtime-gate.sh` to ensure phase-05 runtime gate still passes, regenerate canonical artifacts, and update verification report with new restart-proof evidence.</action>
  <verify>Run `bash .planning/phases/05-docker-runtime-verification-closure/scripts/restart-stability-gate.sh` then `bash .planning/phases/05-docker-runtime-verification-closure/scripts/runtime-gate.sh`; confirm evidence contains `pre_restart_connected: yes`, `post_restart_connected: yes`, `reconnect_loop_detected: no`, `HTTP 101 seen: yes`, and `no-orphan-processes-detected`.</verify>
  <done>Phase 05 now proves stable default restart behavior and retains full DOCK-01 runtime gate pass criteria.</done>
</task>

</tasks>

<verification>
From clean Docker state, run restart-stability gate then runtime gate; confirm both pass and evidence/log artifacts show zero duplicate-daemon lock churn, stable websocket after restart, HTTP 101 handshake, and clean no-orphan shutdown markers.
</verification>

<success_criteria>
Normal compose startup/restart is stable by default (no duplicate-daemon lock churn), websocket remains connected after restart without flap loops, and phase-05 runtime gate still passes with refreshed evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/05-docker-runtime-verification-closure/05-docker-runtime-verification-closure-06-SUMMARY.md`
</output>
