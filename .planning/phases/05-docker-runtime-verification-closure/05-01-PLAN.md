---
phase: 05-docker-runtime-verification-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/05-docker-runtime-verification-closure/evidence/compose-up-attached.txt
  - .planning/phases/05-docker-runtime-verification-closure/evidence/compose-ps-start.json
  - .planning/phases/05-docker-runtime-verification-closure/evidence/compose-logs-start.txt
  - .planning/phases/05-docker-runtime-verification-closure/evidence/process-tree.txt
  - .planning/phases/05-docker-runtime-verification-closure/evidence/ws-handshake.md
  - .planning/phases/05-docker-runtime-verification-closure/evidence/compose-ps-stop.json
  - .planning/phases/05-docker-runtime-verification-closure/evidence/post-stop-process-check.txt
  - .planning/phases/01-foundation-and-docker/01-foundation-and-docker-VERIFICATION.md
  - .planning/phases/05-docker-runtime-verification-closure/05-docker-runtime-verification-closure-VERIFICATION.md
  - .planning/v1-v1-MILESTONE-AUDIT.md
autonomous: false
must_haves:
  truths:
    - docker compose starts one oisin-ui container that runs daemon, web UI, and tmux in one runtime.
    - Browser app served from Docker-mapped URL upgrades WebSocket connection to daemon with HTTP 101.
    - Controlled shutdown leaves no running project containers or orphaned child processes.
  artifacts:
    - path: docker-compose.yml
      provides: single-service container definition and host port mapping for runtime verification.
    - path: scripts/start.sh
      provides: coordinated daemon/web startup and signal-safe teardown behavior inside container.
    - path: .planning/phases/05-docker-runtime-verification-closure/evidence/compose-ps-start.json
      provides: machine-readable proof of runtime container state during startup.
    - path: .planning/phases/05-docker-runtime-verification-closure/evidence/process-tree.txt
      provides: in-container process proof that tmux plus daemon/web processes are running under the single oisin-ui container.
    - path: .planning/phases/05-docker-runtime-verification-closure/evidence/ws-handshake.md
      provides: browser network proof of ws upgrade request and 101 response.
    - path: .planning/phases/05-docker-runtime-verification-closure/evidence/compose-ps-stop.json
      provides: post-stop proof that no project containers remain running.
    - path: .planning/phases/05-docker-runtime-verification-closure/evidence/post-stop-process-check.txt
      provides: explicit post-stop check result proving no lingering oisin-ui runtime child processes remain on host.
    - path: .planning/phases/01-foundation-and-docker/01-foundation-and-docker-VERIFICATION.md
      provides: re-verified DOCK-01 status updated from human_needed to passed with runtime evidence references.
    - path: .planning/v1-v1-MILESTONE-AUDIT.md
      provides: milestone audit rollup updated to no requirement blockers for DOCK-01.
  key_links:
    - from: docker-compose.yml
      to: scripts/start.sh
      via: oisin-ui container boot command executes signal-aware startup script under tini.
      pattern: command: .*start\.sh|ENTRYPOINT .*tini
    - from: packages/web/src/lib/ws.ts
      to: .planning/phases/05-docker-runtime-verification-closure/evidence/ws-handshake.md
      via: browser-host-derived websocket target matches observed WS request URL and 101 upgrade.
      pattern: VITE_DAEMON_PORT|101 Switching Protocols
    - from: .planning/phases/05-docker-runtime-verification-closure/evidence/compose-up-attached.txt
      to: .planning/phases/05-docker-runtime-verification-closure/evidence/compose-ps-stop.json
      via: attached controlled stop timeline followed by post-stop compose state snapshot.
      pattern: Gracefully stopping|\[\]
    - from: .planning/phases/05-docker-runtime-verification-closure/evidence/process-tree.txt
      to: .planning/phases/05-docker-runtime-verification-closure/evidence/post-stop-process-check.txt
      via: running-state process tree contains tmux, then post-stop host check records no lingering oisin-ui runtime processes.
      pattern: tmux|no-orphan-processes-detected
---

<objective>
Close DOCK-01 with runtime evidence so milestone v1 moves from gaps_found to fully passed.

Purpose: clear the final audit blocker by proving single-container startup, browser WS handshake, and clean controlled stop.
Output: phase-05 evidence artifacts plus updated phase/milestone verification docs showing DOCK-01 passed.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1-v1-MILESTONE-AUDIT.md
@.planning/phases/05-docker-runtime-verification-closure/05-RESEARCH.md
@.planning/phases/01-foundation-and-docker/01-foundation-and-docker-VERIFICATION.md
@docker-compose.yml
@Dockerfile
@scripts/start.sh
@packages/web/src/lib/ws.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture Docker runtime startup/process evidence</name>
  <files>.planning/phases/05-docker-runtime-verification-closure/evidence/compose-up-attached.txt, .planning/phases/05-docker-runtime-verification-closure/evidence/compose-ps-start.json, .planning/phases/05-docker-runtime-verification-closure/evidence/compose-logs-start.txt, .planning/phases/05-docker-runtime-verification-closure/evidence/process-tree.txt</files>
  <action>Create `.planning/phases/05-docker-runtime-verification-closure/evidence/` fresh for this run. Start the stack with attached `docker compose up --build` and capture terminal output to `compose-up-attached.txt`. While compose is running, capture `docker compose ps --format json` into `compose-ps-start.json`, `docker compose logs --no-color --timestamps` into `compose-logs-start.txt`, and `docker top $(docker compose ps -q oisin-ui)` into `process-tree.txt`. Keep compose running for the browser verification checkpoint.</action>
  <verify>Evidence files exist, `compose-ps-start.json` shows exactly one `oisin-ui` service in running state with expected mapped ports, and `process-tree.txt` explicitly includes `tmux` plus daemon/web runtime processes inside that container.</verify>
  <done>Startup/process evidence proves daemon + web + tmux are up in one container before browser handshake verification begins.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify browser WebSocket 101 and perform controlled stop</name>
  <what-built>Single-container runtime is running with startup/process evidence captured and ready for browser/protocol confirmation.</what-built>
  <how-to-verify>1) Open `http://localhost:44285`. 2) In browser DevTools Network -> WS, select `/ws?clientSessionKey=web-client`; confirm request URL resolves to daemon port `6767` and status is `101 Switching Protocols` with live frames. 3) Save findings in `.planning/phases/05-docker-runtime-verification-closure/evidence/ws-handshake.md` (timestamp, request URL, status, frame evidence). 4) Return to attached compose terminal and press Ctrl+C once for controlled stop. 5) After shutdown completes, run `docker compose ps --all --format json > .planning/phases/05-docker-runtime-verification-closure/evidence/compose-ps-stop.json` and `docker compose down --remove-orphans`. 6) Run `if ps -Ao command | rg -q '/Users/oisin/dev/oisin-ui/(packages/server|packages/web|scripts/start\.sh)'; then echo 'orphans-found' > .planning/phases/05-docker-runtime-verification-closure/evidence/post-stop-process-check.txt; else echo 'no-orphan-processes-detected' > .planning/phases/05-docker-runtime-verification-closure/evidence/post-stop-process-check.txt; fi` and confirm the evidence file contains `no-orphan-processes-detected`.</how-to-verify>
  <resume-signal>Type "approved" if all checks passed, or provide exact failure observations from WS/status/stop output.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Close verification docs and milestone audit</name>
  <files>.planning/phases/01-foundation-and-docker/01-foundation-and-docker-VERIFICATION.md, .planning/phases/05-docker-runtime-verification-closure/05-docker-runtime-verification-closure-VERIFICATION.md, .planning/v1-v1-MILESTONE-AUDIT.md</files>
  <action>Update Phase 1 verification from `human_needed` to `passed` using captured phase-05 evidence references and corrected runtime ports (`44285` web, `6767` daemon). Create a phase-05 verification report summarizing DOCK-01 closure with links to all evidence artifacts. Refresh `.planning/v1-v1-MILESTONE-AUDIT.md` so requirements score is 11/11, status is passed (or equivalent no-gap state), and DOCK-01 is no longer listed in gaps.</action>
  <verify>Verification frontmatters reflect passed status, DOCK-01 gap list is empty, and evidence file paths referenced in both verification and audit docs exist.</verify>
  <done>Milestone audit no longer reports DOCK-01 blocker and v1 can be marked complete.</done>
</task>

</tasks>

<verification>
Confirm evidence set is complete (startup state, logs, process tree, WS 101 proof, post-stop state), then confirm phase verification docs and milestone audit all point to DOCK-01 as passed with zero remaining requirement gaps.
</verification>

<success_criteria>
`docker compose up --build` runtime is evidenced, browser WS handshake is evidenced at HTTP 101 from Docker-served URL, controlled stop shows no orphans, and milestone audit blocker list is empty.
</success_criteria>

<output>
After completion, create `.planning/phases/05-docker-runtime-verification-closure/05-docker-runtime-verification-closure-01-SUMMARY.md`
</output>
