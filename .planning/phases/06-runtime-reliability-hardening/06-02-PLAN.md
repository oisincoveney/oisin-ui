---
phase: 06-runtime-reliability-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/src/terminal/terminal-stream.ts
  - packages/web/src/terminal/terminal-stream.test.ts
autonomous: true
must_haves:
  truths:
    - "User input entered during attach recovery is queued (bounded) instead of dropped silently."
    - "Queued terminal input flushes automatically on successful attach recovery."
    - "Queue never grows unbounded; overflow/ttl drops are explicit and deterministic."
  artifacts:
    - "packages/web/src/terminal/terminal-stream.ts implements bounded queue with max bytes/chunks/ttl and flush-on-attach hook."
    - "packages/web/src/terminal/terminal-stream.test.ts validates enqueue, flush, ttl expiry, and bounded drop semantics."
  key_links:
    - "Attach success path must invoke stream confirmation that triggers queued input flush."
    - "Queue guard must enforce transport and stream-id readiness checks before send-now."
---

<objective>
Add bounded queued terminal input so reconnect recovery preserves user intent without replay storms (RUN-02 prerequisite).

Purpose: Keep input enabled during short reconnect windows while preventing unbounded buffering.
Output: Queue/flush input behavior in terminal adapter with deterministic tests.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-runtime-reliability-hardening/06-CONTEXT.md
@.planning/phases/06-runtime-reliability-hardening/06-RESEARCH.md
@packages/web/src/terminal/terminal-stream.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement bounded terminal input queue in adapter</name>
  <files>packages/web/src/terminal/terminal-stream.ts</files>
  <action>Add queue support for text input when websocket is disconnected, attach stream unavailable, or adapter not input-ready. Enforce hard bounds (`maxBytes`, `maxChunks`, `ttlMs`) and explicit drop behavior when limits exceeded. Keep send-now path unchanged when transport+stream are ready.</action>
  <verify>Run `bun run --filter @getpaseo/web typecheck`.</verify>
  <done>Input entered during recovery is retained within bounds and no unbounded queue growth is possible.</done>
</task>

<task type="auto">
  <name>Task 2: Flush queue on attach success and clear on invalidation</name>
  <files>packages/web/src/terminal/terminal-stream.ts</files>
  <action>Wire queue flush to attach confirmation lifecycle so buffered input is replayed in-order once stream is valid. Clear expired entries and clear queue on stream invalidation/switch where replay would be unsafe. Avoid duplicate replay across repeated attach confirmations.</action>
  <verify>Run `bun run --filter @getpaseo/web typecheck`.</verify>
  <done>Queued input flushes exactly once after successful attach and does not leak across stale streams.</done>
</task>

<task type="auto">
  <name>Task 3: Add adapter tests for queue bounds and flush semantics</name>
  <files>packages/web/src/terminal/terminal-stream.test.ts</files>
  <action>Add unit tests using fake timers for ttl expiry and exact flush ordering. Cover: enqueue while disconnected, flush on attach success, overflow drop behavior, and queue clear on stream invalidation.</action>
  <verify>Run `bun run --filter @getpaseo/web test -- src/terminal/terminal-stream.test.ts`.</verify>
  <done>Tests fail on regressions that drop intended queued input, replay stale input, or exceed configured queue bounds.</done>
</task>

</tasks>

<verification>
During reconnect retry window, typed input is preserved and sent after attach recovery, with bounded queue size and deterministic drop policy.
</verification>

<success_criteria>
RUN-02 queued-send contract exists and is bounded: no silent drop during short churn, no unbounded replay risk.
</success_criteria>

<output>
After completion, create `.planning/phases/06-runtime-reliability-hardening/06-runtime-reliability-hardening-02-SUMMARY.md`
</output>
