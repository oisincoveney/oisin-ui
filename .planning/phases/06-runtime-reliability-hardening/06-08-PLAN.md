---
phase: 06-runtime-reliability-hardening
plan: 08
type: execute
wave: 7
depends_on:
  - 06-06
  - 06-07
files_modified:
  - packages/server/src/client/daemon-client.ts
  - packages/server/src/server/test-utils/daemon-test-context.ts
  - packages/server/src/server/daemon-e2e/thread-management.e2e.test.ts
  - .planning/phases/06-runtime-reliability-hardening/06-runtime-reliability-hardening-VERIFICATION.md
autonomous: true
gap_closure: true
must_haves:
  truths:
    - "Phase 06 verification command chain (typecheck, daemon regression, web e2e) completes in one deterministic run."
    - "Daemon test setup consistently gets past initial fetchAgents without timeout flakes."
    - "RUN-01..RUN-04 verification evidence is reproducible from documented commands after gap closure."
  artifacts:
    - "packages/server/src/client/daemon-client.ts exposes/uses a bounded readiness barrier for first RPC safety."
    - "packages/server/src/server/test-utils/daemon-test-context.ts uses readiness barrier before initial fetchAgents."
    - "packages/server/src/server/daemon-e2e/thread-management.e2e.test.ts includes regression that fails on first-request timeout race."
    - ".planning/phases/06-runtime-reliability-hardening/06-runtime-reliability-hardening-VERIFICATION.md documents passing command sequence and evidence updates."
  key_links:
    - "daemon-test-context connect path must call daemon-client readiness barrier before fetchAgents."
    - "verification doc command chain must include passing daemon regression command that previously timed out."
---

<objective>
Close the client/test-context half of the fetchAgents timeout race and re-establish one deterministic end-to-end Phase 06 verification path.

Purpose: Ensure runtime reliability verification is repeatable and blocks regressions in first-RPC behavior.
Output: Bounded post-connect readiness barrier wiring, regression assertions, and refreshed verification evidence.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-runtime-reliability-hardening/06-UAT.md
@.planning/debug/phase-06-fetchagents-timeout.md
@.planning/phases/06-runtime-reliability-hardening/06-runtime-reliability-hardening-07-SUMMARY.md
@packages/server/src/client/daemon-client.ts
@packages/server/src/server/test-utils/daemon-test-context.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add explicit post-connect readiness barrier before first RPC</name>
  <files>packages/server/src/client/daemon-client.ts, packages/server/src/server/test-utils/daemon-test-context.ts</files>
  <action>Implement a bounded readiness barrier in daemon-client (or equivalent helper) that confirms session/request path readiness immediately after websocket open. Wire createDaemonTestContext to await this barrier before initial fetchAgents. Keep timeout/error messages actionable and aligned with existing waitForWithCancel semantics.</action>
  <verify>Run `bun run --filter @getpaseo/server test -- src/server/daemon-e2e/thread-management.e2e.test.ts` and confirm beforeEach initialization no longer flakes on fetchAgents timeout.</verify>
  <done>First RPC from daemon test context is gated by explicit readiness and no longer depends on ad-hoc delay timing.</done>
</task>

<task type="auto">
  <name>Task 2: Strengthen daemon e2e regression for first-request race closure</name>
  <files>packages/server/src/server/daemon-e2e/thread-management.e2e.test.ts</files>
  <action>Extend/adjust daemon e2e coverage to assert the first immediate post-connect fetchAgents/ping path succeeds within bounded time and cannot regress to silent drop timeout. Keep assertions deterministic and independent of ambient runtime state.</action>
  <verify>Run `bun run --filter @getpaseo/server test -- src/server/daemon-e2e/thread-management.e2e.test.ts`.</verify>
  <done>Regression suite reliably catches first-request drop reintroduction and passes with barrier + queue/drain fix in place.</done>
</task>

<task type="auto">
  <name>Task 3: Re-run deterministic Phase 06 verification path and update report</name>
  <files>.planning/phases/06-runtime-reliability-hardening/06-runtime-reliability-hardening-VERIFICATION.md</files>
  <action>Execute the documented deterministic command sequence end-to-end (typecheck + daemon regression + web e2e), capture updated evidence, and revise the verification report so RUN-01..RUN-04 mapping explicitly includes the now-passing daemon regression path that previously timed out.</action>
  <verify>Run documented sequence and ensure all commands pass, including daemon regression command in same run chain.</verify>
  <done>Verification report reflects a single repeatable passing path with explicit daemon + web evidence and no unresolved blocker gap.</done>
</task>

</tasks>

<verification>
Documented Phase 06 verification sequence completes in one run, and daemon regression no longer fails in setup with first-request timeout.
</verification>

<success_criteria>
Gap truth is restored: deterministic Phase 06 command chain is runnable end-to-end with stable daemon/web evidence for RUN-01..RUN-04.
</success_criteria>

<output>
After completion, create `.planning/phases/06-runtime-reliability-hardening/06-runtime-reliability-hardening-08-SUMMARY.md`
</output>
