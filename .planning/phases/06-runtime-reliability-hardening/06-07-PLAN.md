---
phase: 06-runtime-reliability-hardening
plan: 07
type: execute
wave: 6
depends_on:
  - 06-06
files_modified:
  - packages/server/src/server/websocket-server.ts
  - packages/server/src/server/agent/providers/claude-agent.ts
  - packages/server/src/server/daemon-e2e/thread-management.e2e.test.ts
autonomous: true
gap_closure: true
must_haves:
  truths:
    - "Running daemon thread-management e2e no longer times out during initial fetchAgents setup."
    - "An immediate first RPC after websocket connect receives a bounded response without manual retry."
    - "Daemon remains responsive during provider startup checks while diagnostics/error reporting still works."
  artifacts:
    - "packages/server/src/server/websocket-server.ts buffers and drains earliest messages only after session/message handling is ready."
    - "packages/server/src/server/agent/providers/claude-agent.ts removes sync provider checks from connect-critical startup path."
    - "packages/server/src/server/daemon-e2e/thread-management.e2e.test.ts contains regression coverage for first-request reliability."
  key_links:
    - "websocket-server connect path must wire pre-ready message queue -> post-init drain without dropping requestId-tagged RPCs."
    - "provider readiness path must remain asynchronous and must not delay websocket request handling."
---

<objective>
Close the server-side half of the fetchAgents timeout race by hardening websocket connect readiness and removing synchronous startup blocking.

Purpose: Make daemon request handling deterministic immediately after connect under runtime churn.
Output: Server-side queue/drain readiness guard plus non-blocking provider startup behavior with regression proof.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-runtime-reliability-hardening/06-UAT.md
@.planning/debug/phase-06-fetchagents-timeout.md
@packages/server/src/server/websocket-server.ts
@packages/server/src/server/agent/providers/claude-agent.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Queue and drain earliest websocket RPCs after session readiness</name>
  <files>packages/server/src/server/websocket-server.ts</files>
  <action>Add an explicit per-connection readiness gate in websocket connect handling. Before session/message dispatch is fully initialized, queue earliest incoming messages (preserve order); once ready, drain queue through the normal request path. Do not drop pre-ready messages and do not bypass existing requestId/correlation handling.</action>
  <verify>Run `bun run --filter @getpaseo/server test -- src/server/daemon-e2e/thread-management.e2e.test.ts` and confirm setup no longer times out at initial fetchAgents.</verify>
  <done>Immediate first RPC after connect is processed deterministically and createDaemonTestContext progresses past initial fetchAgents.</done>
</task>

<task type="auto">
  <name>Task 2: Remove synchronous provider checks from connect-critical startup</name>
  <files>packages/server/src/server/agent/providers/claude-agent.ts</files>
  <action>Move synchronous provider capability checks off the critical connect/request path. Replace blocking sync behavior with async cached initialization so websocket request handling can start immediately; preserve provider diagnostics and error reporting semantics.</action>
  <verify>Run `bun run --filter @getpaseo/server test -- src/server/daemon-e2e/thread-management.e2e.test.ts` and confirm no regression in provider detection behavior.</verify>
  <done>Connect path no longer depends on synchronous provider checks and does not widen first-request race windows.</done>
</task>

<task type="auto">
  <name>Task 3: Add server regression proving first-request reliability</name>
  <files>packages/server/src/server/daemon-e2e/thread-management.e2e.test.ts</files>
  <action>Add deterministic coverage that sends an immediate first RPC after connect and asserts a bounded successful response instead of timeout. Include a negative guard that fails if first-message drop behavior reappears.</action>
  <verify>Run `bun run --filter @getpaseo/server test -- src/server/daemon-e2e/thread-management.e2e.test.ts`.</verify>
  <done>Server e2e fails on first-request drops and passes when immediate post-connect RPC is reliable.</done>
</task>

</tasks>

<verification>
Daemon e2e suite runs without `timed out waiting for message (10000ms)` during setup and includes explicit first-RPC reliability assertion.
</verification>

<success_criteria>
Server-side connect/readiness race and startup blocking no longer cause first-request drop behavior under Phase 06 reliability regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/06-runtime-reliability-hardening/06-runtime-reliability-hardening-07-SUMMARY.md`
</output>
