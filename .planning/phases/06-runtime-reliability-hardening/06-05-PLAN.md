---
phase: 06-runtime-reliability-hardening
plan: 05
type: execute
wave: 4
depends_on:
  - 06-03
  - 06-04
files_modified:
  - packages/web/src/lib/ws.ts
  - packages/web/src/thread/thread-store.ts
  - packages/web/src/components/app-sidebar.tsx
  - packages/web/src/App.tsx
autonomous: true
must_haves:
  truths:
    - "After daemon restart, UI detects server identity change and enters bounded warm-up state instead of lock-churn loops."
    - "During warm-up, create/switch/delete actions are blocked with explicit reason."
    - "When warm-up completes, prior active thread restores when present; if missing, newest available thread is auto-selected; recovery shows `Reconnected` toast."
  artifacts:
    - "packages/web/src/lib/ws.ts exposes server_info payload including serverId to consumers."
    - "packages/web/src/thread/thread-store.ts stores runtime warm-up state and restart restoration metadata."
    - "packages/web/src/components/app-sidebar.tsx gates risky actions with disabled state + tooltip reason while warm-up active."
    - "packages/web/src/App.tsx orchestrates restart detection, warm-up completion, and thread restoration/fallback."
  key_links:
    - "WebSocket status `server_info.serverId` must be consumed and compared to prior serverId to detect daemon restart."
    - "Warm-up completion must wire to action gating release and single recovery toast emission."
---

<objective>
Implement restart-aware warm-up gating and deterministic active-thread restoration path (RUN-01).

Purpose: Ensure post-restart recovery is automatic, bounded, and safe from risky-action churn.
Output: serverId-driven restart detection, warm-up gating UI, and restoration fallback behavior.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-runtime-reliability-hardening/06-CONTEXT.md
@.planning/phases/06-runtime-reliability-hardening/06-RESEARCH.md
@packages/web/src/lib/ws.ts
@packages/web/src/thread/thread-store.ts
@packages/web/src/components/app-sidebar.tsx
@packages/web/src/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consume server_info.serverId and model restart warm-up state</name>
  <files>packages/web/src/lib/ws.ts, packages/web/src/thread/thread-store.ts, packages/web/src/App.tsx</files>
  <action>Parse/expose `status: server_info` payload in websocket client path and persist last seen `serverId`. When `serverId` changes, set warm-up active state with reason and recovery metadata. Keep current thread selection during warm-up and avoid immediate destructive action dispatch until warm-up completion criteria are met (thread list refresh + attach settle).</action>
  <verify>Run `bun run --filter @getpaseo/web typecheck`.</verify>
  <done>Daemon restart is explicitly detected in client runtime state and transitions into warm-up mode instead of repeated reconnect/attach churn.</done>
</task>

<task type="auto">
  <name>Task 2: Gate risky thread actions with minimal warm-up UX</name>
  <files>packages/web/src/components/app-sidebar.tsx</files>
  <action>Disable create/switch/delete controls while warm-up is active and provide tooltip reason text per locked context. Keep UX minimal (status chip style indicator, not global blocking banner). Preserve accessibility and existing sidebar interaction behavior outside warm-up state.</action>
  <verify>Run `bun run --filter @getpaseo/web typecheck` and restart daemon while sidebar is visible.</verify>
  <done>User cannot trigger risky actions during warm-up and always sees a clear reason for disabled controls.</done>
</task>

<task type="auto">
  <name>Task 3: Implement restore-or-fallback selection and recovery success clear</name>
  <files>packages/web/src/thread/thread-store.ts, packages/web/src/App.tsx</files>
  <action>After warm-up success, restore previously active thread when still available; if missing, auto-select newest available thread by timestamp ordering. Clear warm-up/recovery error state automatically and emit single `Reconnected` toast on successful recovery completion.</action>
  <verify>Run `bun run --filter @getpaseo/web test -- src/thread/thread-store.test.ts` and perform docker restart manual check.</verify>
  <done>Post-restart recovery deterministically resolves to valid active-thread state with no manual cleanup and no stale lock-churn loops.</done>
</task>

</tasks>

<verification>
Restart daemon while app is open: warm-up gate appears, risky actions are blocked with reason, and recovery completes with restored/fallback active thread plus one `Reconnected` toast.
</verification>

<success_criteria>
RUN-01 satisfied: restart/reconnect path is bounded, self-healing, and action-safe.
</success_criteria>

<output>
After completion, create `.planning/phases/06-runtime-reliability-hardening/06-runtime-reliability-hardening-05-SUMMARY.md`
</output>
