---
phase: 06-runtime-reliability-hardening
plan: 06
type: execute
wave: 5
depends_on:
  - 06-01
  - 06-02
  - 06-03
  - 06-04
  - 06-05
files_modified:
  - packages/server/src/server/daemon-e2e/thread-management.e2e.test.ts
  - packages/web/e2e/thread-management-web.spec.ts
  - .planning/phases/06-runtime-reliability-hardening/06-runtime-reliability-hardening-VERIFICATION.md
autonomous: true
must_haves:
  truths:
    - "Restart/reconnect, attach recovery, create failure, and active delete reliability checks run deterministically from code/tests/docs."
    - "Verification artifacts prove loops are bounded and no stale retries survive delete/restart recovery."
    - "Phase verification report can be marked pass/fail using reproducible command sequence."
  artifacts:
    - "packages/server/src/server/daemon-e2e/thread-management.e2e.test.ts contains bounded attach/delete lifecycle regressions."
    - "packages/web/e2e/thread-management-web.spec.ts contains reconnect/create/delete runtime hardening regressions."
    - ".planning/phases/06-runtime-reliability-hardening/06-runtime-reliability-hardening-VERIFICATION.md documents deterministic command path and evidence mapping to RUN-01..RUN-04."
  key_links:
    - "Verification doc commands must map one-to-one to RUN-01..RUN-04 truths and reference concrete evidence outputs."
    - "Web e2e and server e2e must assert bounded behavior (deadline/stop conditions), not only happy-path reconnect success."
---

<objective>
Close verification for Phase 06 with deterministic automated checks and refreshed verification report.

Purpose: Prove runtime hardening outcomes are reproducible and prevent regressions.
Output: Server/web regression coverage plus updated phase verification document mapped to RUN-01..RUN-04.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-runtime-reliability-hardening/06-CONTEXT.md
@.planning/phases/06-runtime-reliability-hardening/06-RESEARCH.md
@packages/server/src/server/daemon-e2e/thread-management.e2e.test.ts
@packages/web/e2e/thread-management-web.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add deterministic server-side regressions for bounded recovery</name>
  <files>packages/server/src/server/daemon-e2e/thread-management.e2e.test.ts</files>
  <action>Extend daemon e2e coverage for bounded attach recovery stop conditions and active-delete no-stale-retry behavior. Assert explicit negative proofs (no infinite attach retry loop, no post-delete attach requests) using deterministic timing guards.</action>
  <verify>Run `bun run --filter @getpaseo/server test -- src/server/daemon-e2e/thread-management.e2e.test.ts`.</verify>
  <done>Server e2e fails if bounded retry/deletion invariants regress.</done>
</task>

<task type="auto">
  <name>Task 2: Add deterministic web runtime regressions for restart/create/delete churn</name>
  <files>packages/web/e2e/thread-management-web.spec.ts</files>
  <action>Update/add web e2e scenarios for: restart warm-up action gating, bounded attach recovery indicator window, create-thread disconnected/timeout bounded errors with details, and immediate `No active thread` on active delete. Avoid flaky waits; use explicit state assertions and deterministic fixture setup.</action>
  <verify>Run `bun run --filter @getpaseo/web test:e2e -- thread-management-web.spec.ts`.</verify>
  <done>Web e2e fails if churn paths reintroduce indefinite pending, infinite retries, or stale selection behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Refresh phase verification report with command/evidence mapping</name>
  <files>.planning/phases/06-runtime-reliability-hardening/06-runtime-reliability-hardening-VERIFICATION.md</files>
  <action>Document deterministic verification runbook covering RUN-01..RUN-04 with concrete commands, expected pass markers, and evidence references. Mark each truth with pass/fail criteria tied to implemented tests and manual checks where required.</action>
  <verify>Run the documented command sequence end-to-end and ensure every requirement row has evidence links.</verify>
  <done>Phase verification doc is execution-ready and traceable for all four runtime reliability requirements.</done>
</task>

</tasks>

<verification>
All RUN-01..RUN-04 checks are reproducible via documented commands and test suites without ad-hoc interpretation.
</verification>

<success_criteria>
Phase 06 closure can be validated deterministically with bounded-behavior proofs for restart, attach, create, and delete flows.
</success_criteria>

<output>
After completion, create `.planning/phases/06-runtime-reliability-hardening/06-runtime-reliability-hardening-06-SUMMARY.md`
</output>
