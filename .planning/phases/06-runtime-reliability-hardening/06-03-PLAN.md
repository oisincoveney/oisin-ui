---
phase: 06-runtime-reliability-hardening
plan: 03
type: execute
wave: 2
depends_on:
  - 06-02
files_modified:
  - packages/web/src/App.tsx
  - packages/web/src/components/ConnectionOverlay.tsx
  - packages/web/src/App.test.tsx
autonomous: true
must_haves:
  truths:
    - "When reconnect succeeds but attach fails, UI enters visible retrying state and retries for max 60 seconds."
    - "Attach retries stop at deadline and surface bounded actionable failure instead of infinite `Terminal not found` loop."
    - "Successful attach recovery clears stale errors and emits one `Reconnected` success toast."
  artifacts:
    - "packages/web/src/App.tsx contains attach recovery finite-state logic (`idle/retrying/failed`) with deadline and timer cleanup."
    - "packages/web/src/components/ConnectionOverlay.tsx shows retry indicator (attempt/deadline/error context)."
    - "packages/web/src/App.test.tsx validates bounded retry stop and success clear/toast behavior."
  key_links:
    - "App attach failure handler must transition to retrying state and schedule bounded retries, not immediate unbounded recursion."
    - "Attach success handler must clear recovery failure state and emit single reconnect toast signal."
---

<objective>
Implement bounded attach recovery state machine with visible retry UX and one-shot recovery success signal (RUN-02 core).

Purpose: Prevent repeated `Terminal not found` churn loops while preserving transparent recovery behavior.
Output: 60-second attach retry lifecycle wired to overlay and success cleanup.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-runtime-reliability-hardening/06-CONTEXT.md
@.planning/phases/06-runtime-reliability-hardening/06-RESEARCH.md
@packages/web/src/App.tsx
@packages/web/src/components/ConnectionOverlay.tsx
@.planning/phases/06-runtime-reliability-hardening/06-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add bounded attach recovery state machine in App lifecycle</name>
  <files>packages/web/src/App.tsx</files>
  <action>Introduce explicit attach recovery state (`idle`, `retrying`, `failed`) with `startedAt`, `deadlineAt` (60s), `attempt`, and `lastError`. On reconnect+attach failure, retry with bounded timer/backoff until deadline; on deadline transition to failed and stop retries. Ensure stale-response cycle guards remain active and all retry timers are cleaned up on unmount/thread change.</action>
  <verify>Run `bun run --filter @getpaseo/web typecheck`.</verify>
  <done>Attach retries are finite and cannot loop indefinitely after terminal-id mismatch failures.</done>
</task>

<task type="auto">
  <name>Task 2: Surface attach retry indicator and recovery success signaling</name>
  <files>packages/web/src/components/ConnectionOverlay.tsx</files>
  <action>Render prominent retry status while in `retrying` phase (attempt count + remaining window + last error summary). Preserve last terminal output and reconnect overlay behavior from locked context. On successful attach recovery, clear prior failure state automatically and emit one `Reconnected` toast.</action>
  <verify>Run `bun run --filter @getpaseo/web typecheck` and restart daemon to observe retry indicator transitions.</verify>
  <done>User sees transparent bounded recovery status and receives single success toast when attach recovers.</done>
</task>

<task type="auto">
  <name>Task 3: Add tests for bounded retry deadline and success clear</name>
  <files>packages/web/src/App.test.tsx</files>
  <action>Add timer-driven tests for retry scheduling and hard stop at 60s deadline. Add success-path test ensuring failed/retrying state clears and `Reconnected` toast fires once per recovery cycle.</action>
  <verify>Run `bun run --filter @getpaseo/web test -- src/App.test.tsx`.</verify>
  <done>Regression tests fail if retries exceed deadline, never stop, or emit repeated success toast spam.</done>
</task>

</tasks>

<verification>
Reconnect with stale terminal mapping shows bounded retry indicator and either recovers with one toast or fails cleanly after 60 seconds.
</verification>

<success_criteria>
RUN-02 attach recovery is finite, visible, and self-clearing on success.
</success_criteria>

<output>
After completion, create `.planning/phases/06-runtime-reliability-hardening/06-runtime-reliability-hardening-03-SUMMARY.md`
</output>
