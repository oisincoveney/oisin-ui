---
phase: 06-runtime-reliability-hardening
plan: 04
type: execute
wave: 3
depends_on:
  - 06-01
  - 06-03
files_modified:
  - packages/web/src/thread/thread-store.ts
  - packages/web/src/App.tsx
  - packages/web/src/thread/thread-store.test.ts
autonomous: true
must_haves:
  truths:
    - "Deleting the active thread switches immediately to `No active thread` with removed sidebar row."
    - "No stale attach retries continue after active thread deletion succeeds."
    - "If delete fails, previous active thread selection is restored."
  artifacts:
    - "packages/web/src/thread/thread-store.ts keeps active selection null after successful active delete and performs rollback only on failure."
    - "packages/web/src/App.tsx cancels pending attach retry timers and invalidates stale attach cycle when active thread becomes null."
    - "packages/web/src/thread/thread-store.test.ts covers success-null semantics and rollback-on-failure behavior."
  key_links:
    - "Delete success response handler must not auto-select fallback thread."
    - "App attach retry scheduler must observe active-thread-null transition and stop all retry activity immediately."
---

<objective>
Harden active-thread deletion so UI lands immediately in neutral state and stale attach activity is canceled (RUN-04).

Purpose: Remove post-delete stale attach churn and surprising fallback selection behavior.
Output: Immediate null active-state semantics plus attach retry cancellation guarantees.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-runtime-reliability-hardening/06-CONTEXT.md
@.planning/phases/06-runtime-reliability-hardening/06-RESEARCH.md
@packages/web/src/thread/thread-store.ts
@packages/web/src/App.tsx
@.planning/phases/06-runtime-reliability-hardening/06-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enforce delete-active immediate null and no auto-fallback</name>
  <files>packages/web/src/thread/thread-store.ts</files>
  <action>Change active delete success behavior to keep `activeThreadKey` as `null` and remove deleted thread row immediately. Preserve optimistic null transition on confirm. Roll back prior active thread only when delete request fails. Do not auto-switch to another thread on successful active delete.</action>
  <verify>Run `bun run --filter @getpaseo/web typecheck`.</verify>
  <done>Successful active delete leaves UI in `No active thread` until user explicitly picks/creates another thread.</done>
</task>

<task type="auto">
  <name>Task 2: Cancel attach retries when active thread is cleared</name>
  <files>packages/web/src/App.tsx</files>
  <action>On active-thread transition to null (including delete-success path), cancel pending attach requests, clear attach retry timers, bump attach cycle guards to invalidate in-flight responses, and clear retry indicator state. Ensure no new attach attempts are scheduled until a new active thread is selected.</action>
  <verify>Run `bun run --filter @getpaseo/web typecheck` and delete active thread during reconnect churn.</verify>
  <done>No attach retry or `Terminal not found` loop continues after active thread deletion success.</done>
</task>

<task type="auto">
  <name>Task 3: Add deletion reliability regression tests</name>
  <files>packages/web/src/thread/thread-store.test.ts</files>
  <action>Add tests covering immediate null-on-success, removed-row state, rollback on failure, and no fallback selection on success. Add assertion hooks for attach cancellation trigger where existing test harness allows.</action>
  <verify>Run `bun run --filter @getpaseo/web test -- src/thread/thread-store.test.ts`.</verify>
  <done>Tests fail if delete success auto-selects a fallback thread or if failure no longer restores prior active selection.</done>
</task>

</tasks>

<verification>
Deleting the active thread always lands in `No active thread` immediately, and stale attach retries stop.
</verification>

<success_criteria>
RUN-04 is satisfied: immediate null state on active delete success, deterministic rollback on delete failure, no stale attach churn.
</success_criteria>

<output>
After completion, create `.planning/phases/06-runtime-reliability-hardening/06-runtime-reliability-hardening-04-SUMMARY.md`
</output>
