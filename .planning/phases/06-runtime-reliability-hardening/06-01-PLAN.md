---
phase: 06-runtime-reliability-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/src/thread/thread-store.ts
  - packages/web/src/components/thread-create-dialog.tsx
  - packages/web/src/thread/thread-store.test.ts
autonomous: true
must_haves:
  truths:
    - "User submitting Create Thread while websocket is not open gets immediate dialog-scoped error, not indefinite pending."
    - "Create bootstrap failure shows concise summary plus expandable technical details with copy action."
    - "Create timeout returns dialog to non-pending state exactly at timeout boundary while preserving form values."
  artifacts:
    - "packages/web/src/thread/thread-store.ts defines structured create failure payload and bounded pending lifecycle cleanup."
    - "packages/web/src/components/thread-create-dialog.tsx renders summary/details/copy UX from structured error payload."
    - "packages/web/src/thread/thread-store.test.ts covers disconnected send, timeout boundary reset, and bootstrap failure detail contract."
  key_links:
    - "Create submit path in thread-create-dialog must consume create error object from thread-store, not global toast path."
    - "thread-store create request lifecycle must always resolve pending by response/send-failure/timeout/store-stop."
---

<objective>
Make thread creation failure behavior bounded and actionable under transient websocket disruption (RUN-03).

Purpose: Eliminate create pending leaks and convert failures into immediate retryable dialog feedback.
Output: Structured create error contract with dialog rendering and regression tests.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-runtime-reliability-hardening/06-CONTEXT.md
@.planning/phases/06-runtime-reliability-hardening/06-RESEARCH.md
@packages/web/src/thread/thread-store.ts
@packages/web/src/components/thread-create-dialog.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden create-thread pending and failure contract</name>
  <files>packages/web/src/thread/thread-store.ts</files>
  <action>Replace create error string usage with a typed object shape `{ summary, details, copyText, requestId }`. Preserve existing immediate send-failure behavior, enforce timeout-bound pending reset at exact deadline, and include bootstrap/server failure details in the error object. Keep failure scoped to create dialog and preserve form values for retry.</action>
  <verify>Run `bun run --filter @getpaseo/web typecheck`.</verify>
  <done>Create flow cannot remain pending indefinitely; disconnected, timeout, and bootstrap failures all produce structured dialog-consumable errors.</done>
</task>

<task type="auto">
  <name>Task 2: Render actionable dialog failure UX with details and copy</name>
  <files>packages/web/src/components/thread-create-dialog.tsx</files>
  <action>Update create dialog error UI to show summary first, add expandable details section, and one-click copy action using `copyText` payload. Do not emit global toast for create failures. Ensure pending button label and disabled state clear when timeout or send failure path fires.</action>
  <verify>Run `bun run --filter @getpaseo/web typecheck` and manually trigger create while socket disconnected.</verify>
  <done>Dialog shows summary + expandable details + copy button and returns from `Creating...` to idle on bounded failure.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression tests for bounded create failure lifecycle</name>
  <files>packages/web/src/thread/thread-store.test.ts</files>
  <action>Add tests for three paths: websocket send failure immediate error, timeout boundary reset, and bootstrap failure details propagation. Use fake timers for timeout boundary assertions and confirm pending cleanup on store teardown.</action>
  <verify>Run `bun run --filter @getpaseo/web test -- src/thread/thread-store.test.ts`.</verify>
  <done>Tests fail if create pending leaks or if structured error payload regresses to flat string behavior.</done>
</task>

</tasks>

<verification>
Create Thread under disconnected/reconnecting transport produces immediate or bounded dialog error with preserved form values and no indefinite spinner.
</verification>

<success_criteria>
RUN-03 create flow is bounded and actionable: no pending leak, no global error leakage, copyable details present.
</success_criteria>

<output>
After completion, create `.planning/phases/06-runtime-reliability-hardening/06-runtime-reliability-hardening-01-SUMMARY.md`
</output>
