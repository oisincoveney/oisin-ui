---
phase: 02-terminal-i
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/server/src/shared/messages.ts
  - packages/server/src/client/daemon-client.ts
  - packages/server/src/terminal/terminal-manager.ts
  - packages/server/src/terminal/tmux-terminal.ts
  - packages/server/src/server/session.ts
  - packages/server/src/server/bootstrap.ts
  - packages/server/src/server/config.ts
  - packages/server/src/server/daemon-e2e/terminal.e2e.test.ts
autonomous: true
must_haves:
  truths:
    - User can reconnect and return to the same terminal session instead of a fresh shell.
    - Initial terminal hydration can be rebuilt from tmux scrollback.
    - Terminal stream attach can resume by byte offset and request reset when needed.
  artifacts:
    - path: packages/server/src/terminal/tmux-terminal.ts
      provides: tmux-backed TerminalSession implementation with capture-pane replay support.
    - path: packages/server/src/terminal/terminal-manager.ts
      provides: default terminal bootstrap keyed by working directory-derived session name.
    - path: packages/server/src/shared/messages.ts
      provides: request/response contracts for default terminal bootstrap and stream attach metadata.
    - path: packages/server/src/client/daemon-client.ts
      provides: typed client methods for default terminal bootstrap + stream attach/resume.
  key_links:
    - from: packages/server/src/server/session.ts
      to: packages/server/src/terminal/tmux-terminal.ts
      via: default terminal bootstrap and attach stream path.
      pattern: handleAttachTerminalStreamRequest|ensure.*default.*terminal
    - from: packages/server/src/client/daemon-client.ts
      to: packages/server/src/shared/messages.ts
      via: correlated request wrappers for terminal bootstrap.
      pattern: type: '.*terminal.*request'
---

<objective>
Make the daemon terminal backend tmux-authoritative so browser sessions can reattach and replay state after disconnects or refreshes.

Purpose: TERM-02 and TERM-04 require recovery semantics that survive socket churn and tab refreshes.
Output: A tmux-backed terminal runtime plus typed bootstrap RPCs that return the same default terminal session for the current project directory.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-terminal-i/02-RESEARCH.md
@.planning/phases/02-terminal-i/o/02-CONTEXT.md
@packages/server/src/terminal/terminal-manager.ts
@packages/server/src/server/session.ts
@packages/server/src/shared/messages.ts
@packages/server/src/client/daemon-client.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement tmux-backed terminal session runtime</name>
  <files>packages/server/src/terminal/tmux-terminal.ts, packages/server/src/terminal/terminal-manager.ts</files>
  <action>Create a tmux-backed terminal session implementation that satisfies the existing TerminalSession interface, including input forwarding, resize forwarding, on-exit hooks, byte-offset raw stream subscription, and replay/reset metadata. Use tmux capture-pane full history (`-S- -E-`) for initial hydration and reset recovery. Keep existing API shape stable so Session callers do not need terminal-type branches.</action>
  <verify>npm run test --workspace=@getpaseo/server -- terminal</verify>
  <done>TerminalManager creates/returns tmux-backed sessions and can replay full scrollback with offset metadata for attach callers.</done>
</task>

<task type="auto">
  <name>Task 2: Add default terminal bootstrap RPC and cwd-derived session naming</name>
  <files>packages/server/src/shared/messages.ts, packages/server/src/client/daemon-client.ts, packages/server/src/server/session.ts, packages/server/src/server/config.ts, packages/server/src/server/bootstrap.ts</files>
  <action>Add a typed request/response for “ensure default terminal” that does not require browser-supplied cwd. On the server, derive the tmux session key from the daemon/project working directory, create-or-reuse the single phase-2 terminal, and auto-run the configured agent command (default `opencode`). Expose this through Session + DaemonClient and keep existing attach_terminal_stream_request semantics (resumeOffset/reset/replayedFrom) intact.</action>
  <verify>npm run typecheck && npm run test --workspace=@getpaseo/server -- terminal.e2e</verify>
  <done>Browser clients can request the default terminal and always receive the same terminal identity for reconnect/refresh flows.</done>
</task>

<task type="auto">
  <name>Task 3: Harden server reconnect/catch-up coverage</name>
  <files>packages/server/src/server/daemon-e2e/terminal.e2e.test.ts</files>
  <action>Add/extend daemon E2E coverage for attach with resumeOffset, stale offset reset path, and full scrollback capture fallback so replay behavior is validated before web integration. Prefer asserting replayedFrom/currentOffset/reset rather than brittle terminal text snapshots.</action>
  <verify>npm run test --workspace=@getpaseo/server -- terminal.e2e</verify>
  <done>Server tests explicitly cover offset-resume, reset fallback, and tmux-backed redraw behavior.</done>
</task>

</tasks>

<verification>
Server exposes a stable default-terminal bootstrap endpoint and terminal streaming remains binary mux compatible.
</verification>

<success_criteria>
TERM-02/TERM-04 backend prerequisites are complete: tmux-backed default terminal identity + offset-aware attach/resume/reset semantics validated by server tests.
</success_criteria>

<output>
After completion, create `.planning/phases/02-terminal-i/02-terminal-i-01-SUMMARY.md`
</output>
