---
phase: 02-terminal-i
plan: 03
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - packages/web/src/lib/ws.ts
  - packages/web/src/components/ConnectionOverlay.tsx
  - packages/web/src/terminal/terminal-stream.ts
  - packages/web/src/terminal/terminal-resize.ts
  - packages/web/src/terminal/terminal-view.tsx
  - packages/web/src/App.tsx
  - packages/server/src/server/session.ts
  - packages/server/src/server/daemon-e2e/terminal.e2e.test.ts
autonomous: true
must_haves:
  truths:
    - Socket disconnects trigger immediate disconnected UI and automatic exponential reconnect forever.
    - Reconnect restores exact terminal state via resume or reset redraw without losing long-running output.
    - Browser resize updates tmux dimensions with debounce while stream output keeps flowing.
    - Reconnected terminal catches up missed output and returns viewport to bottom after catch-up.
  artifacts:
    - path: packages/web/src/lib/ws.ts
      provides: reconnect telemetry and infinite retry state transitions.
    - path: packages/web/src/terminal/terminal-resize.ts
      provides: ResizeObserver + debounced resize propagation pipeline.
    - path: packages/web/src/terminal/terminal-stream.ts
      provides: reconnect resume/reset/catch-up behavior and stale input dropping.
    - path: packages/server/src/server/session.ts
      provides: attach/reset behavior required by reconnect catch-up paths.
  key_links:
    - from: packages/web/src/lib/ws.ts
      to: packages/web/src/components/ConnectionOverlay.tsx
      via: instant status transitions on close/error.
      pattern: disconnected|reconnecting
    - from: packages/web/src/terminal/terminal-resize.ts
      to: packages/server/src/server/session.ts
      via: debounced rows/cols updates sent over websocket.
      pattern: resize
    - from: packages/web/src/terminal/terminal-stream.ts
      to: packages/server/src/server/session.ts
      via: resumeOffset attach and reset-driven full redraw.
      pattern: resumeOffset|reset
---

<objective>
Complete reconnect, resize-sync, and catch-up reliability so terminal sessions survive network interruptions and UI resizing without data loss.

Purpose: TERM-02, TERM-03, and TERM-04 are reliability guarantees, not just initial rendering.
Output: reconnect-safe terminal runtime with infinite backoff, redraw/catch-up logic, resize propagation, and validation coverage.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-terminal-i/02-RESEARCH.md
@.planning/phases/02-terminal-i/o/02-CONTEXT.md
@.planning/phases/02-terminal-i/02-terminal-i-01-SUMMARY.md
@.planning/phases/02-terminal-i/02-terminal-i-02-SUMMARY.md
@packages/web/src/lib/ws.ts
@packages/web/src/components/ConnectionOverlay.tsx
@packages/server/src/server/session.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement resilient reconnect flow with reset redraw + catch-up</name>
  <files>packages/web/src/lib/ws.ts, packages/web/src/terminal/terminal-stream.ts, packages/web/src/terminal/terminal-view.tsx, packages/web/src/App.tsx</files>
  <action>On websocket close/error, immediately mark disconnected, clear in-flight keystrokes, and show existing reconnect overlay with blurred terminal background while keeping selection/copy possible. Reconnect with exponential backoff (no retry cap) and log each attempt/failure to browser console. On reconnect without hard refresh, request refresh-from-server redraw; if attach reports reset or offset gap, rehydrate from full tmux capture and then resume live stream, scrolling to bottom after catch-up.</action>
  <verify>npm run build --workspace=@oisin/web && npm run typecheck</verify>
  <done>Disconnect/reconnect cycles recover terminal state reliably with no stale buffered input replayed after reconnection.</done>
</task>

<task type="auto">
  <name>Task 2: Add debounced resize pipeline across xterm/ws/tmux</name>
  <files>packages/web/src/terminal/terminal-resize.ts, packages/web/src/terminal/terminal-view.tsx, packages/server/src/server/session.ts</files>
  <action>Implement ResizeObserver-driven fit on container changes, then debounce backend resize propagation (~200ms) so tmux receives bounded resize updates while output streaming remains uninterrupted. Ensure rows/cols are included during initial attach and subsequent resize messages to prevent vim/OpenCode rendering drift.</action>
  <verify>npm run typecheck && npm run test --workspace=@getpaseo/server -- terminal.e2e</verify>
  <done>Resizing browser window keeps xterm and tmux dimensions synchronized without output pauses or layout corruption.</done>
</task>

<task type="auto">
  <name>Task 3: Lock reliability with reconnect regression coverage</name>
  <files>packages/server/src/server/daemon-e2e/terminal.e2e.test.ts, packages/web/src/components/ConnectionOverlay.tsx</files>
  <action>Add/extend regression checks for disconnect status transitions, resumeOffset attach behavior, reset fallback redraw path, and post-catch-up scroll-to-bottom semantics. Keep overlay copy/select behavior intact while terminal is blurred/disconnected.</action>
  <verify>npm run test --workspace=@getpaseo/server -- terminal.e2e && npm run typecheck</verify>
  <done>Automated checks cover core reconnect and resize failure modes, and UI disconnect behavior matches locked phase context decisions.</done>
</task>

</tasks>

<verification>
Simulated disconnect + reconnect + resize runs preserve session continuity and terminal correctness.
</verification>

<success_criteria>
TERM-02, TERM-03, and TERM-04 are satisfied for the active-thread placeholder terminal: infinite reconnect retries, exact session resume, and synchronized dimensions across browser, websocket, and tmux. Real multi-thread session routing remains a Phase 3 responsibility.
</success_criteria>

<output>
After completion, create `.planning/phases/02-terminal-i/02-terminal-i-03-SUMMARY.md`
</output>
