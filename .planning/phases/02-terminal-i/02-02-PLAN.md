---
phase: 02-terminal-i
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - packages/web/package.json
  - package-lock.json
  - packages/web/src/index.css
  - packages/web/src/lib/ws.ts
  - packages/web/src/terminal/terminal-stream.ts
  - packages/web/src/terminal/terminal-resize.ts
  - packages/web/src/terminal/terminal-view.tsx
  - packages/web/src/App.tsx
autonomous: true
must_haves:
  truths:
    - User can see and interact with a live embedded terminal in the browser.
    - Terminal output is rendered from binary mux stream data and input is sent back to tmux.
    - Hard refresh can rehydrate full terminal history before live streaming resumes.
  artifacts:
    - path: packages/web/src/terminal/terminal-view.tsx
      provides: xterm v6 mount/open/dispose + input/output bridge.
    - path: packages/web/src/terminal/terminal-stream.ts
      provides: browser-side binary mux decode/ack plumbing with offset tracking.
    - path: packages/web/src/App.tsx
      provides: full-screen terminal shell and default-terminal bootstrap wiring.
    - path: packages/web/src/lib/ws.ts
      provides: websocket transport hooks for text + binary handlers used by terminal runtime.
  key_links:
    - from: packages/web/src/App.tsx
      to: packages/server/src/client/daemon-client.ts
      via: default terminal bootstrap then stream attach.
      pattern: ensure.*default.*terminal|attachTerminalStream
    - from: packages/web/src/terminal/terminal-stream.ts
      to: packages/server/src/shared/binary-mux.ts
      via: mux frame decode + ack send on output apply.
      pattern: decodeBinaryMuxFrame|TerminalBinaryMessageType\.Ack
---

<objective>
Build the browser terminal UI and stream adapter so users can type commands and see live tmux output in a full-screen terminal.

Purpose: TERM-01 requires a production terminal surface, not just connection status.
Output: xterm.js-based terminal component wired to the daemon binary mux stream, including initial hydration from server state.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-terminal-i/02-RESEARCH.md
@.planning/phases/02-terminal-i/o/02-CONTEXT.md
@.planning/phases/02-terminal-i/02-terminal-i-01-SUMMARY.md
@packages/web/src/lib/ws.ts
@packages/web/src/App.tsx
@packages/server/src/shared/binary-mux.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add xterm v6 terminal surface and theme/font wiring</name>
  <files>packages/web/package.json, package-lock.json, packages/web/src/index.css, packages/web/src/terminal/terminal-view.tsx</files>
  <action>Install `@xterm/xterm`, `@xterm/addon-fit`, and `@xterm/addon-webgl`; create a full-screen `terminal-view` component that mounts xterm, loads Fit/WebGL addons, applies theme colors from CSS variables, and uses a bundled programming font fallback chain. Ensure component lifecycle disposes addons and terminal cleanly on unmount.</action>
  <verify>npm run build --workspace=@oisin/web</verify>
  <done>The app renders a full-screen xterm instance with project-consistent colors and no lifecycle leaks on remount.</done>
</task>

<task type="auto">
  <name>Task 2: Implement browser binary stream adapter with ack/offset tracking</name>
  <files>packages/web/src/lib/ws.ts, packages/web/src/terminal/terminal-stream.ts</files>
  <action>Extend websocket client utilities to support binary frame receive/send and build a `terminal-stream` adapter that decodes terminal output frames, writes chunks to xterm, tracks highest applied offset, and sends ACK frames back to the server window. Keep byte offsets authoritative (not character count) and preserve reset/replay semantics from attach responses.</action>
  <verify>npm run typecheck --workspace=@oisin/web</verify>
  <done>Output chunks are rendered in order, ACKs are emitted with applied offsets, and stream state can resume from stored offsets.</done>
</task>

<task type="auto">
  <name>Task 3: Wire App to default terminal bootstrap + initial scrollback hydration</name>
  <files>packages/web/src/App.tsx, packages/web/src/terminal/terminal-view.tsx</files>
  <action>Replace placeholder landing content with terminal-first layout. On load, request the server default terminal, attach stream with current dimensions, hydrate from full tmux scrollback on first load/hard refresh, then continue live streaming. Keep existing overlay/status indicator visible and avoid writing reconnect banners into terminal buffer.</action>
  <verify>npm run build --workspace=@oisin/web && npm run typecheck</verify>
  <done>Browser shows a usable live terminal bound to the server default session, with full history visible on hard refresh before live output continues.</done>
</task>

</tasks>

<verification>
Web client can open terminal, send input, render live output, and hydrate prior tmux history on page load.
</verification>

<success_criteria>
TERM-01 is satisfied end-to-end: embedded interactive terminal per active thread placeholder with binary stream rendering and initial history hydration.
</success_criteria>

<output>
After completion, create `.planning/phases/02-terminal-i/02-terminal-i-02-SUMMARY.md`
</output>
