---
phase: 02-terminal-i
plan: 05
type: execute
wave: 5
depends_on:
  - "02-04"
files_modified:
  - packages/web/src/lib/ws.ts
  - packages/server/src/server/config.ts
  - scripts/start.sh
  - packages/web/src/App.tsx
  - packages/web/src/components/ConnectionOverlay.tsx
  - packages/server/playwright.config.ts
  - packages/server/e2e/terminal-web-smoke.spec.ts
autonomous: true
gap_closure: true
must_haves:
  truths:
    - Opening the web client attaches to the daemon endpoint used by runtime defaults, so terminal cursor and input become active without manual env guesswork.
    - If ws/attach fails, the UI shows actionable endpoint/error telemetry instead of silent non-interactive terminal state.
    - A browser-level automated smoke test fails when terminal cursor/input/output interactivity regresses.
  artifacts:
    - path: packages/web/src/lib/ws.ts
      provides: daemon ws endpoint resolution aligned to runtime default and exposed connection diagnostics.
    - path: packages/server/src/server/config.ts
      provides: daemon listen default and comments/documentation consistency.
    - path: scripts/start.sh
      provides: single source-of-truth export for daemon/web port alignment in local container/dev startup.
    - path: packages/web/src/App.tsx
      provides: attach failure diagnostics surfaced to UI state.
    - path: packages/web/src/components/ConnectionOverlay.tsx
      provides: visible ws endpoint + failure reason telemetry during connecting/disconnected states.
    - path: packages/server/playwright.config.ts
      provides: executable browser smoke harness for server workspace e2e checks.
    - path: packages/server/e2e/terminal-web-smoke.spec.ts
      provides: terminal interactivity smoke assertions for cursor visibility, input echo, and output rendering.
  key_links:
    - from: packages/web/src/lib/ws.ts
      to: packages/server/src/server/config.ts
      via: shared daemon port default + explicit env override path.
      pattern: VITE_DAEMON_PORT|DEFAULT_PORT|getDefaultListen
    - from: scripts/start.sh
      to: packages/web/src/lib/ws.ts
      via: exported VITE_DAEMON_PORT derived from daemon listen configuration.
      pattern: PASEO_LISTEN|VITE_DAEMON_PORT
    - from: packages/web/src/App.tsx
      to: packages/web/src/components/ConnectionOverlay.tsx
      via: propagated attach/ws diagnostics rendered in overlay.
      pattern: attach failed|status|diagnostic
    - from: packages/server/e2e/terminal-web-smoke.spec.ts
      to: packages/web/src/App.tsx
      via: browser interaction asserting terminal ready, input, and output behavior.
      pattern: cursor|echo|terminal
---

<objective>
Close the diagnosed UAT blocker where the embedded terminal is non-interactive on load by removing ws endpoint drift and adding a browser smoke gate.

Purpose: prevent silent ws/attach mismatches from shipping and make failures immediately diagnosable when they occur.
Output: aligned daemon/web endpoint defaults, UI telemetry for attach/ws failures, and an automated browser smoke test for terminal interactivity.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02-terminal-i/02-terminal-i-UAT.md
@.planning/phases/02-terminal-i/02-terminal-i-VERIFICATION.md
@.planning/debug/phase-02-terminal-i-uat-block.md
@packages/web/src/lib/ws.ts
@packages/web/src/App.tsx
@packages/server/src/server/config.ts
@scripts/start.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unify daemon endpoint defaults across server runtime and web ws resolution</name>
  <files>packages/web/src/lib/ws.ts, packages/server/src/server/config.ts, scripts/start.sh</files>
  <action>Make one port default path for terminal ws connectivity. Align `packages/server/src/server/config.ts` default/comment so documented default and actual listen default are identical. Update `packages/web/src/lib/ws.ts` ws URL resolution to use the same daemon default when explicit web env override is absent, and keep override support for container/dev variants. In `scripts/start.sh`, export the web daemon port env from the daemon listen setting so web and daemon stay synchronized in local/docker startup. Avoid introducing a second fallback chain or magic constants in multiple files.</action>
  <verify>npm run typecheck --workspace=@getpaseo/server && npm run typecheck --workspace=@oisin/web</verify>
  <done>With no manual env tweaking, ws target selection follows runtime defaults consistently; config comments and behavior no longer disagree; local/docker startup exports a single aligned daemon port source.</done>
</task>

<task type="auto">
  <name>Task 2: Surface ws endpoint mismatch and attach failure diagnostics in terminal UI</name>
  <files>packages/web/src/lib/ws.ts, packages/web/src/App.tsx, packages/web/src/components/ConnectionOverlay.tsx</files>
  <action>Add lightweight connection diagnostics state that records ws target endpoint and last connection/attach failure reason. Thread this state into App and ConnectionOverlay so users see actionable telemetry (target endpoint + failure cause/hint) while connecting/reconnecting/disconnected. Keep overlay non-interactive (`pointer-events-none`) and do not regress existing reconnect behavior or terminal selection/copy semantics.</action>
  <verify>npm run typecheck --workspace=@oisin/web</verify>
  <done>When attach/ws fails, UI clearly states which endpoint was attempted and why terminal activation failed, instead of appearing idle without context.</done>
</task>

<task type="auto">
  <name>Task 3: Add browser-level terminal interactivity smoke test</name>
  <files>packages/server/playwright.config.ts, packages/server/e2e/terminal-web-smoke.spec.ts</files>
  <action>Create a Playwright smoke suite in server workspace that boots isolated daemon + web test runtime, opens the web terminal, and asserts: terminal mounts, cursor becomes active after attach, typing an `echo` command yields output in the terminal, and no attach failure overlay remains. Reuse existing daemon test harness patterns for isolated temp runtime/ports; avoid brittle timing sleeps by waiting on explicit UI/state conditions.</action>
  <verify>npm run test:e2e --workspace=@getpaseo/server -- e2e/terminal-web-smoke.spec.ts</verify>
  <done>Automated browser smoke fails for non-interactive terminal regressions and passes when cursor/input/output interactivity works end-to-end.</done>
</task>

</tasks>

<verification>
Gap is closed when terminal interactivity loads correctly under default setup, diagnostics make endpoint/attach failures explicit, and browser smoke enforces cursor+input+output behavior in CI/local e2e.
</verification>

<success_criteria>
The failed UAT truth is now verifiable: opening the web client yields an interactive terminal with active cursor/input/output, and regressions are blocked by an automated smoke test.
</success_criteria>

<output>
After completion, create `.planning/phases/02-terminal-i/02-terminal-i-05-SUMMARY.md`
</output>
