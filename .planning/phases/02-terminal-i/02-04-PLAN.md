---
phase: 02-terminal-i
plan: 04
type: execute
wave: 4
depends_on:
  - "02-03"
files_modified:
  - packages/web/src/App.tsx
  - packages/server/src/terminal/terminal-manager.ts
  - packages/server/src/server/session.ts
  - packages/server/src/server/daemon-e2e/terminal.e2e.test.ts
autonomous: true
gap_closure: true
must_haves:
  truths:
    - Concurrent default-terminal bootstrap requests resolve to one stable terminal identity.
    - Attach returns a non-null stream id and terminal input routes to the active terminal reliably.
    - Running terminal commands from the web client produces live output in headless regression coverage.
  artifacts:
    - path: packages/web/src/App.tsx
      provides: single-flight ensure flow with requestId-correlated ensure response handling.
    - path: packages/server/src/terminal/terminal-manager.ts
      provides: single-flight ensureDefaultTerminal guard preventing concurrent terminal churn.
    - path: packages/server/src/server/session.ts
      provides: stable attach/input lookup behavior when terminal identity is stale during bootstrap races.
    - path: packages/server/src/server/daemon-e2e/terminal.e2e.test.ts
      provides: regression test for concurrent ensure + attach + input with non-null stream continuity.
  key_links:
    - from: packages/web/src/App.tsx
      to: packages/server/src/server/session.ts
      via: requestId-correlated ensure response before attach/input flow.
      pattern: ensure_default_terminal_request|requestId|attach_terminal_stream_request
    - from: packages/server/src/terminal/terminal-manager.ts
      to: packages/server/src/server/session.ts
      via: shared single-flight default terminal resolution for ensure/attach/input paths.
      pattern: ensureDefaultTerminal
    - from: packages/server/src/server/daemon-e2e/terminal.e2e.test.ts
      to: packages/web/src/App.tsx
      via: race regression asserting streamId presence and command output continuity.
      pattern: concurrent|ensure|attach|terminal_input
---

<objective>
Close the diagnosed terminal stream bootstrap race so default terminal attach/input stays stable under concurrent ensure requests.

Purpose: unblock UAT Test 2 by eliminating terminal id churn that causes streamId=null and Terminal not found warnings.
Output: race-safe client/server ensure flow plus regression coverage proving non-null stream attach and live command output.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02-terminal-i/02-terminal-i-UAT.md
@.planning/phases/02-terminal-i/02-terminal-i-03-SUMMARY.md
@.planning/debug/phase-02-uat-streamid-null.md
@packages/web/src/App.tsx
@packages/server/src/terminal/terminal-manager.ts
@packages/server/src/server/session.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add client ensure single-flight with requestId-correlated response handling</name>
  <files>packages/web/src/App.tsx</files>
  <action>Refactor default terminal bootstrap so only one ensure_default_terminal request is in flight at a time. Include a requestId on ensure requests and ignore late/out-of-order ensure responses that do not match the active request. Do not mutate current terminal identity from stale responses; only proceed to attach/input setup after the correlated response is accepted.</action>
  <verify>npm run typecheck --workspace=@oisin/web</verify>
  <done>Client no longer churns terminalId from overlapping ensure responses, and attach flow always uses the latest correlated default terminal identity.</done>
</task>

<task type="auto">
  <name>Task 2: Add server single-flight ensure lock and stale-id attach/input hardening</name>
  <files>packages/server/src/terminal/terminal-manager.ts, packages/server/src/server/session.ts</files>
  <action>Implement a single-flight guard in ensureDefaultTerminal so concurrent callers share one in-flight terminal creation/reuse result. In session attach/input handlers, when a stale terminal id misses lookup during bootstrap races, resolve against the stable default terminal identity path instead of returning null stream routing. Preserve existing behavior for truly unknown non-default terminal ids.</action>
  <verify>npm run typecheck --workspace=@getpaseo/server</verify>
  <done>Concurrent ensure calls resolve to one stable terminal identity and attach/input no longer fail with streamId=null or Terminal not found for this diagnosed race.</done>
</task>

<task type="auto">
  <name>Task 3: Lock regression for concurrent ensure + attach + input reliability</name>
  <files>packages/server/src/server/daemon-e2e/terminal.e2e.test.ts</files>
  <action>Add a focused regression test that triggers concurrent ensure_default_terminal calls, then performs attach and terminal_input through the resulting identity. Assert attach returns non-null streamId, input is accepted, and command output (for example `echo`) is observed on the stream without terminal lookup warnings.</action>
  <verify>npm run test --workspace=@getpaseo/server -- terminal.e2e</verify>
  <done>Automated coverage reproduces the original race and proves stable stream attach/input behavior under concurrent ensure conditions.</done>
</task>

</tasks>

<verification>
UAT blocker is closed when concurrent default terminal bootstrap no longer produces streamId=null, command input routes reliably, and live output is observed.
</verification>

<success_criteria>
Phase 02 gap truth passes: running commands from the embedded terminal reliably returns live output, including in headless regression conditions that previously reproduced the bootstrap race.
</success_criteria>

<output>
After completion, create `.planning/phases/02-terminal-i/02-terminal-i-04-SUMMARY.md`
</output>
