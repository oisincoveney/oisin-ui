---
phase: 02-terminal-i
plan: 07
type: execute
wave: 7
depends_on:
  - "02-06"
files_modified:
  - packages/server/src/server/daemon-e2e/terminal.e2e.test.ts
autonomous: true
gap_closure: true
must_haves:
  truths:
    - Rapid reconnect/refresh cycles keep terminal input routable after each new attach.
    - Reconnect plus active resize still preserves output continuity and valid input routing.
    - Automated regressions fail if stale stream ids survive into post-reconnect typing.
  artifacts:
    - path: packages/server/src/server/daemon-e2e/terminal.e2e.test.ts
      provides: reconnect/refresh stream-id rollover regression and combined reconnect+resize continuity regression.
  key_links:
    - from: packages/server/src/server/daemon-e2e/terminal.e2e.test.ts
      to: packages/server/src/server/session.ts
      via: repeated attach/input assertions validate latest stream identity routing.
      pattern: attach|streamId|terminal_input|Terminal stream not found
    - from: packages/server/src/server/daemon-e2e/terminal.e2e.test.ts
      to: packages/web/src/App.tsx
      via: reconnect/resize scenario exercises client attach gating contract end-to-end.
      pattern: reconnect|resize|output continuity
---

<objective>
Close the remaining UAT blockers by adding deterministic regressions for stale stream-id rollover and reconnect+resize continuity.

Purpose: prevent future regressions where attach appears successful but input still targets stale stream ids.
Output: e2e reliability tests that reproduce rapid reconnect/refresh churn and enforce latest-stream input routing with resize continuity.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02-terminal-i/02-terminal-i-UAT.md
@.planning/phases/02-terminal-i/02-terminal-i-03-SUMMARY.md
@.planning/phases/02-terminal-i/02-terminal-i-04-SUMMARY.md
@.planning/phases/02-terminal-i/02-terminal-i-06-SUMMARY.md
@packages/server/src/server/daemon-e2e/terminal.e2e.test.ts
@packages/server/src/server/session.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add rapid reconnect/refresh stream-id rollover regression</name>
  <files>packages/server/src/server/daemon-e2e/terminal.e2e.test.ts</files>
  <action>Add a focused reliability test that performs repeated ensure+attach cycles under simulated reconnect/refresh churn, then sends terminal input after each attach. Assert each input targets the most recent stream id, output remains observable, and stale-stream warnings are absent (or explicitly asserted as zero occurrences in captured logs/events for this path).</action>
  <verify>npm run test --workspace=@getpaseo/server -- terminal.e2e --testNamePattern="reconnect.*stream|refresh.*stream"</verify>
  <done>Regression fails when stale stream ids survive reconnect/refresh and passes only when latest-stream routing is enforced after every attach.</done>
</task>

<task type="auto">
  <name>Task 2: Add combined reconnect plus resize continuity regression</name>
  <files>packages/server/src/server/daemon-e2e/terminal.e2e.test.ts</files>
  <action>Extend terminal reliability coverage with a scenario that streams output, forces reconnect/reattach, applies resize churn, then validates both continued ordered output and valid post-reconnect input routing. Reuse existing resize continuity assertions but explicitly include reconnect attach rollover in the same test path to match UAT failure conditions.</action>
  <verify>npm run test --workspace=@getpaseo/server -- terminal.e2e --testNamePattern="reconnect.*resize|resize.*reconnect"</verify>
  <done>Automated coverage now proves resize stability is trustworthy after reconnect because stream identity remains valid and output/input continue correctly.</done>
</task>

<task type="auto">
  <name>Task 3: Run full terminal daemon e2e suite as reliability gate</name>
  <files>packages/server/src/server/daemon-e2e/terminal.e2e.test.ts</files>
  <action>Execute the full terminal e2e test file after adding regressions to confirm no collateral failures across existing ensure, replay/reset, resize, and attach routing coverage. Keep tests deterministic by reusing existing harness timing/await patterns rather than adding blind sleeps.</action>
  <verify>npm run test --workspace=@getpaseo/server -- terminal.e2e</verify>
  <done>Entire terminal daemon e2e suite passes with new regressions included, establishing a durable gate for this stale stream-id UAT gap.</done>
</task>

</tasks>

<verification>
Gaps are closed when e2e tests reliably reproduce reconnect/refresh and reconnect+resize churn and prove latest-stream input routing with uninterrupted output continuity.
</verification>

<success_criteria>
UAT tests 5, 6, and 7 are now guarded by automated regressions: reconnect catch-up remains interactive, resize validation is unblocked and stable, and rapid reconnect/refresh no longer leaves stale stream input paths.
</success_criteria>

<output>
After completion, create `.planning/phases/02-terminal-i/02-terminal-i-07-SUMMARY.md`
</output>
