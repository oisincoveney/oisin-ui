---
phase: 02-terminal-i
plan: 06
type: execute
wave: 6
depends_on:
  - "02-05"
files_modified:
  - packages/web/src/App.tsx
  - packages/web/src/terminal/terminal-stream.ts
  - packages/server/src/server/session.ts
autonomous: true
gap_closure: true
must_haves:
  truths:
    - After reconnect or refresh churn, terminal input is blocked until the latest attach succeeds for the current stream identity.
    - Once reattached, new terminal input routes to the latest stream id instead of a stale pre-reconnect stream id.
    - Repeated reconnect cycles do not leave the client in an attached-looking state with unroutable input.
  artifacts:
    - path: packages/web/src/App.tsx
      provides: reconnect/attach state machine that invalidates old stream identity before accepting post-reconnect input.
    - path: packages/web/src/terminal/terminal-stream.ts
      provides: stream-bound input gating and hard reset behavior when stream identity rolls over.
    - path: packages/server/src/server/session.ts
      provides: explicit stale-stream input rejection path aligned to latest attach lifecycle.
  key_links:
    - from: packages/web/src/App.tsx
      to: packages/web/src/terminal/terminal-stream.ts
      via: attach-confirmed stream id handoff controls input enablement.
      pattern: attach|streamId|setInputEnabled|reset
    - from: packages/web/src/App.tsx
      to: packages/server/src/server/session.ts
      via: reconnect attach response sets active stream id before any terminal_input send.
      pattern: attach_terminal_stream_response|terminal_input
    - from: packages/server/src/server/session.ts
      to: packages/web/src/terminal/terminal-stream.ts
      via: stale stream input is rejected and does not route to terminal runtime.
      pattern: Terminal stream not found for input|streamId
---

<objective>
Close the reconnect stream-id drift gaps by hard-resetting stream identity during reconnect and gating input until the latest attach is confirmed.

Purpose: stop stale streamId terminal_input frames that produce "Terminal stream not found for input" after reconnect/refresh churn.
Output: reconnect-safe attach lifecycle with deterministic stream invalidation and post-attach input routing correctness.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02-terminal-i/02-terminal-i-UAT.md
@.planning/phases/02-terminal-i/02-terminal-i-04-SUMMARY.md
@.planning/phases/02-terminal-i/02-terminal-i-05-SUMMARY.md
@packages/web/src/App.tsx
@packages/web/src/terminal/terminal-stream.ts
@packages/server/src/server/session.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Invalidate stream identity immediately on disconnect/reconnect transitions</name>
  <files>packages/web/src/App.tsx</files>
  <action>Refactor reconnect/attach orchestration so any socket close/error/reconnect transition hard-resets the active stream identity before attach retries begin. Keep terminal terminalId tracking, but clear stream-bound sendability state and require a fresh attach response before input is eligible. Prevent stale async attach completions from restoring superseded stream state by correlating attach attempts to the current reconnect cycle.</action>
  <verify>npm run typecheck --workspace=@oisin/web</verify>
  <done>During reconnect churn, client cannot send terminal input on a pre-reconnect stream id; stream identity remains null/invalid until the newest attach response is accepted.</done>
</task>

<task type="auto">
  <name>Task 2: Gate terminal input in stream adapter on active attached stream confirmation</name>
  <files>packages/web/src/terminal/terminal-stream.ts, packages/web/src/App.tsx</files>
  <action>Strengthen terminal-stream input send path so it only emits terminal_input for the currently active, attach-confirmed stream id. Add explicit adapter methods for stream rollover/reset and wire App to call them before reconnect attach and after successful attach. Do not queue old keystrokes while detached; preserve current drop-on-disconnect behavior. Ensure replay/ack handling still works after stream rollover.</action>
  <verify>npm run typecheck --workspace=@oisin/web</verify>
  <done>Input frames are never emitted with stale stream ids across reconnect/refresh races, while normal post-attach typing continues to work.</done>
</task>

<task type="auto">
  <name>Task 3: Align server stale-stream rejection with reconnect attach lifecycle diagnostics</name>
  <files>packages/server/src/server/session.ts</files>
  <action>Update terminal input handling to make stale stream-id rejection deterministic and lifecycle-aware during rapid reattach. Preserve fail-fast behavior for unknown stream ids, but ensure rejection path reflects latest attach state and does not accidentally route input to an invalid stream. Keep existing warning signal for observability and avoid introducing permissive fallback routing that hides identity drift bugs.</action>
  <verify>npm run typecheck --workspace=@getpaseo/server</verify>
  <done>Server consistently rejects stale stream-id input after stream rollover, matching client attach state expectations and preventing false-success input routing.</done>
</task>

</tasks>

<verification>
Gap is closed when reconnect or refresh churn cannot produce stale-stream terminal_input sends, and server/client attach lifecycle stays synchronized on one active stream identity.
</verification>

<success_criteria>
UAT truths 5 and 7 become achievable: after reconnect, input routes through the latest attached stream id only, with no recurring stale stream-not-found warnings from post-attach typing.
</success_criteria>

<output>
After completion, create `.planning/phases/02-terminal-i/02-terminal-i-06-SUMMARY.md`
</output>
