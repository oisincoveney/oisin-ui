---
phase: 08-deterministic-verification-closure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/server/e2e/thread-management-web.spec.ts
autonomous: true

must_haves:
  truths:
    - "A test explicitly covers create thread A -> create thread B -> click sidebar to switch to A -> delete A -> see No active thread"
    - "The switch is performed by clicking the sidebar row (not keyboard shortcut)"
    - "Deletion is confirmed via alertdialog"
    - "After deletion, No active thread is visible and thread A row is gone"
    - "bun run --filter @getpaseo/server test:e2e passes with the new test included"
  artifacts:
    - path: "packages/server/e2e/thread-management-web.spec.ts"
      provides: "create->switch->delete test"
      contains: "click-switch"
  key_links:
    - from: "createThreadViaUi"
      to: "sidebar row click"
      via: "locator([data-sidebar='menu-button']).click()"
      pattern: "activeCandidateRow\\.click"
    - from: "sidebar row click"
      to: "alertdialog Delete Thread"
      via: "hover + delete button + confirm"
      pattern: "alertdialog.*Delete Thread"
---

<objective>
Add a create -> click-switch -> delete test to thread-management-web.spec.ts covering the full VER-02 flow.

Purpose: VER-02 — thread management browser regression must deterministically validate create -> switch -> delete with fixture setup.
Output: New test in packages/server/e2e/thread-management-web.spec.ts that passes without skips.
</objective>

<execution_context>
@~/.config/Claude/get-shit-done/workflows/execute-plan.md
@~/.config/Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/server/e2e/thread-management-web.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add create->click-switch->delete test to thread-management-web.spec.ts</name>
  <files>packages/server/e2e/thread-management-web.spec.ts</files>
  <action>
Append a new test at the end of packages/server/e2e/thread-management-web.spec.ts (after the "create thread exits pending immediately with disconnected error when websocket is offline" test):

```
test("create->click-switch->delete removes active thread and shows no active thread", async ({ page }) => {
  const threadATitle = `thread-switch-delete-a-${Date.now()}`;
  const threadBTitle = `thread-switch-delete-b-${Date.now()}`;

  await page.goto(runtime.webUrl);

  // Create thread A — becomes active
  await createThreadViaUi(page, threadATitle);
  await expect(
    page.locator("[data-sidebar='menu-button'][data-active='true']", { hasText: threadATitle }),
  ).toBeVisible();

  // Create thread B — becomes active
  await createThreadViaUi(page, threadBTitle);
  await expect(
    page.locator("[data-sidebar='menu-button'][data-active='true']", { hasText: threadBTitle }),
  ).toBeVisible();

  // Click sidebar row to switch back to thread A
  const threadARow = page.locator("[data-sidebar='menu-button']", { hasText: threadATitle }).first();
  await expect(threadARow).toBeVisible();
  await threadARow.click();
  await expect(
    page.locator("[data-sidebar='menu-button'][data-active='true']", { hasText: threadATitle }),
  ).toBeVisible();

  // Hover thread A item to reveal delete button
  const threadAItem = threadARow.locator("xpath=ancestor::*[@data-sidebar='menu-item'][1]");
  await threadAItem.hover();

  // Delete thread A
  await page.getByRole("button", { name: `Delete ${threadATitle}` }).click();
  await expect(page.getByRole("alertdialog", { name: "Delete Thread" })).toBeVisible();
  await page.getByRole("button", { name: "Delete Thread" }).click();

  // Verify no active thread state
  await expect(page.getByText("No active thread")).toBeVisible({ timeout: 2_000 });
  await expect(
    page.locator("[data-sidebar='menu-button'][data-active='true']", { hasText: threadATitle }),
  ).toHaveCount(0);
  await expect(threadAItem).toHaveCount(0);
  await expect(page.getByText(/Reattaching terminal \(attempt \d+\)/)).toHaveCount(0);
});
```

Insert this test verbatim. Do not modify any existing tests.
  </action>
  <verify>npx tsc --noEmit --project packages/server/tsconfig.json 2>&1 | head -20</verify>
  <done>File contains the new test with "create->click-switch->delete" in the test name. No TypeScript errors.</done>
</task>

<task type="auto">
  <name>Task 2: Run server e2e suite to confirm new test passes</name>
  <files></files>
  <action>
Run: bun run --filter @getpaseo/server test:e2e

All tests in packages/server/e2e/ must pass including the new "create->click-switch->delete removes active thread and shows no active thread" test. No skipped tests. Exit code 0.

If the test fails because thread A's sidebar row is not found after thread B creation (sidebar may scroll or reorder), add an explicit scroll-into-view before clicking:
  await threadARow.scrollIntoViewIfNeeded();
  await threadARow.click();

Do NOT add any conditional skip or test.fixme.
  </action>
  <verify>bun run --filter @getpaseo/server test:e2e 2>&1 | tail -20</verify>
  <done>All server e2e tests pass. New test "create->click-switch->delete" is listed as passed. Exit code 0.</done>
</task>

</tasks>

<verification>
- packages/server/e2e/thread-management-web.spec.ts contains test "create->click-switch->delete removes active thread and shows no active thread"
- bun run --filter @getpaseo/server test:e2e exits 0 with all tests passing including the new one
- VER-03 satisfied: the single command sequence `bun run --filter @getpaseo/server typecheck && bun run --filter @oisin/web typecheck && bun run --filter @getpaseo/server test && bun run --filter @getpaseo/server test:e2e` passes end-to-end
</verification>

<success_criteria>
VER-02 satisfied: thread management browser regression deterministically validates create -> switch -> delete fixture flow.
VER-03 satisfied: the full verification command chain passes (typecheck + test + test:e2e).
</success_criteria>

<output>
After completion, create `.planning/phases/08-deterministic-verification-closure/08-02-SUMMARY.md`
</output>
