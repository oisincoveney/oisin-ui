---
phase: 08-deterministic-verification-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/e2e/diff-panel.spec.ts
  - packages/server/e2e/diff-panel.spec.ts
autonomous: true

must_haves:
  truths:
    - "diff-panel spec runs without any conditional skip path"
    - "diff-panel spec creates its own isolated runtime with a UI-created active thread"
    - "diff-panel spec cleans up its runtime after all tests complete"
    - "bun run --filter @getpaseo/server test:e2e includes diff-panel.spec.ts and passes"
  artifacts:
    - path: "packages/server/e2e/diff-panel.spec.ts"
      provides: "Isolated-runtime diff-panel regression"
      contains: "startRuntime"
    - path: "packages/web/e2e/diff-panel.spec.ts"
      provides: "Deleted (migrated)"
  key_links:
    - from: "packages/server/e2e/diff-panel.spec.ts"
      to: "createThreadViaUi"
      via: "beforeAll fixture"
      pattern: "createThreadViaUi"
    - from: "packages/server/e2e/diff-panel.spec.ts"
      to: "runtime.webUrl"
      via: "page.goto"
      pattern: "page\\.goto\\(runtime\\.webUrl\\)"
---

<objective>
Migrate diff-panel.spec.ts from packages/web/e2e/ (ambient, conditional-skip) to packages/server/e2e/ (isolated runtime, no skip path).

Purpose: VER-01 — diff-panel regression must run deterministically against a fixture-owned active thread.
Output: packages/server/e2e/diff-panel.spec.ts with isolated startRuntime + createThreadViaUi fixture; packages/web/e2e/diff-panel.spec.ts deleted.
</objective>

<execution_context>
@~/.config/Claude/get-shit-done/workflows/execute-plan.md
@~/.config/Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/server/e2e/thread-management-web.spec.ts
@packages/web/e2e/diff-panel.spec.ts
@packages/server/playwright.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create packages/server/e2e/diff-panel.spec.ts with isolated runtime fixture</name>
  <files>packages/server/e2e/diff-panel.spec.ts</files>
  <action>
Create a new file packages/server/e2e/diff-panel.spec.ts. Model it on thread-management-web.spec.ts — copy the full runtime infrastructure verbatim (getAvailablePort, waitForHttpOk, spawnProcess, stopProcess, createGitRepo, writePersistedConfig, startRuntime, Runtime type). Adapt createGitRepo to use prefix "diff-panel-repo-" and writePersistedConfig to use projectId "diff-panel-project". Use paseoHomeRoot prefix "diff-panel-runtime-".

In beforeAll: call startRuntime(), instantiate DaemonClient (same pattern as thread-management-web), connect + fetchAgents, then call createThreadViaUi(page, "diff-panel-active-thread") — but createThreadViaUi needs a page. Since beforeAll doesn't receive a page, instead use the test fixture approach: create the active thread inside the single test using createThreadViaUi before opening the diff panel.

Copy createThreadViaUi verbatim from thread-management-web.spec.ts (it takes page + title, clicks "Create new thread", fills Thread Name + Base Branch "main", clicks "Create Thread", awaits dialog gone + sidebar entry visible + no "No active thread" text + no reattach text, asserts latency < 10s).

Copy runTerminalCommand from the old diff-panel.spec.ts (locates .xterm-helper-textarea, focuses, types command, presses Enter).

Write a single test "renders collapsed metadata rows, refreshes from terminal edits, and stays read-only" inside test.describe("diff panel regressions"):
1. page.goto(runtime.webUrl)
2. createThreadViaUi(page, "diff-panel-active-thread") — this creates the active thread; after this the "Open diff panel" button MUST be enabled (no conditional skip)
3. const openPanelButton = page.getByRole('button', { name: 'Open diff panel' })
4. await expect(openPanelButton).toBeEnabled() — hard assertion, no skip
5. await openPanelButton.click()
6. Continue with the exact same test body as the original diff-panel.spec.ts from line 26 onward (panel visible, runTerminalCommand to create E2E_HIGHLIGHT_FILE, refresh, check highlighted row, expand section, verify hljs classes, rename flow, cleanup). Use the same constants: E2E_HIGHLIGHT_FILE = '.paseo-diff-panel-e2e.ts', RENAME_SOURCE_FILE = '.planning/PROJECT.md', RENAME_TARGET_FILE = '.planning/PROJECT.diff-panel-rename-e2e.md'.

IMPORTANT: Do NOT include any `if (!(await openPanelButton.isEnabled())) { test.skip(...) }` block. The fixture guarantees the active thread exists.

In afterAll: close DaemonClient, stopProcess(runtime.web), stopProcess(runtime.daemon), rm repoPath + paseoHomeRoot (same pattern as thread-management-web afterAll).

The file must import: expect, test, type Page from "@playwright/test"; net, os, path, existsSync, mkdir, mkdtemp, rm, writeFile, execSync, spawn, ChildProcess, fileURLToPath — same as thread-management-web. Also import DaemonClient from "../src/server/test-utils/index.js".
  </action>
  <verify>npx tsc --noEmit --project packages/server/tsconfig.json 2>&1 | head -20</verify>
  <done>File exists at packages/server/e2e/diff-panel.spec.ts with no TypeScript errors, contains "startRuntime", "createThreadViaUi", no "test.skip" string anywhere in the file.</done>
</task>

<task type="auto">
  <name>Task 2: Delete packages/web/e2e/diff-panel.spec.ts</name>
  <files>packages/web/e2e/diff-panel.spec.ts</files>
  <action>
Delete the file packages/web/e2e/diff-panel.spec.ts. It has been fully migrated to packages/server/e2e/diff-panel.spec.ts. Use the filesystem delete tool or bash rm.

After deletion, verify no other file in packages/web/ imports or references diff-panel.spec.ts.
  </action>
  <verify>ls packages/web/e2e/diff-panel.spec.ts 2>&1; echo "exit: $?"</verify>
  <done>packages/web/e2e/diff-panel.spec.ts does not exist (ls returns non-zero exit).</done>
</task>

<task type="auto">
  <name>Task 3: Run server e2e suite to confirm diff-panel passes</name>
  <files></files>
  <action>
Run: bun run --filter @getpaseo/server test:e2e

This executes playwright against packages/server/e2e/ which now includes diff-panel.spec.ts alongside thread-management-web.spec.ts and terminal-web-smoke.spec.ts. All three must pass.

If diff-panel.spec.ts fails because "Open diff panel" button is disabled after createThreadViaUi, investigate: the button may need the terminal to be attached (not just the thread created). If so, add a waitFor for the terminal textarea to be visible before clicking "Open diff panel":
  await expect(page.locator('.xterm-helper-textarea').first()).toBeVisible({ timeout: 30_000 })
Then retry.

Do NOT add any conditional skip. Fix the fixture instead.
  </action>
  <verify>bun run --filter @getpaseo/server test:e2e 2>&1 | tail -20</verify>
  <done>All server e2e tests pass including diff-panel.spec.ts. No skipped tests. Exit code 0.</done>
</task>

</tasks>

<verification>
- packages/server/e2e/diff-panel.spec.ts exists, contains "startRuntime", "createThreadViaUi", no "test.skip"
- packages/web/e2e/diff-panel.spec.ts does not exist
- bun run --filter @getpaseo/server test:e2e exits 0 with all tests passing
</verification>

<success_criteria>
VER-01 satisfied: diff-panel browser regression runs against a deterministic active-thread fixture with no conditional skip path.
</success_criteria>

<output>
After completion, create `.planning/phases/08-deterministic-verification-closure/08-01-SUMMARY.md`
</output>
