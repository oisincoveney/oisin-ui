---
phase: 01-foundation-and-docker
plan: 05
type: execute
wave: 3
depends_on: ["02", "03", "04"]
files_modified:
  - "packages/web/src/App.tsx"
  - "packages/web/src/lib/ws.ts"
  - "packages/web/src/components/ConnectionOverlay.tsx"
autonomous: false
must_haves:
  truths:
    - "User opens the web UI and sees a connected status indicator"
    - "If connection drops, a Disable Input overlay appears"
  artifacts:
    - path: "packages/web/src/lib/ws.ts"
      provides: "WebSocket connection client"
    - path: "packages/web/src/components/ConnectionOverlay.tsx"
      provides: "UI states for connection"
  key_links:
    - from: "packages/web/src/lib/ws.ts"
      to: "ws://localhost:3000"
      via: "WebSocket API"
      pattern: "new WebSocket"
---

<objective>
Wire up the client-daemon connection flow, implementing robust WebSocket logic in the Web UI.

Purpose: The UI must maintain a resilient connection to the daemon to power the terminal and agents.
Output: Effect-based WebSocket client, connection status UI, and a loading/disconnect overlay.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Effect-based WebSocket Client</name>
  <files>packages/web/src/lib/ws.ts</files>
  <action>
    Implement a WebSocket client in `packages/web` using standard WebSocket API, wrapped or managed by Effect TS.
    Explicitly connect to `ws://localhost:3000`.
    Implement exponential backoff with infinite retries on connection failure.
    Handle the server's ping messages by automatically sending a pong response.
    Expose a reactive store or context (e.g., using Effect/React hooks) for the connection status (`connecting`, `connected`, `disconnected`).
  </action>
  <verify>Ensure code compiles cleanly with strict TS checks.</verify>
  <done>WS client is robust and handles reconnections and heartbeats.</done>
</task>

<task type="auto">
  <name>Task 2: Connection Status UI</name>
  <files>packages/web/src/App.tsx, packages/web/src/components/ConnectionOverlay.tsx</files>
  <action>
    Create a `ConnectionOverlay` component.
    - If status is `connecting` (initial load): Show a fullscreen spinner.
    - If status is `disconnected` (connection dropped): Show a translucent "Disable input - Reconnecting..." overlay to prevent commands.
    Add a small persistent connection status indicator (green dot for connected, red for disconnected) in the main layout (`App.tsx`).
  </action>
  <verify>Run `npm run build --workspace=packages/web` to check for UI errors.</verify>
  <done>UI correctly responds to connection state changes.</done>
</task>

<task type="checkpoint:human-verify">
  <what-built>Full stack Docker environment and Web UI connection resilience.</what-built>
  <how-to-verify>
    1. Run `docker-compose up --build`.
    2. Open http://localhost:5173 in the browser.
    3. Verify you see the fullscreen spinner briefly, then it disappears and shows a "Connected" status indicator.
    4. Kill the server process manually (or stop the container), and verify the "Disable input - Reconnecting..." overlay appears.
    5. Restart the server and verify the UI reconnects automatically.
  </how-to-verify>
  <resume-signal>Type "approved" if the connection flow works as expected.</resume-signal>
</task>

</tasks>

<verification>
Human verify checkpoint handles the final end-to-end test.
</verification>

<success_criteria>
The Web UI reliably connects to the Daemon via WebSocket, recovering from disconnects automatically.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-docker/01-05-SUMMARY.md`
</output>