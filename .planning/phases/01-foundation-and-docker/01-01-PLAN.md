---
phase: 01-foundation-and-docker
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "package.json"
  - "README.md"
autonomous: true
must_haves:
  truths:
    - "Workspace contains only necessary backend/cli code"
    - "Root config resolves workspaces correctly without missing directories"
  artifacts:
    - path: "package.json"
      provides: "Root workspace definition"
      contains: "packages/server"
  key_links:
    - from: "package.json"
      to: "packages/server"
      via: "workspaces array"
      pattern: "packages/server"
---

<objective>
Bootstrap the Oisin UI project by copying the Paseo base, stripping unused clients, and configuring the base workspace.

Purpose: We are converting a multi-client codebase (mobile, desktop) into a pure web-client architecture, so we must shed the unused weight.
Output: A clean monorepo with `server` and `cli` packages remaining, and an updated `package.json`.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy Base Project Source</name>
  <files>None (copies to root)</files>
  <action>
    Use `rsync` or similar via the bash tool to copy the contents of `~/dev/old-oisin-ui/` into the current directory (`/Users/oisin/dev/oisin-ui`). 
    Exclude `node_modules`, `.git`, `.tmp`, and `.logs`.
  </action>
  <verify>Run `ls -la packages/` to confirm files are copied.</verify>
  <done>Base Paseo files exist in the current directory.</done>
</task>

<task type="auto">
  <name>Task 2: Strip Unused Packages</name>
  <files>package.json</files>
  <action>
    Delete the directories: `packages/app`, `packages/desktop`, `packages/website`, and `packages/relay`.
    Update the root `package.json` to remove these packages from the `workspaces` array.
    Remove any npm scripts in root `package.json` that reference these deleted workspaces (e.g., `dev:app`, `android`, `ios`, `dev:desktop`, etc.).
  </action>
  <verify>Run `npm install` to ensure workspace installs successfully without missing directories.</verify>
  <done>Only `packages/server` and `packages/cli` remain in the workspaces array and on disk.</done>
</task>

<task type="auto">
  <name>Task 3: Clean Root Files</name>
  <files>README.md, docs/</files>
  <action>
    Delete the `docs/` directory.
    Rewrite `README.md` to briefly describe "Oisin UI: A self-hosted web terminal for AI coding agents". Remove the old Paseo readme content.
  </action>
  <verify>Run `cat README.md` to check content.</verify>
  <done>README.md reflects the new project identity and docs are removed.</done>
</task>

</tasks>

<verification>
Ensure `npm install` runs cleanly without complaining about missing packages. Check that `packages/app` is gone.
</verification>

<success_criteria>
The workspace contains only `server` and `cli`, builds cleanly, and has a correct root `package.json`.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-docker/01-01-SUMMARY.md`
</output>