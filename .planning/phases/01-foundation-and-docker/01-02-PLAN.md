---
phase: 01-foundation-and-docker
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - "packages/server/src/index.ts"
  - "packages/server/package.json"
autonomous: true
must_haves:
  truths:
    - "Daemon exposes an Express + WS server"
    - "Daemon handles ping/pong heartbeats to keep connections alive"
  artifacts:
    - path: "packages/server/src/index.ts"
      provides: "Daemon entry point"
      contains: "app.listen"
  key_links:
    - from: "packages/server/src/index.ts"
      to: "WebSocket clients"
      via: "ws ping/pong mechanism"
      pattern: "ws.ping|ws.pong"
---

<objective>
Refactor the daemon (`packages/server`) to act as a pure Express + WebSocket backend, stripping mobile/relay-specific endpoints while keeping core managers and voice infrastructure.

Purpose: Simplifies the server to serve only our new Web UI via WebSockets.
Output: A streamlined `index.ts` in the server package with a heartbeat mechanism.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Simplify Express Server</name>
  <files>packages/server/src/index.ts</files>
  <action>
    Refactor `index.ts` to set up a basic Express app with a raw HTTP server, and attach a `ws` WebSocket server to it.
    Configure it to run on port 3000 (or `process.env.PORT`).
    Remove endpoints explicitly related to Paseo's mobile app (if any, like `/auth/mobile`, relay webhooks).
    Keep the initialization of core managers and voice/STT systems.
  </action>
  <verify>Run `npm run typecheck --workspace=packages/server`.</verify>
  <done>Express and WS server initialized correctly on the specified port.</done>
</task>

<task type="auto">
  <name>Task 2: Add WebSocket Ping/Pong Heartbeat</name>
  <files>packages/server/src/index.ts</files>
  <action>
    Implement a 30s interval ping/pong heartbeat in the WebSocket connection handler.
    The server should send a `ping` to all connected clients every 30 seconds.
    If a client does not respond with a `pong` before the next interval, terminate the connection to prevent memory leaks.
  </action>
  <verify>Check source code visually for `setInterval` and `ws.terminate()` logic based on `isAlive` flag.</verify>
  <done>Heartbeat mechanism is fully implemented in the WS handler.</done>
</task>

</tasks>

<verification>
Start the server and confirm it listens on port 3000 and doesn't crash.
</verification>

<success_criteria>
Daemon runs a lean Express + WS server with ping/pong lifecycle management.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-docker/01-02-SUMMARY.md`
</output>