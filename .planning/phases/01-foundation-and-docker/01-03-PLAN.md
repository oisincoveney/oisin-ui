---
phase: 01-foundation-and-docker
plan: 03
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - "package.json"
  - "packages/web/package.json"
  - "packages/web/vite.config.ts"
  - "packages/web/tailwind.config.js"
autonomous: true
must_haves:
  truths:
    - "Web application is scaffolded using Vite and React 19"
    - "Styling is configured using Tailwind and ShadCN"
  artifacts:
    - path: "packages/web/package.json"
      provides: "Web app dependencies"
      contains: "vite"
    - path: "packages/web/vite.config.ts"
      provides: "Vite build config"
  key_links:
    - from: "package.json"
      to: "packages/web"
      via: "workspaces array"
      pattern: "packages/web"
---

<objective>
Scaffold the new Vite + React 19 web client to replace the old Expo application.

Purpose: We need a modern, fast, web-only SPA to host our terminal and UI.
Output: A new `packages/web` workspace with Vite, Tailwind, ShadCN, and Effect TS.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Vite + React Workspace</name>
  <files>packages/web/*, package.json</files>
  <action>
    Use `npm create vite@latest packages/web -- --template react-ts` (or similar) to generate the web package.
    Update `packages/web/package.json` name to `@oisin/web` or similar.
    Add `packages/web` to the root `package.json` workspaces.
    Install `bun` types, `oxc` for linting/compilation if required (or standard Vite config with strict TS).
    Update React to version 19 if not already.
  </action>
  <verify>Run `npm run build --workspace=packages/web`.</verify>
  <done>Vite app builds successfully within the monorepo.</done>
</task>

<task type="auto">
  <name>Task 2: Configure Tailwind and ShadCN</name>
  <files>packages/web/tailwind.config.js, packages/web/src/index.css, packages/web/components.json</files>
  <action>
    Install `tailwindcss`, `postcss`, `autoprefixer` in `packages/web`.
    Initialize Tailwind config with a dark theme skeleton.
    Initialize `shadcn-ui` (using `npx shadcn@latest init` or manually setting up `components.json` and base utils).
    Ensure the `src/index.css` imports Tailwind directives.
  </action>
  <verify>Check `packages/web/tailwind.config.js` exists and base styles are in `index.css`.</verify>
  <done>Tailwind and ShadCN foundation is ready for UI components.</done>
</task>

<task type="auto">
  <name>Task 3: Add Effect TS</name>
  <files>packages/web/package.json</files>
  <action>
    Install `effect` and `@effect/schema` in `packages/web`.
    This establishes the baseline dependencies for the networking and state architecture defined in the project decisions.
  </action>
  <verify>Run `npm install` and check `packages/web/package.json` for `effect`.</verify>
  <done>Effect TS dependencies are installed.</done>
</task>

</tasks>

<verification>
Run the Vite dev server briefly to ensure it starts without errors.
</verification>

<success_criteria>
`packages/web` is a functional React SPA workspace with Tailwind, ShadCN, and Effect TS installed.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-docker/01-03-SUMMARY.md`
</output>