---
phase: 01-foundation-and-docker
plan: 06
type: execute
wave: 1
depends_on: []
files_modified: ["packages/web/src/lib/ws.ts"]
autonomous: true
gap_closure: true
must_haves:
  truths:
    - "User can run `docker-compose up` and access the web UI via a network IP, successfully establishing a daemon connection."
    - "User opens web UI and sees a connected status indicator, even when not using localhost."
  artifacts:
    - path: "packages/web/src/lib/ws.ts"
      provides: "Dynamic WebSocket URL based on browser origin"
  key_links:
    - from: "packages/web/src/lib/ws.ts"
      to: "daemon server"
      via: "dynamic hostname derivation"
---

<objective>
Fix hardcoded WebSocket URL to support connections from non-localhost network interfaces.

Purpose: The web UI was hardcoded to connect to `localhost`, breaking when the container is accessed from other IP addresses or hostnames.
Output: Dynamic WebSocket connection URL derivation logic in the web client.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/web/src/lib/ws.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make WebSocket URL Dynamic</name>
  <files>packages/web/src/lib/ws.ts</files>
  <action>
    Modify the WebSocket URL derivation in `packages/web/src/lib/ws.ts`:
    - Remove the hardcoded `const WS_URL = 'ws://localhost:3000/ws?clientSessionKey=web-client'`
    - Create a function `getWsUrl()` that dynamically returns the URL:
      - Protocol: `window.location.protocol === 'https:' ? 'wss:' : 'ws:'`
      - Hostname: `window.location.hostname`
      - Port: Check `import.meta.env.VITE_DAEMON_PORT` with a fallback to `'3000'`
    - Update `connect()` to instantiate the socket using `getWsUrl()`.
    - Ensure `clientSessionKey=web-client` query parameter is maintained.

    Gap reason: "The web UI is hardcoded to connect to `ws://localhost:3000`. If you access the web UI via a different host (like 127.0.0.1, or a network IP), the websocket connection fails because the host browser cannot resolve localhost to the container."

  </action>
  <verify>Run `npm run typecheck --workspace=packages/web` (or equivalent) to ensure no type errors.</verify>
  <done>The WebSocket URL is constructed using `window.location.hostname` and the client connects to the correct host.</done>
</task>

</tasks>

<verification>
Review the code changes to ensure `window.location.hostname` is utilized.
</verification>

<success_criteria>

- `packages/web/src/lib/ws.ts` is updated.
- The WebSocket URL is fully dynamic based on the browser's current location and environment configuration.
  </success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-docker/01-06-SUMMARY.md`
</output>
