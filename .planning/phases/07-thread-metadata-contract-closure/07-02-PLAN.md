---
phase: 07-thread-metadata-contract-closure
plan: "02"
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - packages/server/src/server/thread/thread-registry.test.ts
  - packages/server/src/server/daemon-e2e/thread-management.e2e.test.ts
autonomous: true

must_haves:
  truths:
    - "Unit test confirms getActiveThread() returns null when no active thread"
    - "Unit test confirms getActiveThread() returns the active thread record after createThread"
    - "E2e test confirms ensure_default_terminal_response includes real projectId and resolvedThreadId after thread creation"
    - "All existing thread-registry tests still pass"
  artifacts:
    - path: "packages/server/src/server/thread/thread-registry.test.ts"
      provides: "getActiveThread unit tests"
      contains: "getActiveThread"
    - path: "packages/server/src/server/daemon-e2e/thread-management.e2e.test.ts"
      provides: "ensure-default metadata e2e test"
      contains: "resolvedThreadId"
  key_links:
    - from: "packages/server/src/server/thread/thread-registry.test.ts"
      to: "packages/server/src/server/thread/thread-registry.ts"
      via: "getActiveThread() calls"
      pattern: "getActiveThread"
    - from: "packages/server/src/server/daemon-e2e/thread-management.e2e.test.ts"
      to: "packages/server/src/client/daemon-client.ts"
      via: "ensureDefaultTerminal() response"
      pattern: "resolvedThreadId"
---

<objective>
Add unit and e2e tests verifying the ensure-default metadata contract: getActiveThread() correctness and real projectId/resolvedThreadId in the ensure-default response.

Purpose: Prevent regression of THRD-01/THRD-02 contract. Tests serve as executable verification criteria.
Output: Passing tests that prove the metadata flows from server registry through the ensure-default response.
</objective>

<execution_context>
@~/.config/Claude/get-shit-done/workflows/execute-plan.md
@~/.config/Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-thread-metadata-contract-closure/07-01-SUMMARY.md
@packages/server/src/server/thread/thread-registry.test.ts
@packages/server/src/server/daemon-e2e/thread-management.e2e.test.ts
@packages/server/src/client/daemon-client.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add getActiveThread unit tests to thread-registry.test.ts</name>
  <files>packages/server/src/server/thread/thread-registry.test.ts</files>
  <action>
    Add a new `describe('getActiveThread')` block to the existing test file. Follow the existing test patterns (use `tmpdir`, `ThreadRegistry` constructor, etc.).

    Tests to add:
    1. "returns null when no threads exist" — fresh registry, call getActiveThread(), expect null
    2. "returns null when active pointer is cleared" — create thread, delete it, call getActiveThread(), expect null
    3. "returns the active thread after createThread" — create a thread, call getActiveThread(), expect the thread record with matching projectId and threadId
    4. "returns the switched-to thread after switchThread" — create two threads, switch to second, call getActiveThread(), expect second thread

    Look at existing tests in the file for the exact setup pattern (how ThreadRegistry is instantiated with a temp dir, how createThread is called, etc.).
  </action>
  <verify>cd packages/server && bun test src/server/thread/thread-registry.test.ts 2>&1 | tail -20</verify>
  <done>All thread-registry tests pass including new getActiveThread tests; no existing tests broken</done>
</task>

<task type="auto">
  <name>Task 2: Add ensure-default metadata e2e test</name>
  <files>packages/server/src/server/daemon-e2e/thread-management.e2e.test.ts</files>
  <action>
    Add a new test to the existing thread-management e2e test file that verifies ensure_default_terminal_response includes real projectId and resolvedThreadId.

    Test name: "ensure-default response includes real projectId and resolvedThreadId after thread creation"

    Test steps:
    1. Create a project + thread via the daemon client (follow existing test patterns)
    2. Call `client.ensureDefaultTerminal()` (already exists in daemon-client.ts)
    3. Assert `response.projectId` equals the created thread's projectId
    4. Assert `response.resolvedThreadId` equals the created thread's threadId
    5. Assert `response.terminal` is non-null (terminal was created)

    Check `packages/server/src/client/daemon-client.ts` around line 2573 to see the `ensureDefaultTerminal` return type — it returns `EnsureDefaultTerminalPayload`. Check what fields are on that type and update it if `projectId`/`resolvedThreadId` are missing from the return type.

    Look at existing tests in thread-management.e2e.test.ts for the exact setup pattern (createDaemonTestContext, project creation, thread creation, etc.). The test should be inside a `describe` block with `shouldRun` guard (same as existing tests).

    Note: e2e tests are excluded from the default vitest run (see vitest.config.ts). Run with:
    `cd packages/server && bun test src/server/daemon-e2e/thread-management.e2e.test.ts`
    (only runs if not in CI — `shouldRun` guard)
  </action>
  <verify>cd packages/server && bun test src/server/daemon-e2e/thread-management.e2e.test.ts 2>&1 | tail -30</verify>
  <done>E2e test passes (or is skipped in CI); response.projectId and response.resolvedThreadId match the created thread</done>
</task>

</tasks>

<verification>
```bash
# Unit tests
cd packages/server && bun test src/server/thread/thread-registry.test.ts

# E2e test (skipped in CI, runs locally)
cd packages/server && bun test src/server/daemon-e2e/thread-management.e2e.test.ts

# Full server test suite (no regressions)
cd packages/server && bun test --exclude '**/daemon-e2e/**' 2>&1 | tail -10
```
</verification>

<success_criteria>
- getActiveThread unit tests: 4 cases, all pass
- E2e test: ensure-default response contains real projectId + resolvedThreadId
- Full server test suite: 752+ tests pass, 0 failures
</success_criteria>

<output>
After completion, create `.planning/phases/07-thread-metadata-contract-closure/07-02-SUMMARY.md`
</output>
