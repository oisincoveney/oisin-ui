---
phase: 09-diff-panel-redesign
plan: 04
type: execute
wave: 2
depends_on:
  - 09-03
files_modified:
  - packages/web/src/components/diff-panel.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "DiffPanel renders a 'Staged (N)' collapsible section and an 'Unstaged (N)' collapsible section"
    - "Empty sections are hidden entirely"
    - "When both sections are empty, 'No changes' text is shown"
    - "Inline accordion diff expansion still works (DiffFileSection unchanged)"
    - "Files within each section are sorted alphabetically"
  artifacts:
    - path: "packages/web/src/components/diff-panel.tsx"
      provides: "Two collapsible sections: Staged and Unstaged, driven by stagedFiles/unstagedFiles from cache"
      contains: "Staged"
  key_links:
    - from: "packages/web/src/components/diff-panel.tsx"
      to: "packages/web/src/diff/diff-store.ts"
      via: "stagedFiles and unstagedFiles props from DiffCacheEntry"
      pattern: "stagedFiles.*unstagedFiles"
    - from: "packages/web/src/components/diff-panel.tsx"
      to: "packages/web/src/components/diff-file-section.tsx"
      via: "DiffFileSection rendered per file in each section"
      pattern: "DiffFileSection"
---

<objective>
Replace the single "Changes (N)" collapsible in `DiffPanel` with two collapsibles: "Staged (N)" and "Unstaged (N)". Hide each section if it has no files. Show "No changes" if both are empty. Wire the component to accept `stagedFiles` and `unstagedFiles` props (from the updated `DiffCacheEntry`).

Purpose: Closes the staged/unstaged UI gap. The inline accordion expansion (DiffFileSection) is already working — do NOT touch it.

Output: `DiffPanel` renders two sections driven by real staged/unstaged data from the daemon.
</objective>

<execution_context>
@~/.config/Claude/get-shit-done/workflows/execute-plan.md
@~/.config/Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-diff-panel-redesign/09-CONTEXT.md
@.planning/phases/09-diff-panel-redesign/09-03-SUMMARY.md
@packages/web/src/components/diff-panel.tsx
@packages/web/src/components/diff-file-section.tsx
@packages/web/src/diff/diff-types.ts
@packages/web/src/diff/diff-store.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update DiffPanel props and two-section layout</name>
  <files>packages/web/src/components/diff-panel.tsx</files>
  <action>
Update `DiffPanelProps` to replace `files: ParsedDiffFile[]` with:
```ts
stagedFiles: ParsedDiffFile[]
unstagedFiles: ParsedDiffFile[]
```
Keep all other props unchanged (`loading`, `error`, `updatedAt`, `onClose`, `onRefresh`, `refreshAction`).

Update the header count display: change `{files.length} changed files` to show total count: `{stagedFiles.length + unstagedFiles.length} changed files`.

Replace the single `<Collapsible>` "Changes (N)" block with two separate `<Collapsible>` blocks:

```tsx
const sortedStaged = [...stagedFiles].sort((a, b) => a.path.localeCompare(b.path))
const sortedUnstaged = [...unstagedFiles].sort((a, b) => a.path.localeCompare(b.path))
const hasNoChanges = stagedFiles.length === 0 && unstagedFiles.length === 0
```

Empty state: if `hasNoChanges && !loading`, show `<p className="text-sm text-muted-foreground">No changes</p>`.

For each non-empty section, render a `<Collapsible defaultOpen>` with the same trigger button style as the existing "Changes" section:
- Staged section (only if `sortedStaged.length > 0`): label `Staged ({sortedStaged.length})`
- Unstaged section (only if `sortedUnstaged.length > 0`): label `Unstaged ({sortedUnstaged.length})`

Each section's `<CollapsibleContent>` renders `sortedStaged` / `sortedUnstaged` files via `<DiffFileSection key={file.path} file={file} />` — exactly as the current single section does.

Remove the old `sortedFiles` variable and the old single `<Collapsible>` block.

Update all call sites: find where `<DiffPanel>` is rendered (App.tsx) and update the props — replace `files={entry.files}` with `stagedFiles={entry.stagedFiles ?? []}` and `unstagedFiles={entry.unstagedFiles ?? []}`. The `?? []` fallback handles the case where the daemon hasn't sent the new fields yet.
  </action>
  <verify>
    `cd packages/web && bunx tsc --noEmit` → zero new errors (pre-existing App.tsx errors are acceptable — see VERIFICATION.md note)
    `bun run lint --filter @oisin/web` → zero errors in diff-panel.tsx
  </verify>
  <done>
    `DiffPanel` accepts `stagedFiles` and `unstagedFiles`. Renders "Staged (N)" and "Unstaged (N)" collapsibles. Empty sections hidden. "No changes" shown when both empty. App.tsx call site updated.
  </done>
</task>

</tasks>

<verification>
- `cd packages/web && bunx tsc --noEmit` → zero new type errors introduced by this plan
- `bun run lint --filter @oisin/web -- packages/web/src/components/diff-panel.tsx` → 0 errors
- Grep: `git grep "Staged" packages/web/src/components/diff-panel.tsx` → matches "Staged ({sortedStaged.length})"
- Grep: `git grep "Unstaged" packages/web/src/components/diff-panel.tsx` → matches "Unstaged ({sortedUnstaged.length})"
- Grep: `git grep "stagedFiles" packages/web/src/components/diff-panel.tsx` → matches prop usage
</verification>

<success_criteria>
- `DiffPanel` renders two collapsible sections: "Staged (N)" and "Unstaged (N)"
- Empty sections are hidden (conditional render, not just collapsed)
- "No changes" shown when both arrays are empty and not loading
- DiffFileSection inline accordion expansion unchanged
- TypeScript clean, no new lint errors
</success_criteria>

<output>
After completion, create `.planning/phases/09-diff-panel-redesign/09-04-SUMMARY.md`
</output>
