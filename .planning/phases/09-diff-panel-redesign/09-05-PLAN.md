---
phase: 09-diff-panel-redesign
plan: 05
type: execute
wave: 2
depends_on:
  - 09-03
files_modified:
  - packages/server/e2e/diff-panel.spec.ts
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "e2e spec verifies the Unstaged section header is visible after writing an untracked file"
    - "e2e spec verifies the renamed file appears in the Staged section (git mv stages the rename)"
    - "e2e spec compiles cleanly with zero TypeScript errors"
  artifacts:
    - path: "packages/server/e2e/diff-panel.spec.ts"
      provides: "Updated assertions for Staged/Unstaged section headers"
      contains: "Unstaged"
  key_links:
    - from: "packages/server/e2e/diff-panel.spec.ts"
      to: "packages/web/src/components/diff-panel.tsx"
      via: "data-testid and section header text assertions"
      pattern: "Unstaged|Staged"
---

<objective>
Update `diff-panel.spec.ts` to assert on the new two-section structure. The test already writes an untracked file (goes to Unstaged) and does a `git mv` rename (staged — goes to Staged). Add assertions that the correct section headers appear.

Purpose: Closes the e2e spec gap. The spec currently asserts on file rows and diff content (all still valid) but has no assertion on section headers.

Output: Spec asserts "Unstaged (N)" section visible after file write; "Staged (N)" section visible after git mv.
</objective>

<execution_context>
@~/.config/Claude/get-shit-done/workflows/execute-plan.md
@~/.config/Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-diff-panel-redesign/09-CONTEXT.md
@packages/server/e2e/diff-panel.spec.ts
@packages/web/src/components/diff-panel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add section header assertions to e2e spec</name>
  <files>packages/server/e2e/diff-panel.spec.ts</files>
  <action>
The existing test flow:
1. Writes `E2E_HIGHLIGHT_FILE` (untracked) → refreshes → asserts file row visible → clicks to expand diff
2. Does `git mv README.md README.rename-e2e.md` (staged rename) → reopens panel → refreshes → asserts rename label visible
3. Reverts changes → final read-only check

Add these assertions at the appropriate points (do NOT restructure the test — insert inline):

**After `highlightedRowPath` is visible (step 1, after refresh):**
```ts
// Untracked file appears in Unstaged section
await expect(panel.getByText(/^Unstaged \(\d+\)/)).toBeVisible({ timeout: 10_000 })
```

**After `renameLabel` is visible (step 2, after git mv + refresh):**
```ts
// git mv stages the rename — it should appear in Staged section
await expect(panel.getByText(/^Staged \(\d+\)/)).toBeVisible({ timeout: 10_000 })
```

Use regex matchers (`/^Staged \(\d+\)/`) so the assertion is count-agnostic — it matches "Staged (1)", "Staged (2)", etc.

Do NOT change any existing assertions, selectors, or test structure. Only add the two new `expect` lines at the described insertion points.
  </action>
  <verify>
    `cd packages/server && bunx tsc --noEmit -p tsconfig.server.typecheck.json` → zero errors
  </verify>
  <done>
    Spec has two new section header assertions. TypeScript clean.
  </done>
</task>

</tasks>

<verification>
- `cd packages/server && bunx tsc --noEmit -p tsconfig.server.typecheck.json` → 0 errors
- `git grep "Unstaged" packages/server/e2e/diff-panel.spec.ts` → matches
- `git grep "Staged" packages/server/e2e/diff-panel.spec.ts` → matches
</verification>

<success_criteria>
- Spec asserts "Unstaged (N)" visible after untracked file write
- Spec asserts "Staged (N)" visible after git mv (staged rename)
- All existing assertions preserved
- TypeScript clean
</success_criteria>

<output>
After completion, create `.planning/phases/09-diff-panel-redesign/09-05-SUMMARY.md`
</output>
