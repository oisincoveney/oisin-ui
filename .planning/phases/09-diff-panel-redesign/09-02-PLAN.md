---
phase: 09-diff-panel-redesign
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - packages/server/e2e/diff-panel.spec.ts
autonomous: true

must_haves:
  truths:
    - "e2e spec passes against the updated DiffPanel layout"
    - "diff-file-row click still expands the diff content"
    - "diff-file-path still shows the file path (including rename label)"
    - "diff-refresh-button still triggers a refresh"
    - "d2h diff content still renders after clicking a file row"
  artifacts:
    - path: "packages/server/e2e/diff-panel.spec.ts"
      provides: "Updated e2e spec compatible with Plan 09-01 layout changes"
  key_links:
    - from: "packages/server/e2e/diff-panel.spec.ts"
      to: "packages/web/src/components/diff-panel.tsx"
      via: "data-testid selectors"
      pattern: "diff-panel|diff-refresh-button|diff-file-row"
---

<objective>
Verify and update the e2e spec for the Plan 09-01 layout changes.

Purpose: The "Changes (N)" collapsible section and commit bar are new DOM structure. Confirm existing testid selectors still resolve correctly; fix any that broke.
Output: diff-panel.spec.ts that passes against the updated layout.
</objective>

<execution_context>
@~/.config/Claude/get-shit-done/workflows/execute-plan.md
@~/.config/Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-diff-panel-redesign/09-CONTEXT.md
@packages/server/e2e/diff-panel.spec.ts
@packages/web/src/components/diff-panel.tsx
@packages/web/src/components/diff-file-section.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and update diff-panel.spec.ts for new layout</name>
  <files>packages/server/e2e/diff-panel.spec.ts</files>
  <action>
Read the current spec and the updated diff-panel.tsx / diff-file-section.tsx from Plan 09-01. Identify any selector mismatches and fix them.

**Known structural changes from Plan 09-01:**
- Files are now inside a `Collapsible` with a "Changes (N)" trigger button. The collapsible is `defaultOpen`, so `diff-file-row` elements are visible without clicking the section header. No selector change needed for file rows.
- A commit bar (`<form>` with disabled Input + Button) is now above the file list. It contains no testid — no conflict.
- `diff-files-count` `<p>` in the header still exists — no change.
- `diff-refresh-button` still exists in the header — no change.
- `diff-file-row`, `diff-file-path`, `diff-file-additions`, `diff-file-deletions`, `diff-file-content` testids are all preserved in DiffFileSection — no change.

**Likely no changes needed.** Verify by reading the spec assertions against the updated component structure:

1. `panel.getByTestId("diff-refresh-button")` — still present in DiffPanel header ✓
2. `panel.getByTestId("diff-file-path").filter({ hasText: E2E_HIGHLIGHT_FILE })` — still present in DiffFileSection ✓
3. `panel.getByTestId("diff-file-row").filter({ hasText: E2E_HIGHLIGHT_FILE }).click()` — still present ✓
4. `panel.getByTestId("diff-file-content").first()` — still present in DiffFileSection ✓
5. `panel.locator(".d2h-code-line-ctn").first()` — still rendered inside diff-file-content ✓
6. Rename label assertion via `diff-file-path` — still present ✓

If all selectors are confirmed intact, make NO changes to the spec. Add a comment at the top of the test body:
```ts
// Layout updated in Phase 09: commit bar + Changes collapsible added.
// All testids preserved — no selector changes required.
```

If any selector is broken (e.g. a button text changed, a testid moved), fix only that selector. Do NOT restructure the test.

After making any changes (or confirming none needed), run a TypeScript check:
`npx tsc --noEmit -p packages/server/tsconfig.json`
  </action>
  <verify>npx tsc --noEmit -p packages/server/tsconfig.json 2>&1 | head -20</verify>
  <done>diff-panel.spec.ts compiles cleanly. All selectors confirmed valid against updated layout. Comment added if no changes were needed.</done>
</task>

</tasks>

<verification>
Run: `npx tsc --noEmit -p packages/server/tsconfig.json`
Expected: zero errors.
</verification>

<success_criteria>
- diff-panel.spec.ts compiles with zero TypeScript errors
- All existing testid selectors confirmed valid (or fixed if broken)
- No structural test changes beyond selector fixes
</success_criteria>

<output>
After completion, create `.planning/phases/09-diff-panel-redesign/09-02-SUMMARY.md`
</output>
