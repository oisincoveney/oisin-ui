---
phase: 09-diff-panel-redesign
plan: 09
type: execute
wave: 3
depends_on: ["09-06", "09-07", "09-08"]
files_modified:
  - packages/web/src/components/diff-panel.tsx
  - packages/web/src/components/diff-file-section.tsx
autonomous: false
gap_closure: true

must_haves:
  truths:
    - "A git-renamed file appears in the diff file list with an old->new path label"
    - "Clicking a renamed file opens its inline diff content without broken/garbled text"
  artifacts:
    - path: "packages/web/src/components/diff-panel.tsx"
      provides: "Rename row renders with old->new label"
    - path: "packages/web/src/components/diff-file-section.tsx"
      provides: "Rename label formatting and inline open flow"
  key_links:
    - from: "packages/web/src/components/diff-file-section.tsx"
      to: "packages/web/src/components/diff-panel.tsx"
      via: "file row click handler opens inline diff"
      pattern: "onFileOpen|oldPath|newPath|rename"
---

<objective>
Re-verify and fix renamed file display after gaps 1-3 are closed.

Purpose: UAT gap 4 was blocked by upstream failures (terminal thrash, waiting snapshot, not-a-git-repo). Now that those are resolved by plans 06-08, this plan exercises the rename path with a real staged rename fixture and fixes any remaining label or inline-open issues.
Output: Renamed files render with clear old→new path label; inline diff opens without broken content.
</objective>

<execution_context>
@~/.config/Claude/get-shit-done/workflows/execute-plan.md
@~/.config/Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-diff-panel-redesign/09-UAT.md
@packages/web/src/components/diff-panel.tsx
@packages/web/src/components/diff-file-section.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and fix rename label rendering</name>
  <files>packages/web/src/components/diff-file-section.tsx
packages/web/src/components/diff-panel.tsx</files>
  <action>
Stage a git rename locally (`git mv src/foo.ts src/bar.ts && git add -A`) and open the diff panel to observe the rename row.

Inspect how the diff payload represents renames — look for `oldPath`/`newPath` or `status: 'renamed'` fields on file entries. In `diff-file-section.tsx` (and/or `diff-panel.tsx`), find where file rows are rendered.

Fix the rename label so it shows `oldPath → newPath` (e.g. `foo.ts → bar.ts`). Use the short basename for display but ensure the full path is used for the inline diff open call.

If the inline diff open for a renamed file passes the wrong path (causing garbled/empty content), fix the path argument to use `newPath` (the post-rename path) when calling the diff viewer.

Do NOT change the diff data fetching logic — only the display label and the path passed to the inline open handler.
  </action>
  <verify>
    1. `cd packages/web && bun run typecheck` — no errors
    2. Stage a rename (`git mv` + `git add`) in any repo, open diff panel — renamed file row shows `old → new` label
    3. Click the renamed file row — inline diff opens with real content (not empty/garbled)
  </verify>
  <done>Renamed file row displays old→new path label; inline diff opens correctly; TypeScript clean</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Rename row label and inline open fix in diff-file-section/diff-panel</what-built>
  <how-to-verify>
    1. In any repo, run: `git mv src/some-file.ts src/renamed-file.ts && git add -A`
    2. Open the diff panel in the app
    3. Confirm the staged section shows a row with `some-file.ts → renamed-file.ts` (or similar old→new format)
    4. Click the row — confirm inline diff content loads without broken text
    5. Confirm no console errors related to rename path resolution
  </how-to-verify>
  <resume-signal>Type "approved" or describe remaining issues</resume-signal>
</task>

</tasks>

<verification>
- `bun run typecheck` passes in packages/web
- Renamed file row shows old→new label in staged section
- Inline diff for renamed file opens with real content
</verification>

<success_criteria>
UAT gap 4 closed: renamed files display correctly and open inline diff without errors. All four UAT truths now verifiable.
</success_criteria>

<output>
After completion, create `.planning/phases/09-diff-panel-redesign/09-09-SUMMARY.md`
</output>
